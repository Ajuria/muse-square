---
// /notifications — MIGRÉE (vue équipe inc 9a, owner 28/08) : la carte Alertes vit dans
// Mon compte → Communication. Cette page ne fait plus que rediriger — le formulaire,
// son script et ses endpoints (/api/notifications/*) sont désormais dans profile.astro.
export const prerender = false;
return Astro.redirect("/profile?tab=comm", 302);
---
