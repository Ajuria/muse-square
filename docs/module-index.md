# Module Index — Single Source of Truth

> **Purpose.** This file is the authoritative map of every server endpoint, library module, script and frontend surface in the app. It exists to stop two failure modes: (1) **building a feature that already exists**, and (2) **guessing at what a file does instead of reading it**.
>
> **Rule before you build.** Before creating any new API route, lib module, or client script, **grep this file first** for the capability (e.g. `sales`, `competitor`, `commitment`, `sensitivity`). If a row already covers it, extend that file — do not create a parallel one.
>
> **Rule for truth.** Every row below was generated by reading the actual file: HTTP handlers are the real `export const GET/POST/...`; data sources are the literal BigQuery table/view identifiers and external hosts found in the code. This file is a **map, not a contract** — when you touch a file, still open it and quote the exact lines. If you change a file's handlers or data sources, update its row here in the same commit.
>
> _Generated 2026-07-09 by reading every file under `src/pages/api`, `src/lib`, `src/scripts`, and the frontend surfaces. Regenerate the same way when it drifts._

## Data access — the truth about how the app reads data

- BigQuery client: [`src/lib/bq.ts`](../src/lib/bq.ts) → `makeBQClient(projectId)`. Credentials from `GOOGLE_SERVICE_ACCOUNT_KEY` (inline JSON) or `GOOGLE_APPLICATION_CREDENTIALS` (keyfile). **The project id is passed per-call by the caller** — it is not hardcoded in `bq.ts`.
- App data project: `muse-square-open-data` (EU). `ms-database-472505` is billing-only — never queried for app data.
- Datasets observed in code: `semantic` (`vw_*` views — the app's read layer), `mart` (`fct_*` facts), `raw` (source/ingested + user state), `analytics` (app-write state: logs, drafts, commitments, channel configs, sensitivity store), `dims` (`dim_*`), `open_data`.
- External services called: Anthropic API (`api.anthropic.com/v1/messages`), Browserless (`production-sfo.browserless.io`), Google Places / Business Profile, Meta Graph, Slack, Resend (email), BestTime, French BAN (`api-adresse.data.gouv.fr`), dbt Cloud (`ym384.us1.dbt.com`).

---

## API endpoints

Columns: **Route** · **Method(s)** · **What it does** · **Primary data source (literal identifiers)**

### `insight/` — core insight surfaces

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `insight/action-log.ts` | POST | Records user action-log events | `analytics.action_log` |
| `insight/action-request.ts` | POST | Logs user action requests + webhook notify | `insight_event.action_requests` |
| `insight/analogs.ts` | GET | Historical similar-day revenue comparison | `mart.fct_client_day_analogs` |
| `insight/sales-breakdown.ts` 🆕 | GET | **Card-specific** sales drill-down ("Ce qui a fait la journée") — top category MOVERS: signal-day vs same-weekday trailing median (n≥3), plus `revenue_share` (mix %) + `revenue_rank`. Reads ONLY the offering mart; **no `assembleDayContext`**; honest-absence (no category rows → `found:false`) | `mart.fct_client_offering_daily` |
| `insight/weather-sensitivity.ts` 🆕 | GET | **Card-specific** weather drill-down ("Votre réponse à la météo") — the venue's OWN weather response from its TRAILING history (Engine-2 direct comparable-day association, NOT the thin `b_sensitivity_store`, NOT the brain). Day's dominant condition (`fct_location_context_daily.lvl_*`) → (1) forward forecast window (`fct_location_weather_forecast_daily_detail`, +peak/extreme day), (2) CHAIN decomposition condition-days vs typical day — footfall→conversion→panier→CA (`fct_client_daily_performance`), (3) product movers ride/don't-ride the condition (`fct_client_offering_daily`, star+laggard). All `transaction_date <= @date`. Honest-absence: `n_cond < 5` → `chain:null` (forecast only, no fabricated effect) **and product movers suppressed (same floor)**; `n_extreme` flags a thin peak day. Accepts `signal_type`: HAZARD card types (`weather_hazard_onset`/`weather_worsened`/`extended_bad_weather_3d`) exclude heat from dominant-condition detection (never lead a wind/rain card with heat) | `mart.fct_location_context_daily`, `mart.fct_location_weather_forecast_daily_detail`, `mart.fct_client_daily_performance`, `mart.fct_client_offering_daily` |
| `insight/audience.ts` 🆕 | GET | **Shared card-detail** "Votre audience" — who/when/how-long, from the SEMANTIC layer: `vw_insight_event_ai_location_context` (primary_audience_1/2, geographic_catchment, capacity_sensitivity, besttime_dwell_time_*) + `vw_insight_event_day_surface` (ft_peak_hour, ft_avg_busyness_pct, audience_availability_label). Honest-absence per field; `found:false` when all empty. NO per-customer CA (no customer key exists) | `semantic.vw_insight_event_ai_location_context`, `semantic.vw_insight_event_day_surface` |
| `insight/tourism.ts` 🆕 | GET | **Card-specific** TOURISM drill-down (Bucket B) using the analyst's NEW tourism models — "who visits my region, who's surging, how to capture them". Region NUTS2 (`vw_insight_event_ai_location_context.region_code_nuts2`, e.g. FR10) → foreign nationalities by nuitées + YoY (`mart.fct_region_foreign_country_profile`) + in-season signal (`mart.fct_foreign_tourism_context_daily`). Leads on volume+YoY (drops the mis-scaled share); frames as "votre région" not the venue's visitors. Growing segments (YoY≥20%) + rule-based Prochaines étapes (anglais/cibler croissance/caler sur pics) | `semantic.vw_insight_event_ai_location_context`, `mart.fct_region_foreign_country_profile`, `mart.fct_foreign_tourism_context_daily` |
| `insight/competitor.ts` 🆕 | GET | **Card-specific** COMPETITOR drill-down — "what competitors DO that impacts me + what to do". TRUTH-FIRST: followed entities all below the real-competitor bar (overlap < 40%) → **État A** honest empty state (state the overlap ceiling + "follow real competitors", no fabricated rivalry). **État B** (real competitors ≥ 40%): actionable positioning (terrain commun / votre atout / le leur, from audience+event_type match + crawled differentiators + top competitor rating) + recent price/offering moves (`int_competitor_offering_changes`) → rule-based specific response per move (price cut → don't match; new offering → counter). Event moves excluded (belong to Events). `f10c3e58` = État A (max overlap 33%) | `mart.fct_competitor_threat_profile`, `intermediate.int_competitor_offering_changes` |
| `insight/events.ts` 🆕 | GET | **Card-specific** EVENTS drill-down ("Paysage événementiel") for event organizers — classifies the nearby landscape **cannibalise / à capitaliser / neutre** from `audience_overlap × category` (high overlap + same category = cannibalise; high + different = capitalise; low = neutre), + crawled `event_examples` (my types, block 2, no attendance benchmark — honest gap), + weekly calendar density (quiet/busy windows, forward), + Prochaines étapes (seeds M'engager). Landscape = events in a window around the card date (`[−30, +120]` days); high-overlap cutoff 40% | `semantic.vw_insight_event_ai_location_context` (my types/crawl), `mart.fct_competitor_events_conflicts` (overlap + dates) |
| `insight/footfall.ts` 🆕 | GET | **Card-specific** FOOTFALL drill-down ("Fréquentation · votre horloge du CA") — SALES-ANCHORED. Leads on the venue's OWN hourly revenue curve for the card's weekday (`semantic.vw_insight_event_client_hourly_profile.avg_revenue`, built from real `transaction_hour`) + weekly CA by day (busy/quiet). BestTime peak (`mart.fct_location_foot_traffic_daily.peak_hour`) is a SECONDARY cross-check, FLAGGED when it diverges ≥4h from where the money is ("fiez-vous à votre CA, pas à l'affluence externe"). `f10c3e58` = morning money-clock (pic 6h) vs BestTime evening (18h) → diverge. Types: `best_day_of_week` + 6 `ft_peak_*`/`ft_quiet_*` (NOT `footfall_vs_basket_decomposition` → that's renderSales). **Now a THIN wrapper over `lib/insightFamilies/footfall.ts` (the shared provider) — response byte-identical.** | `semantic.vw_insight_event_client_hourly_profile`, `mart.fct_location_foot_traffic_daily` |
| `insight/sales-discount.ts` 🆕 | GET | **Card-specific** drill-down for `sales_discount_no_lift` ("Remises sans effet"). Do discount days earn MORE? Compares CA-vs-30d-avg on the venue's high- vs low-discount days over ~90d. `f10c3e58` = high-discount days +1,3% vs low-discount +12,0% → discounts don't lift CA (wasted margin). ASSOCIATIVE caveat (may discount BECAUSE slow). One of the 5 GENERATED perf cards — now `renderSalesDiscount`. NOTE: `sales_traffic_not_converting` (the other gap) is NOT built — conversion/visitor data is 100% NULL for f10c3e58 (Kaggle set has no visitor counts) | `mart.fct_client_sales_signals_daily` (discount_rate, discount_rate_baseline, discount_rate_delta_pct, revenue_vs_30d_avg_pct, daily_revenue, daily_discount_total) |
| `insight/weather-window.ts` 🆕 | GET | **Card-specific** drill-down for `extended_bad_weather` ("Météo prolongée — votre fenêtre") — treats it as a WINDOW to plan around, INCLUDING heat (a July canicule is the archetype; excluding heat was the bug). Detects the run of consecutive extreme days from the card date (any `lvl_* ≥2`), its dominant feature + peak, then MEASURES the venue's OWN CA on past days with that condition vs without (truth-first: for a café, heat is an OPPORTUNITY). `f10c3e58` = 7-day canicule 13→19/07, CA **+29%** (n=11, txns+18/basket+8) → capitalize. n-gated (≥5). → `renderWeatherWindow`. NOTE: phantom `extended_bad_weather_3d` in the `_isWeather` detector is dead (real SPECS type is `extended_bad_weather`) | `mart.fct_location_context_daily` (lvl_*), `mart.fct_location_weather_forecast_daily_detail` (apparent tmax), `mart.fct_client_sales_signals_daily` |
| `insight/sales-decomp.ts` 🆕 | GET | **Card-specific** drill-down for `footfall_vs_basket_decomposition` ("Ventes ou panier — d'où vient le mouvement"). Decomposes the day's CA-vs-30d-avg into TRANSACTIONS (trafic) vs PANIER, dominant driver highlighted, PLUS the venue's own ~45d trend (which driver persistently moves the business + basket trajectory, honestly flat vs recul). `f10c3e58` = trafic-driven (21j vs 11j), basket flat → "le panier est votre marge inexploitée". One of the 5 GENERATED perf cards (`V1_ALERT_ACTION_TYPES`) — was on the old brain, now `renderSalesDecomp` | `mart.fct_client_sales_signals_daily` (daily_revenue, revenue_30d_avg, revenue_vs_30d_avg_pct, daily_transactions, transactions_delta_pct, avg_basket, basket_delta_pct, primary_revenue_driver) |
| `insight/family-report.ts` 🆕 | POST | **Generalized family report** — "sales-report but for every card family". `{location_id, date, families?}` → runs the registered `lib/insightFamilies` PROVIDERS → `{sections:[{family,title,render,data,sources}], facts}`. Rendered by the `family-report.astro` page via `MSCardKit.<render>(data)`; pooled `facts` feed an optional grounded exec-summary. **UNWIRED from chat** (the prompt Q&A now renders the card inline) — kept as the foundation for a future MULTI-family printable/on-demand digest (the true sales-report analog: aggregate ≥2 families into one shareable document). Footfall = only registered family today | via `lib/insightFamilies/*` providers |
| `insightevent/family-report.astro` 🆕 | page | **Family report SURFACE** (the sales `rapport.astro` analog, generalized). Reads `?location_id&date&family`, POSTs `insight/family-report.ts`, renders each section via `MSCardKit.render<Family>` (reconstructs `{ok:true,...}` — provider `data` has `found` but not `ok`). **Not linked from chat today** (kept for the future multi-family digest). Clerk-gated; loads `card-kit.js?v=8` | `insight/family-report.ts` |
| `insight/track-record.ts` 🆕 | GET | **Shared card-detail** "Ce qui a marché" (Engine 1) — for a card `action_type`, did past user commitments beat baseline? Counts `done/beat/missed` at COMMITMENT grain from the PRE-EXPLODE outcomes (no factor fan-out double-count) + returns `best` (best past plan text/dispositif/retro_worked → insight "Plan à reprendre"). Honest-absence `found:false` when none (a residual, never "proven") | `mart.fct_client_commitment_outcomes`, `analytics.action_commitments` (best plan) |
| `insight/best-in-class.ts` 🆕 | GET | "Lieux comparables" for the insight "Plan à essayer" cards — resolves the venue vertical → maps `action_type` → lever → vetted external case-study analogs (move X → reported outcome Y, cited) via `bestInClassStore`. Honest-absence `found:false`. An analog to try, never a promised result | `semantic.vw_insight_event_ai_location_context` (industry), `analytics.best_in_class_plays` (via `bestInClassStore`) |
| `insight/days.ts` | GET | Composes multi-day surface: signals, alerts feed, action candidates, points clés | `semantic.vw_insight_event_selected_days_surface`, `…_ai_location_context`, `…_change_feed`, `…_action_candidates`, `analytics.channel_configs` |
| `insight/enrich-context.ts` | POST | Claude web_search real-world context for a day; caches 30d | `analytics.context_enrichment` |
| `insight/enrich-event.ts` | POST | Claude web_search competitor-event enrichment; caches 30d | `dims.dim_event_enrichment` |
| `insight/find-dates.ts` | POST | Finds optimal dates by criteria (delegates to `findDates` lib) | via lib (BQ surfaces) |
| `insight/generate-action-draft.ts` | POST | Generates channel copy per signal. **Phase 3b: GROUNDED on the brain** — `assembleDayContext('brief', excludeEngines)` → `toGroundedDayPayload` is the ONE source (profile from `profile_raw`; external facts from citable_facts). The copy asserts only citable_facts; `validate_grounded_draft` rejects a fabricated competitor/event/weather/attendance/outcome (regenerate once → reject). Legal sources: citable_facts + venue identity + user instruction. Engines NEVER in a draft. The ad-hoc profile + 3 conditional fetches + raw fetch + `buildFactsBlock` DELETED | `dayContext.assembleDayContext` → (day context, single reader); `analytics.saved_drafts` (style), `insight_event.action_draft_log` |
| `insight/generate-sowhat.ts` | POST | One-sentence impact per signal via Claude Haiku | none (input-only) |
| `insight/map.ts` | GET | Map surface: nearby events, competitor alerts, mobility/road/subway disruptions | `semantic.vw_insight_event_map_signals`, `…_competitor_alerts`, `…_competitor_signals`, `open_data.event_industry_keywords_normalization`, `dims.dim_client_location` |
| `insight/monitor.ts` | GET | Monitoring dashboard: profile, signals, risk matrix, change feed, action candidates, BestTime traffic. **Phase 1: `day`(signals)+`profile` re-sourced from `dayContext.assembleDayContext` (`day_surface_raw`/`profile_raw`, slice `context`) — the brain is the single reader of day_surface + ai_location_context; monitor's own `profileQuery`/`signalsQuery` + `_profileCache` DELETED (parity-verified 82/40 cols). **Phase 1b: each day object also carries the brain's additive-rich context — `followed_competitors` (venues w/ google_rating + rating_trend), `weather_alert`/`weather_alert_fr` (acute, contextCopy), `commercial_events`, `foreign_visitors` (French), `attribution`/`attribution_fr` (delta_att ESTIMÉ chips via contextCopy) — for insight.astro to render (honest-absence per section)** | `dayContext.assembleDayContext` → `semantic.vw_insight_event_ai_location_context`, `…_day_surface` (no longer read directly); still direct: `…_change_feed`, `…_competitor_alerts`, `…_action_candidates`, `…_user_active_goal`, `…_user_activity`, `raw.saved_items`, `raw.watched_competitors`, `analytics.channel_configs`, `analytics.action_commitments` (Phase 1: suppress `action_candidates` whose `suppression_key` = an ACTIVE commitment's `origin_suppression_key`) |
| `insight/month.ts` | GET | 30-day window summary + AI window/special-days narration, deterministic fallback | `semantic.vw_insight_event_30d_window_surface`, `…_30d_surface`, `…_ai_location_context`, `…_ai_region_context` |
| `insight/prompt.ts` | POST | **Core AI prompt endpoint** (5300+ lines): month/days/selected-days narrative + entity web-search. `MS_AUTH_BYPASS=1` bypasses auth here only. **Phase 2: the DAY_WHY (day-horizon) answer is GROUNDED on the brain** — `assembleDayContext` → `toGroundedDayPayload` → packager `grounded_day` (cites only citable_facts; validator rejects ungrounded; regenerate-once → deterministic `renderDayWhyV1` floor). The old `narrative_input_day` derivation is DELETED. Model IDs via `modelFor`; classifier routed through the one client. Month/window still on `DecisionSignals` (deferred to the period-brain). **Family-LED answers: a dateless card-family question ("quand je vends le plus ?" → footfall) is promoted from the month default to the grounded day path (`resolveHorizonFromText` classifier promotion) and answered PRIMARILY from the family provider — its claim-typed facts become the citable whitelist, the day-brain's competing facts are dropped, so the answer LEADS with the asked dimension. Producer `family_grounded_claude`; a `family_deterministic` floor means the family answer is never lost to a grounded reject. Non-family day/month questions unchanged. **The response also carries `family_card:{render,data}` (the full provider card) with `primary:null` — `ie-prompt.js` renders it INLINE in the chat via `MSCardKit.<render>` (the complete hourly curve + weekly + decisions), so the detailed analysis IS the answer — no LLM summarization, no click-through. `prompt.astro` loads `card-kit.js` for this.** | `dayContext.assembleDayContext` (DAY_WHY facts) + `semantic.vw_insight_event_selected_days_surface`, `…_day_surface`, `…_30d_surface`, `…_30d_window_surface`, `…_ai_location_context`, `…_competitor_signals`, `…_mobility_disruptions`, `…_eventcalendar_event_lookup`, `semantic.vw_ms_insight_ai_decision_policy_rules`, `raw.saved_items`, `raw.saved_item_dates`, Anthropic API |
| `insight/reactions-today.ts` | GET | Type B + **four-tier context-decision service**: `tiers = { mesure, estimation, concurrence, action }` — measured / estimated / observed competition / measured action (reconduire-gated). **PURE SHAPING of the brain (`assembleDayContext`) payload — reads NOTHING directly** (Engine 1 & 2, estimation, competitors all from the brain); suppress-in-2 applied here. Legacy `factors[]` kept for engagement until it migrates | `src/lib/dayContext` (the brain — sole data source), `src/lib/sensitivityFeatures.json`, `src/lib/contextCopy`; dev `?src=seed` handled inside the brain |
| `insight/sales-report.ts` | POST | Sales report: daily series, comparison periods, category mix, context, action candidates, correlations. Named events + foreign visitors via shared `src/lib/dayContext` (no fork) | `mart.fct_client_daily_performance`, `…_client_sales_signals_daily`, `…_client_offering_daily`, `…_location_context_daily`, `…_location_daily_action_candidates`, `…_location_events_radius_daily`, `dims.dim_client_location`, `src/lib/dayContext` (named events/foreign) |
| `insight/sensitivities.ts` | GET | Returns venue sensitivity profiles (Type B) at tier level (reactions.astro harness) | `src/lib/dayContext` (the brain — `dc.sensitivities`); no direct store read |
| `insight/set-goal.ts` | POST | Creates/clears operational goal (acquisition/revenue/reputation/veille) | `analytics.goal_state` |

### `import/` — sales & location ingestion

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `import/locations.ts` | GET | Lists user-owned establishments for CSV import selection | `dims.dim_client_location` |
| `import/sales.ts` | POST | Inserts JSON sales payload (transactions + line items) | `raw.client_transactions` |
| `import/sales-csv.ts` | POST | Parses CSV/Excel, maps columns, idempotent delete-supersede load by source+dates | `raw.client_transactions` |

### `competitive/` — competitor & event tracking

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `competitive/add-competitor.ts` | POST | Insert/update competitor, crawl site, extract via Claude, geocode via Google Places | `raw.competitor_directory`, `…watched_competitors`, `…competitor_tracking`, `…competitor_events`, Google Places, Browserless, Anthropic |
| `competitive/add-event.ts` | POST | Insert watched event w/ dedup, confidence, boost logic | `raw.watched_events` |
| `competitive/check-competitor.ts` | POST | Lookup competitor by name+city | `semantic.vw_insight_event_competitor_lookup`, `raw.competitor_directory` |
| `competitive/check-event.ts` | POST | Lookup event by name+city+date w/ tolerance | `semantic.vw_insight_eventcalendar_event_lookup` |
| `competitive/competitor-crawl-history.ts` | GET | Crawl-run history (30) for a competitor | `raw.competitor_events` |
| `competitive/competitor-events.ts` | GET | User-confirmed competitor events (30d/20) | `raw.competitor_events` |
| `competitive/competitor-profile.ts` | GET | Full competitor profile + threat + cached Claude analysis | `semantic.vw_insight_event_competitor_lookup`, `mart.fct_competitor_threat_profile`, `semantic.…_ai_location_context`, `…_competitor_alerts`, `…_competitor_signals`, `raw.watched_competitors`, `…competitor_directory`, Anthropic |
| `competitive/competitor-signals.ts` | GET | Signals for date (±7d), followed list, top unfollowed threats | `semantic.vw_insight_event_competitor_signals`, `raw.watched_competitors`, `mart.fct_competitor_threat_profile`, `semantic.…_competitors_followed` |
| `competitive/search-competitor.ts` | POST | Claude web_search for competitor candidates | Anthropic (claude-haiku-4-5) + web_search |
| `competitive/search-db.ts` | POST | Local BQ search of event calendar + competitor directory, merged/deduped | `semantic.vw_insight_eventcalendar_event_lookup`, `…_competitor_lookup`, `mart.fct_competitor_directory`, `raw.watched_events`, `…watched_competitors`, `dims.dim_client_location` |
| `competitive/search-event.ts` | POST | Claude web_search for event candidates | Anthropic (claude-haiku-4-5) + web_search |
| `competitive/search-web.ts` | POST | Unified web search (events/competitors), caches to enrichment dim | `semantic.vw_insight_event_ai_location_context`, `dims.dim_event_enrichment`, Anthropic + web_search |
| `competitive/suivis.ts` | GET | All watched events + competitors for user (all locations) | `raw.watched_events`, `…watched_competitors`, `semantic.…_competitor_lookup`, `raw.competitor_events`, `…insight_event_user_location_profile` |
| `competitive/unfollow-competitor.ts` | POST | Soft-delete competitor follow + clear tracking | `raw.watched_competitors`, `…competitor_tracking` |
| `competitive/unfollow-event.ts` | POST | Soft-delete watched event | `raw.watched_events` |
| `competitive/update-competitor-url.ts` | POST | Update source URL, crawl inline, discover agenda path | `raw.watched_competitors`, `…competitor_directory`, `…competitor_events`, Browserless |

### `channels/` — outbound publishing & OAuth

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `channels/automation.ts` | GET/POST/DELETE | CRUD automation rules (signal→channel routing) | `analytics.automation_rules` |
| `channels/config.ts` | GET/POST | Read/write channel configs (OAuth tokens) per location | `analytics.channel_configs` |
| `channels/gbp-callback.ts` | GET | Google Business Profile OAuth callback | `analytics.channel_configs`, Google Business Account/Info APIs |
| `channels/gbp-connect.ts` | GET | Start GBP OAuth | none |
| `channels/internal-alert.ts` | GET/POST/DELETE | CRUD internal alert rules (note_interne/slack/email), allowlisted action_types | `analytics.internal_alert_rules` |
| `channels/internal-send.ts` | POST | One-off internal alert dispatch | `analytics.saved_drafts`, `…channel_configs` |
| `channels/meta-callback.ts` | GET | Meta OAuth callback (FB + IG dual configs) | `analytics.channel_configs`, Meta Graph |
| `channels/meta-connect.ts` | GET | Start Meta OAuth | none |
| `channels/publish.ts` | POST | Publish to channel (Slack/email/SMS/WhatsApp/GBP/FB/IG) w/ handler routing + logging | `analytics.channel_configs`, `…action_log`, `…publish_log`, Slack/Resend/Google/Meta |
| `channels/slack-callback.ts` | GET | Slack OAuth callback | `analytics.channel_configs`, Slack OAuth |
| `channels/slack-connect.ts` | GET | Start Slack OAuth | none |
| `channels/team.ts` | GET/POST/DELETE | CRUD team members + routing config | `analytics.team_members` |

### `commitments/` — "M'engager" engagement tracking

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `commitments/disposition.ts` | POST | Update disposition (fait/pas_encore) + note; also accepts `execution_quality` (complete/partial/none — the diagnosis self-check that routes the recommended move) | `analytics.action_commitments` |
| `commitments/edit.ts` | POST | Edit committed action text + owner | `analytics.action_commitments` |
| `commitments/evolution.ts` | GET | Day-grain revenue/context/residual series for commitment window; `commitment` payload also returns structured retro (`retro_worked`/`change`/`repeat`) + `created_at`/`action_done_at`/`threshold_value` (owner+date header, read/edit pre-fill) + `execution_quality`; also returns `move_stats` (per-move hit-rates for the diagnosis advice) + `best_in_class` (vetted "lieux comparables" analogs for the diagnosis panel — resolves venue vertical → lever → plays via `bestInClassStore`) | `mart.fct_client_day_residual`, `…location_context_features_daily`, `analytics.action_commitments` (latest snapshot), `mart.fct_location_action_moves` (move hit-rates), `semantic.vw_insight_event_ai_location_context` (industry), `analytics.best_in_class_plays` (via `bestInClassStore`) |
| `commitments/index.ts` | GET/POST/DELETE | List / create / soft-cancel commitments. Sets `origin_factor` (Engine-1 A↔B bridge): explicit `body.origin_factor` else derived from the action_type theme via `themeForActionType` (non-NULL). Adjustment "how" loop: POST accepts `adjustment_move`/`adjustment_note`/`parent_commitment_id` (re-commit a child on a fresh window); DELETE takes the move from the body (`stop` = Arrêter → system card reappears via suppression) | `analytics.action_commitments`, `src/lib/recoThemeMap` |
| `commitments/retro.ts` | POST | "Documenter" (Spec 2): STRUCTURED retro `retro_worked`/`retro_change`/`retro_repeat` (+ legacy `retro_note`), partial-safe merge | `analytics.action_commitments` |

### `analytics/` — logging, drafts, admin metrics

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `analytics/admin-dashboard.ts` | GET | Admin dashboard of all sites + stats | `mart.fct_admin_dashboard` |
| `analytics/admin-errors.ts` | GET | Error + crawl-failure logs for user/location | `analytics.api_error_log`, `…crawl_log` |
| `analytics/admin-feedback.ts` | GET | User feedback on signal confirmations | `analytics.signal_confirmations` |
| `analytics/admin-page.ts` | GET | Admin HTML page (Clerk-gated) | none |
| `analytics/card-states.ts` | GET | Per-card outcome states (30d) | `analytics.action_log` |
| `analytics/confirm.ts` | POST | Record signal confirm/dismiss | `analytics.signal_confirmations`, `…action_log` |
| `analytics/list-drafts.ts` | GET | List active saved drafts | `analytics.saved_drafts` |
| `analytics/pending-feedback.ts` | GET | Recent draft events (7d) | `analytics.action_log` |
| `analytics/save-draft.ts` | POST | Create/save a draft | `analytics.saved_drafts` |
| `analytics/signal-accuracy.ts` | GET | Signal accuracy metrics by type | `semantic.vw_insight_event_signal_accuracy` |
| `analytics/track.ts` | POST | Log user events (views/drafts/confirmations/outcomes) | `analytics.action_log` |

### `saved-items/` — saved events lifecycle

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `saved-items/alerts.ts` | POST | Max alert levels + top categories for saved items on dates | `semantic.vw_insight_event_change_feed` |
| `saved-items/bilan.ts` | POST | Record event outcome survey | `raw.event_outcomes` |
| `saved-items/create.ts` | POST | Create saved item + dates | `raw.saved_items`, `…saved_item_dates` |
| `saved-items/delete.ts` | POST | Soft-delete saved item + dates | `raw.saved_items`, `…saved_item_dates` |
| `saved-items/get.ts` | POST | Fetch one saved item + dates + profile | `raw.saved_items`, `…saved_item_dates`, `…insight_event_user_location_profile` |
| `saved-items/list.ts` | POST | List saved items (filter by stage, paginated) | `raw.saved_items`, `…saved_item_dates`, `…insight_event_user_location_profile` |
| `saved-items/snapshot.ts` | GET/POST | Capture/retrieve opportunity + weather snapshot at date | `semantic.vw_insight_event_day_surface`, `raw.saved_item_snapshots`, `…event_outcomes` |
| `saved-items/update.ts` | POST | Update saved item fields + optional snapshot | `raw.saved_items`, `…saved_item_dates` |

### `notifications/` — user preferences

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `notifications/get.ts` | GET | Fetch notification preferences | `raw.notification_preferences` |
| `notifications/save.ts` | POST | Save/merge notification preferences | `raw.notification_preferences` |

### `cron/` — scheduled jobs (GET-triggered)

| Route | Purpose | Data source |
|---|---|---|
| `cron/alerts.ts` | Send critical alerts (level ≥3) via Resend email | `raw.alerts`, `…saved_items`, `…insight_event_user_location_profile`, `…notification_preferences`, `…competitor_alerts` |
| `cron/bilan.ts` | Post-event retro email 7d after event end | `raw.saved_items`, `…insight_event_user_location_profile`, `…event_outcomes` |
| `cron/commitment-resolve.ts` | Resolve commitments whose window closed | `analytics.action_commitments` |
| `cron/competitor-alerts.ts` | Alerts for competitor events (conflict ≥2) overlapping saved dates | `mart.fct_competitor_events_conflicts`, `…fct_competitor_directory`, `raw.watched_events`, `…insight_event_user_location_profile` |
| `cron/crawl-best-in-class.ts` 🆕 | **Demand-drain** for the "lieux comparables" store (NOT a calendar refresh): per run crawls ≤`n` (default 1, cap 3) stale/missing cells, only for verticals with ACTIVE commitments, only cells missing or older than `ttl`/`BIC_TTL_DAYS` (90). **Selects cells + returns 200 `{status:started}` in ~1.5s, then crawls+loads in the BACKGROUND via `waitUntil`** (web_search is ~20-40s/cell — a sync response times out external pingers like cron.org free; mirrors competitor-surveillance.ts). Supersedes only cells that produced fresh plays (no wipe on transient empty). Shared crawl contract = `bestInClassCrawlCore.mjs` | Anthropic `web_search_20250305`; `analytics.action_commitments` + `semantic.vw_insight_event_ai_location_context` (active verticals) → `analytics.best_in_class_plays` |
| `cron/competitor-surveillance.ts` | Crawl competitor URLs (Browserless) → extract via Claude → write events | `raw.competitor_directory`, `…competitor_tracking`, `…competitor_events`, Browserless, Anthropic |
| `cron/daily-briefing.ts` | **Point du jour (Phase 3: GROUNDED on the brain)** — per location, `dayContext.assembleDayContext('brief')` is the ONE source of day context; the narrative is grounded (`toGroundedDayPayload` → packager `grounded_day` @ `modelFor('briefing')` Haiku, validator rejects ungrounded). Material-signal gate (no email on quiet days); yesterday score = one read (delta as citable fact). Ad-hoc score/weather/competitor queries + the raw Claude fetch + hardcoded model DELETED; crawl-health/stale dropped from the brief | `dayContext.assembleDayContext` → (day context, single reader); still direct: `raw.insight_event_user_location_profile`, `dims.dim_client_location`, `semantic.…_ai_location_context` (users), `…_day_surface` (yesterday score), `raw.saved_items`, `raw.notification_preferences` |
| `cron/daily-dispatch.ts` | Dispatch automation rules from today's change feed | `analytics.automation_rules`, `semantic.vw_insight_event_change_feed` |
| `cron/daily.ts` | Daily emails 0–7d before saved event dates | `raw.saved_items`, `…insight_event_user_location_profile`, `…notification_preferences` |
| `cron/digest.ts` | Weekly digest email (7d trends + competitor events) | `raw.insight_event_user_location_profile`, `…notification_preferences`, `dims.dim_client_location`, `semantic.…_ai_location_context`, `…_day_surface` |
| `cron/internal-alert-sweep.ts` | Process internal alert cards → Slack/email → persist | `analytics.channel_configs`, `…saved_drafts` |
| `cron/snapshot-competitors.ts` | Snapshot Google Places details for competitor locations | `raw.insight_event_user_location_profile`, Google Places |
| `cron/snapshot-homepage.ts` | Extract structured signals from institutional/media homepages via Browserless + Claude Haiku | `raw.competitor_directory`, `…competitor_tracking`, `…competitor_snapshots`, Browserless, Anthropic |
| `cron/snapshot-own-locations.ts` | Snapshot Google Places for own locations w/ place_id | `raw.insight_event_user_location_profile`, Google Places |
| `cron/sync-besttime.ts` | Weekly foot-traffic forecast from BestTime | `raw.besttime_foot_traffic`, `dims.dim_ai_context_location`, BestTime |
| `cron/sync-fb-performance.ts` | Facebook post insights → analytics | `analytics.channel_configs`, `…publish_log`, Facebook Graph |
| `cron/sync-gbp-performance.ts` | GBP performance metrics → analytics | `analytics.channel_configs`, `…publish_log`, GBP Performance |

### `profile/` — user & location management

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `profile/change-password.ts` | POST | Update password via Clerk | Clerk |
| `profile/crawl-website.ts` | POST | Crawl location site (Browserless) → extract via Claude → store enrichment | Browserless, Anthropic, `raw.insight_event_user_location_profile` (write) |
| `profile/location-add.ts` | POST | Create location, geocode via BAN, idempotent dedup | `raw.insight_event_user_location_profile`, BAN |
| `profile/location-delete.ts` | POST | Delete non-primary location | `raw.insight_event_user_location_profile` |
| `profile/location-rename.ts` | POST | Rename non-primary location | `raw.insight_event_user_location_profile` |
| `profile/location-set-primary.ts` | POST | Set location primary | `raw.insight_event_user_location_profile` |
| `profile/locations.ts` | GET | All user locations, primary first | `raw.insight_event_user_location_profile` |
| `profile/save-user.ts` | POST | Update identity in Clerk + BQ | Clerk, `raw.insight_event_user_location_profile` (write) |
| `profile/save.ts` | POST | UPSERT profile: geocode (BAN), BestTime register, website crawl, dbt trigger, dim sync | `raw.insight_event_user_location_profile` (MERGE), `dims.dim_client_location` (MERGE), BAN, BestTime, Anthropic |
| `profile/set-active-location.ts` | POST | Set active location cookie (`ms_active_location`) | none |

### `admin/`, `metro/`, `legacy/`, debug

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `admin/users.ts` | GET | List all users + aggregated location data | `raw.insight_event_user_location_profile` |
| `metro/search.ts` | GET | Search Île-de-France transit stops by prefix | `dims.dim_idf_stops_lines` |
| `legacy/legacy_days_compared_dates.ts` | GET | (legacy) selected-days comparison + points clés | `semantic.vw_insight_event_selected_days_surface`, `…_ai_location_context` |
| `legacy/legacy_month_data.ts` | GET | (legacy) 30-day window + calendar grid + points clés | `semantic.vw_insight_event_30d_window_surface`, `…_30d_surface`, `…_ai_location_context`, `…_ai_region_context` |
| `_debug-auth.ts` / `auth-inspect.ts` / `debug-auth.ts` | GET | Auth/locals inspection for debugging | none |

---

## Libraries — `src/lib/`

### Core / shared

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `insightFamilies/` 🆕 | `FAMILIES`, `familyForQuestion`, `footfallFamily`, `FamilyResult`/`FamilyProvider` | **"One provider, three consumers"** dispatch table for insight card families. Each provider reads ONE family's data ONCE → `{data (→ MSCardKit.render), facts (claim-typed → toGroundedDayPayload extraFacts), sources}`. Three consumers reuse ONE provider: (1) deep-page endpoints (`insight/<family>.ts`, thin wrappers over `.data`); (2) `insight/family-report.ts` (multi-family digest); (3) the grounded prompt Q&A — a dateless family question is answered family-LED and its **full card renders INLINE in the chat** (`prompt.ts` attaches `family_card`, `ie-prompt.js` renders it via `MSCardKit`; provider `facts` also fold in as grounded `extraFacts`). `familyForQuestion` normalizes accents ("A quel moment" == "À"); matchers require a sales/traffic context so event-date-picking ("meilleurs jours du mois") does NOT route here. Vertical slice: **footfall** registered; other 5 roll on mechanically | the `semantic`/`mart` sources of each family |
| `actionCommitments.ts` | `readLatestSnapshot`, `readMergeWrite`, `assertTermsPresent` | Read-merge-write core for commitments (CAS-retry). `COLUMN_SPEC` includes `origin_factor` (Engine-1 A↔B bridge) + structured-retro cols `retro_worked`/`retro_change`/`retro_repeat` (Spec 2) + adjustment "how" cols `adjustment_move`/`adjustment_note`/`parent_commitment_id` — drives INSERT column list + typed nulls | `analytics.action_commitments` |
| `admins.ts` | `ADMIN_USER_IDS`, `isAdmin` | Admin allowlist + checker | none |
| `bq.ts` | `makeBQClient` | BigQuery client factory (SA key / keyfile) | client setup |
| `dbt-trigger.ts` | `triggerDbtJobs`, `triggerSalesRefresh` | Fire-and-forget dbt Cloud job triggers | dbt Cloud API |
| `error-logger.ts` | `logCrawl`, `logApiError` | Fire-and-forget BQ error logger (never throws) | `analytics.crawl_log`, `…api_error_log` |
| `insighteventUrl.ts` | `buildMonthUrl`, `buildDaysUrl`, `parseSelectedDatesCsv`, `serializeSelectedDates` | Insight-event deep-link URL builder/parser | none |
| `rate-limit.ts` | `rateLimit`, `rateLimitResponse` | In-memory per-user rate limiter (per-instance) | none |
| `requireLocationOwnership.ts` | `requireLocationOwnership` | Access guard (admin or owned location_id) | none |
| `scope.ts` | `resolveOperationalScope` | Multi-site scope resolver (single / all) | none |

### Recommendation taxonomy

| Module | Key exports | Purpose |
|---|---|---|
| `reco-taxonomy.ts` | `RECO_BUCKETS`, `ACTION_TYPE_THEME`, `isFeedOnly`, `isThemeDisabled` | 5 buckets × 9 themes × 73 action cards; theme suppression |
| `recoThemeMap.ts` | `RECO_THEME_ACTION_TYPES`, `filterDisabledThemes`, `themeForActionType` | Canonical theme→action_type map (parity w/ `public/action-cards.js`); `themeForActionType` derives a commitment's `origin_factor` (theme) at capture |

### Commitments ("M'engager")

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `commitmentConstants.ts` | `WINDOW_DAYS`, `THRESHOLD_Z`, `MATERIAL_SHARE`, `GRACE_DAYS`, `RHO_FLOOR` | Single-source resolution gate constants | none |
| `commitmentContext.ts` | `assembleEvolutionExtras` | Evolution-page extras: holiday norm, context, advice. action_type track record via the brain's **`getActionRollup`** scoped sub-accessor (the ONE outcomes read, pre-explode commitment-grain COUNTIF, no double-count) — not the full assemble. Range-based named-context queries stay (legit period view); named_events are RELEVANCE-GATED (drop standing admin listings: `event_duration_days > 90` / theme `Education / Formation`) | `mart.fct_client_day_residual`, `…location_context_features_daily`, `…location_events_topn_daily`, `…foreign_tourism_context_daily`, `…client_daily_performance`, `src/lib/dayContext` (`getActionRollup`) |
| `commitmentCopy.ts` | `EVOL_COPY` | Canonical French copy for engagement UI | none |
| `commitmentOrigins.ts` | `COMMITMENT_ORIGIN_ACTION_TYPES`, `isCommitmentOrigin` | Allowlist of action types that seed commitments | none |
| `commitmentResolve.ts` | `resolveCommitment` | Statistical resolution: window metric, z w/ autocorr VIF, holiday gate. Also computes `window_active_factors` (CSV) — registry factors active on ≥ `WINDOW_FACTOR_SHARE` (0.50) of window days, via the single-source registry predicates against its declared `context_table` | `mart.fct_client_day_residual`, `…location_context_features_daily`, `mart.fct_location_context_daily` (factor predicates), `src/lib/sensitivityFeatures.json` |

### Sensitivity (Type B)

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `sensitivityFeatures.json` | `revenue[]` (feature → `tier` → `predicate`/`impact_col` → `mechanism` → `fittable`) | Type B / context-decision SINGLE-SOURCE registry — one tier-tagged list drives the engine fit list/build (**`fittable` only**), the endpoint `ACTIVE_EXPR`, and the Tier-2 `impact_col` map. No second list | none (data map) |
| `dayContext.ts` | `assembleDayContext`, `getActionRollup`, `namedEventsRange`, `foreignVisitorsRange`, `isCleanFrench`, `TOURIST_COUNTRIES` | **THE BRAIN** — the ONE assembler every consumer reads; nothing reads the store/learning/opportunity marts directly. Composes Engine 2 (`getSensitivities`, w/ `active_today`), Engine 1 (learning → `actionTrackByFactor`; outcomes rollup → `actionTrackByType` via `getActionRollup` sub-accessor), Tier-2 estimation (`delta_att` → `impacts`, **suppress-in-2 applied — measured drops the prior**), competitors, **the Engine1×Engine2 `decomposition` (observed_difference, no-action baseline, independent-N tier), the static `profile` (venue context), + the `llm` contract (CLAIM-TYPED `citable_facts` measured/observed_difference/observed_proximity + `driver` observed_ranking + `forbidden` — bans causal verbs on driver/competitors/tourism; declared_weather_sensitivity ≠ measured)**. `opts.devSeed` swaps demo fixtures | Engine 2 `analytics.b_sensitivity_store` (via `sensitivityStore`); Engine 1 `mart.fct_location_commitment_learning` + `…fct_client_commitment_outcomes`; `mart.fct_location_opportunity_components_daily` (delta_att); `semantic.vw_insight_event_ai_location_context` (profile); `semantic.vw_insight_event_day_surface`, `mart.fct_location_impact_daily_mobility`, `…mobility_disruption_changes`, `…daily_action_candidates`, `…events_topn_daily`, `…competitor_threat_profile`+`…directory`, `intermediate.int_competitor_offering_changes`+`…snapshot_deltas`, `…foreign_tourism_context_daily` (school **+ public** holiday origins, deduped union); reservoir: `mart.fct_region_day_annotations_daily` (commercial events, deduped vs named events), `…weather_alerts_daily` + `…weather_forecast_daily_detail` (acute alert — distinct register, NOT suppress-in-2'd vs measured heat); **signals[] REUSED from detection: `semantic.vw_insight_event_change_feed` (observed_change) + `…vw_insight_event_action_candidates` (observed cards) — distinct grain from context{}, never re-detected**; decomposition reads `analytics.action_commitments` (windows). **Scoped `opts.slice` (full/context/brief/signals)** — light surfaces skip Engine-1/decomposition; **`brief` (Point du jour / cron) also skips competitor enrichment + delta_att estimation** (~30% faster, same citable_facts). **Phase 0.5/1: day_surface read is `SELECT *` → structured `day_surface` projection (opportunity/competition/weather-surface/attribution) + raw `day_surface_raw`; profile read is an explicit used-column projection (SAFE_OFFSET on nearest_transit_line_name) → `VenueProfile`(+`transit`,`enriched`) + raw `profile_raw`; these RAW rows are monitor's Phase-1 compatibility bridge (single-reader). Foreign origins French via `contextCopy.frCountry`. In-process SLICE-AWARE memo (2-min TTL, per loc/date/slice/devSeed; a broader cached payload serves a narrower request). SCOPE: in-process only — guaranteed win is within-invocation (multi-date + repeat in a warm process); cross-endpoint collapse on Vercel is best-effort (separate invocations), guaranteed sharing needs a shared cache (deferred)** |
| `contextCopy.ts` | `label_key` fallback strings (owner-final) | Owner French for context-decision `label_key`s where no clean mart string exists (mobility traffic fallback, events, concurrence_competitor). Mart `fact_text` is reused verbatim; this fills the gaps | none |
| `sensitivityCopy.ts` | `FEATURE_FR`, `TIER_SECTION`, `envTodayLine`, `envPeriodLine`, `actionLine`, `citeSensitivity`, `decompositionLine` | Type B French copy, tier-register aware (owner-final). `decompositionLine` = the observed-difference cite ("+X pts au-dessus de vos journées … sans action") — never a proven cause | none |
| `sensitivityStore.ts` | `getSensitivities`, `canInfluence`, `TIER_INFLUENCE` | Typed accessor for vetted sensitivity store (tier + influence gate) | `analytics.b_sensitivity_store` (→ `mart.fct_location_sensitivity` in prod) |
| `bestInClassCrawlCore.mjs` (+`.d.mts`) | `LEVER_LABELS`, `INDUSTRY_LABELS`, `INTENT_LABELS`, `userPrompt`, `callSearch`, `extractPlays`, `validate`, `SCHEMA` | SINGLE SOURCE for the best-in-class crawl CONTRACT (vocab + prompt + honesty gate + load schema). ESM, portable (no BQ/fs); shared by the standalone script (`crawl-best-in-class.mjs`) + the demand-drain cron (`cron/crawl-best-in-class.ts`) — edit the contract HERE only | none (pure); uses global `fetch` |
| `bestInClassStore.ts` | `getBestInClassPlays`, `leverForActionType`, `intentForState` | Typed accessor for the "lieux comparables" crawl store — reads vetted external case-study analogs by vertical + lever (+ optional `intent`; best-first: confidence, then named source); `leverForActionType` maps a card `action_type` → one lever; `intentForState` maps the "Votre action paie-t-elle ?" verdict (below/aligned/above) → intent (pivot/reinforce/scale). An analog to try, never a promised result | `analytics.best_in_class_plays` (demand-loaded by `crawl-best-in-class.cjs`) |

### Channels / competitive / import (sub-libs)

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `channels/internalSend.ts` | `sendSlack`, `sendEmail` | Internal RULE-alert send primitives (sealed rail) | Slack, Resend |
| `internalAlertCards.ts` | `V1_ALERT_ACTION_TYPES`, `V1_ALERT_ACTION_TYPE_SET` | V1 internal-alert allowlist (5 perf RULE cards) | none |
| `competitive/constants.ts` | `VALID_CONFIDENCE`, `VALID_INDUSTRY`, `BUCKET_MAP`, `classifySource`, … | Industry/confidence validators + source URL classifier | none |
| `competitive/geocode.ts` | `geocodeCompetitor` | Geocode via BAN, reject >30km from city | BAN API |
| `competitive/url-discovery.ts` | `discoverAgendaUrl`, `discoverTarifsUrl`, `isAgendaPath`, `isHomepagePath` | Agenda/tarif URL discovery via Browserless BQL | Browserless BQL |
| `import/salesCsv.ts` | `parseSalesCsv`, `validateGrid`, `mapHeaders`, `parseDate`, `normalizeNumber`, … | Deterministic, pure CSV parse/validate for FR POS uploads | none |
| `import/sourceMappings.ts` | `GENERIC_MAPPING`, `SOURCE_OVERRIDES`, `resolveMapping` | Per-source header→canonical field mappings | none |

---

## AI subsystem — `src/lib/ai/`

Deterministic-first pipeline: build facts → decide → render (French) → optionally package via Claude. Claude only ever sees allowlisted fields.

### Contract & runtime backbone

| Module | Key exports | Purpose |
|---|---|---|
| `msAsterContract.ts` | `MS_ASTER_CONTRACT_VERSION`, `MS_ASTER_CONTRACT` | Global non-negotiable AI invariants (MS ASTER constitution) |
| `assertions/assertions_v1.ts` | `assertNoSentenceWithoutFactIdV1` | Enforce: no surfaced sentence without a known fact_id |
| `contracts/facts_v1.ts` | `FactV1`, `LineItemV1`, `RenderLineV1`, `CoverageBlockV1` | V1 fact / line-item / coverage contract types |
| `models.ts` | `modelFor`, `ModelRole` | **SINGLE model-ID registry** — kills scattered literals + `CLAUDE_MODEL*` env reads; roles: packager/**briefing** (Haiku, daily cron)/classifier/enrichment/web_search/drafting. Sonnet 5 flagged for schema-native packager validation |
| `groundedPayload.ts` | `toGroundedDayPayload`, `groundedFactStrings`, `GroundedDayPayload` | **THE grounding adapter (Phase 2)** — brain `dc.llm` envelope → grounded prompt payload (citable_facts + signals + engines + forbidden; optional `extraFacts`). The ONE interface the day prompt / Point du jour (and later drafts/reports/assistant) reuse; no re-derivation |
| `briefingGate.ts` | `isMaterialBriefing`, `BriefingGateInput` | Pure Point du jour material-signal gate (no email on quiet days) — unit-tested (`briefingGate.test.ts`); acute weather / commercial / competitor change / ≥0.3 Δscore / saved-event milestone |
| `runtime/allowlist.ts` | `ALLOWED_INPUT_FIELDS`, `pickAllowedPayload`, `assertPayloadCoverage` | Restrict Claude visibility to semantic+decision fields |
| `runtime/claude.ts` | `callClaudeMessagesAPI`, `ClaudeCallUsage` | Wrapper for Claude `v1/messages` — model via `modelFor`; **prompt caching** on the system prefix (`cacheSystem`, returns `usage` incl. `cache_read`); `userText` for raw-text (classifier) calls |
| `runtime/json.ts` | `parseJsonObjectStrict`, `stripMdCodeFence` | Strict JSON parse (strips md fences) |
| `runtime/runPackager.ts` | `runAIPackagerClaude`, `Mode`, `MonthSubMode` | Orchestrator: mode → prompt → Claude → validator. **`grounded_day` mode (Phase 2)** bypasses the field-allowlist (payload is brain-curated) + enforces the grounding validator; surfaces `usage` (cache) in debug |

### Prompts & validators (`contracts/`)

| Module | Purpose |
|---|---|
| `packagePromptV3.ts` | V3 narrative system prompt (window_top_days / compare_dates / day_why) |
| `packagerGroundedDayPrompt.ts` | **Grounded day-horizon prompt (Phase 2, VERDICT-FIRST)** — cites ONLY the brain's `citable_facts`; LEADS with a one-sentence verdict (état du jour + raison dominante), then the 2-3 facts that carry it, ranked by salience. NO action/advice (the real fired action card is attached in code). No invented numbers/entities/causes; honest-absence |
| `packagerGroundedDayValidator.ts` | **Grounding validator (Phase 2)** — HARD-rejects any surfaced number / named entity / causal construction / predicted outcome not tracing to the payload; never warn-and-pass |
| `groundingChecks.ts` | `extractNumbers`, `norm`, `makeEntityRegex`, `CAUSAL_PATTERNS`, `DRAFT_OUTCOME_PATTERNS` | Shared anti-fabrication primitives for both grounded validators. Numbers grounded by MAGNITUDE (normalizes −/–/— dashes; "−20 %" == "20 %") |
| `packagerGroundedDraftValidator.ts` | `validate_grounded_draft` | **Grounding validator for DRAFTS (Phase 3b)** — free-text channel copy; rejects a fabricated external fact (competitor/event/weather/attendance/outcome). THREE legal sources: citable_facts + venue identity + user instruction (operator's own prices/hours pass); creative voice/CTA free |
| `packagerMonthPrompt.ts` | Month orchestration prompt (window_summary vs special_days) |
| `packagerMonthSpecialDaysPrompt.ts` | Special-days narration prompt |
| `packagerMonthWindowSummaryPrompt.ts` | Month window-summary prompt |
| `packagerMonthWindowWorstDaysPrompt.ts` | Worst-days-in-window prompt |
| `packagerUiV2Prompt.ts` / `packagerUiV2Validator.ts` | UI V2 FR prompt + output validator |
| `packagerUiV3Prompt.ts` | **Empty stub (0 lines)** — placeholder, not wired |
| `packagerUiV3Validator.ts` | Validates `UiPackagingV3` contract (v=3, header, dates, sections) |
| `packagerValidators.ts` | Month narration validators (orchestrator / window_summary / special_days) |
| `uiPackagingV3.ts`, `uiPackagingV3Types.ts` | V3 UI packaging types (header, dates, sections, evidence, lookup) |

### Deterministic engines (`decision/`, `facts/`, `find_dates/`)

| Module | Key exports | Purpose |
|---|---|---|
| `decision/decision_signals.v1.ts` | `DecisionSignalsV1`, `WeatherSignalV1`, `CompetitionSignalV1`, `CalendarSignalV1` | Decision-signal types by dimension |
| `decision/day_why/day_why_v1.ts` | `renderDayWhyV1`, `DayWhyV1Input` | Deterministic day narrative: regime, competition, weather, actions |
| `decision/engines/compare_dates.ts` | `compareDatesDeterministicV1` | Rank dates by regime/score/weather/competition |
| `decision/top_days/window_top_days.ts` | `windowTopDaysDeterministic` | Top-3 dates: facts + line_items + coverage |
| `decision/worst_days/window_worst_days.ts` | `windowWorstDaysDeterministic` | Worst-3 dates: facts + line_items + coverage |
| `facts/buildCoverageFromRows.v1.ts` | `buildCoverageFromRowsV1` | Coverage builder (field presence by dimension) |
| `facts/buildFactsFromSelectedDaysRow.v1.ts` | `buildFactsFromSelectedDaysRowV1` | Fact builder from selected-days row |
| `find_dates/find-dates.ts` | `findDates`, `FindDatesParams`, `FindDatesResult` | BQ query + Claude narrative w/ scoring + quality floor |

### Impact narration (`impact_narrations/`, `impact_rules/`)

| Module | Key exports | Purpose |
|---|---|---|
| `impact_narrations/compile_month_impact_narration_v1.ts` | `compileMonthImpactNarrationV1` | Map decision signals → implications via assertion assets |
| `impact_narrations/evaluate_impact_blocks_v1.ts` | `evaluateImpactBlocksV1` | Signals → FiredBlocks (weather/calendar/competition) |
| `impact_narrations/impact_assertion_asset.v1.ts` | `IMPACT_ASSERTION_ASSETS_V1` | Truth-asset library (3 weather, 14 calendar, 8 competition) |
| `impact_narrations/impact_mapping_contract_v1.ts` | `FiredBlock`, `ImpactDimensionMappingV1` | Signal→block mapping contract |
| `impact_narrations/impact_mappings_v1.ts` | `WEATHER_MAPPING_V1`, `CALENDAR_MAPPING_V1`, `COMPETITION_MAPPING_V1` | rule_id → fired-block mappings (no text) |
| `impact_narrations/impact_narration_fr.ts` | `toImpactNarrationV1Fr` | EN tag → FR translation |
| `impact_narrations/impact_narration_v1.ts` | `ImpactNarrationV1`, `ImpactNarrationMasterV1` | Narration contract types by dimension |
| `impact_rules/v1_5/*` | `competition.rules`, `seasonality.rules`, `weather.rules`, `impact_rule_asset` | v1.5 impact rule assets per dimension |

### Rendering & UI build

| Module | Key exports | Purpose |
|---|---|---|
| `points_cles/points_cles_v1.ts` | `renderPointsClesV1` | Deterministic "Points clés" renderer (days view) |
| `render/renderLineItemsFr.v1.ts` | `renderLineItemsFrV1` | V1 IR → French text via template dispatch |
| `render/shared/render_points_cles_v1.ts` | `renderPointsClesV1` | Shared de-dup + ordering (action lines last) |
| `ui/types.ts` | `PackagerOutput`, `PackagerResult`, `SelectedDaysPackagerResult` | Packager output types |
| `ui_normalized/ui_normalized_v2.ts` | `buildUiNormalizedV2`, `normalizeWindowTopDayRow` | Normalize raw rows, month filters, per-date facts |
| `ui_packaging_v3/buildUiPackagingV3Month.ts` | `buildUiPackagingV3Month` | Deterministic month packager (truth-only sections) |

> `src/components/ai/render/shared/render_points_cles_v1.ts` and `src/components/ai/ir/lookup_ir_v1.ts` mirror/consume these on the component side.

---

## Scripts — `src/scripts/`

| Script | Purpose | Data source |
|---|---|---|
| `build-designmatrix.cjs` | Type B COMMITTED design-matrix build: residual × context → `b_real_designmatrix` (the engine's fit source). Features driven by the single-source registry `src/lib/sensitivityFeatures.json`; `is_oos` = most-recent 20%/venue. `--dry <table>` writes a scratch copy | `mart.fct_client_day_residual` + `mart.fct_location_context_daily` + `src/lib/sensitivityFeatures.json` (in) → `analytics.b_real_designmatrix` (out) |
| `sensitivity-engine.cjs` | Type B offline engine: OLS+BQML fit, VIF pre-check, per-venue contrast + consistency gates, BH FDR, tier ladders, pooled lean. REAL factor set/mechanism gate = `src/lib/sensitivityFeatures.json` (single source) | `analytics.b_v1_seed` / `b_real_designmatrix` + `src/lib/sensitivityFeatures.json` (in) → `b_sensitivity_store` / `_seedtest` (out) |
| `crawl-best-in-class.mjs` | Offline crawl for the "lieux comparables" advice slot (ESM; shares the crawl contract `src/lib/bestInClassCrawlCore.mjs` with the demand-drain cron): per (industry × lever × **intent**: pivot/reinforce/scale) web-searches reputable sources for a real case study (comparable venue applied move X → measured outcome Y), validates (drops no-source / no-outcome), loads `analytics.best_in_class_plays`. Names the venue only when the source does; external web only, never another user's data. **`MODE=refresh` = DEMAND-DRIVEN (not calendar)**: crawls only verticals with ACTIVE commitments, only cells missing or older than `TTL_DAYS` (90) — usually a no-op; supersedes just the crawled cells (WRITE_APPEND). `MODE=full` = whole rebuild (WRITE_TRUNCATE). `INDUSTRIES=`/`LEVERS=`/`INTENTS=` subset | Anthropic `web_search_20250305` (needs ANTHROPIC_API_KEY); `refresh` reads `analytics.action_commitments` + `semantic.vw_insight_event_ai_location_context` (active verticals) → `analytics.best_in_class_plays` (out); read via `bestInClassStore` |
| `ai_runtime_smoke.mjs` | Smoke test for AI packager runtime (`runAIPackagerClaude`) | needs ANTHROPIC_API_KEY |
| `print_env.mjs` | Diagnostic: prints ANTHROPIC_API_KEY prefix/len + CLAUDE_MODEL | none |
| `prompt.ts` | Client-side in-page AI prompt rendering (calls `/api/insight/prompt`) | none |
| `days.ts` | **Empty stub** | — |
| `month.ts` | **Empty stub** | — |

---

## Frontend surfaces

### App pages (`src/pages/app/`) — Clerk-gated

`insightevent/` route group: `pulse.astro`, `monitor.astro`, `insight.astro`, `days.astro`, `month.astro`, `map.astro`, `prompt.astro`, `engagement.astro`, `events.astro`, `competitor.astro`, `suivis.astro`, `reactions.astro`, `rapport.astro`. Plus `admin/admin.astro`, `index.astro`, and `insightmarketing/prompt.astro`.

> Per CLAUDE.md: `public/action-cards.js` is browser-cached by `?v=` — bump the cache-buster on the consuming surface (pulse ~348, monitor ~285, rapport ~47, insight ~158 — currently `?v=27`) and hard-refresh after edits. Astro dev does **not** reliably hot-reload API-route `.ts` — restart the dev server after server-side edits.

### Static client scripts (`public/`) — served fresh, no build/HMR, cache-busted by `?v=`

| Script | Purpose | Calls |
|---|---|---|
| `action-cards.js` (2815 ln) | **Single source of truth** for action-card branding, personalized sowhat, per-channel draft seeds, Agir dropdown. Does ALL rendering client-side from `day_surface` + `location_context`. TODO (when rolling the card-specific pattern): the `sales_surge` sowhat infers the driver from `dominant_factor`+delta fallback; the **authoritative** field is `primary_revenue_driver` (conversion/footfall/basket) on `fct_client_sales_signals_daily` — repoint then. | (rendered from data passed in) |
| `scripts/ie-prompt.js` (1188 ln) | Insight-event prompt UI harness. **`renderAiOutputHtml`: when the response carries `family_card`, renders the full family card INLINE via `MSCardKit.<render>({ok:true,...data})` before the intent branches** (footfall etc. — the detailed answer in-chat, no click). Needs `card-kit.js` loaded by `prompt.astro` | `/api/insight/prompt`, `/api/insight/monitor`, `/api/insight/find-dates`, `/api/import/locations`, `/api/import/sales-csv`, `/api/competitive/competitor-signals` |
| `map-markers.js` (543 ln) | Leaflet marker rendering for the Insight Event map (`createVenueMarker`, `createEventMarker`, `createRoadMarker`, …) | (Leaflet layers) |
| `reco-library.js` | **Owner-editable** source of the recommended action PLANS (`window.MS_SALES_RECO_LIB` + `_BY_INDUSTRY` override) — structured `{title,description,why,tag}` objects (legacy strings still accepted), rendered premium on insight "Plan à essayer"; `action-cards.js` `_planText` flattens for the M'engager form + report. Loaded before `action-cards.js` on pulse + rapport + insight | — |
| `commit-form.js` (121 ln) | Shared "M'engager" commitment form (`window.MSCommitForm`), used by pulse feed + évolution page | `/api/commitments` |
| `card-kit.js` 🆕 | **SINGLE source of the card-detail render kit** (`window.MSCardKit`): shared components (`msTable`, `msSortTable`, `msMovers`, `msStrip`, `msDecision`, `msDeltaCell`, formatters) + PURE renderers (json→HTML, no fetch/DOM): system cards `renderWeather/renderSales/renderAudience/renderTrackRecord/renderEvents/renderCompetitor/renderTourism/renderFootfall`, and the USER-generated card `renderEvolution(data, COPY)` (the commitment "Consulter l'évolution" doc — chart, decision headline, advice, capture, sources). `insight.astro` loaders + `engagement.astro` fetch then delegate here (engagement keeps only the capture/advice WIRING). Verified offline by `card-harness.html` (renders real `f10c3e58` JSON) — page == harness | consumes the `insight/*` + `commitments/evolution` card JSON |
| `card-harness.html` 🆕 (dev) | Render harness — loads `card-kit.js` with real `f10c3e58` fixtures (sales, weather rich + honest-absence, audience, track-record both branches) so card output is verified in a browser BEFORE shipping (page is Clerk-gated). Serve `public/` + open; not linked from the app | `card-kit.js` |
| `reactions.js` (67 ln) | Type B read-only "Comment votre lieu réagit" renderer; paints pre-cited lines only | `/api/insight/sensitivities` |
| `kpi.js` (19 ln) | Plausible analytics event tags (`data-kpi`, PDF downloads) | — |
| `vendor/leaflet/leaflet.js` | Self-hosted Leaflet (never CDN — per CLAUDE.md) | — |

### Reusable components (`src/components/`)

`Nav.astro`, `Footer.astro`, `InsightBottomBar.astro`, `PiloterBottomBar.astro`, `OfferCard.astro`, `SectionQuestionnaire.astro`; `ai/PackagerCard.astro`, `ai/ir/lookup_ir_v1.ts`, `ai/render/shared/render_points_cles_v1.ts`; `home/` (`Hero`, `MiniOffers`, `Proofs`, `SocialProof`).
