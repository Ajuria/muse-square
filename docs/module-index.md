# Module Index — Single Source of Truth

> **Purpose.** This file is the authoritative map of every server endpoint, library module, script and frontend surface in the app. It exists to stop two failure modes: (1) **building a feature that already exists**, and (2) **guessing at what a file does instead of reading it**.
>
> **Rule before you build.** Before creating any new API route, lib module, or client script, **grep this file first** for the capability (e.g. `sales`, `competitor`, `commitment`, `sensitivity`). If a row already covers it, extend that file — do not create a parallel one.
>
> **Rule for truth.** Every row below was generated by reading the actual file: HTTP handlers are the real `export const GET/POST/...`; data sources are the literal BigQuery table/view identifiers and external hosts found in the code. This file is a **map, not a contract** — when you touch a file, still open it and quote the exact lines. If you change a file's handlers or data sources, update its row here in the same commit.
>
> _Generated 2026-07-09 by reading every file under `src/pages/api`, `src/lib`, `src/scripts`, and the frontend surfaces. Regenerate the same way when it drifts._

## Data access — the truth about how the app reads data

- BigQuery client: [`src/lib/bq.ts`](../src/lib/bq.ts) → `makeBQClient(projectId)`. Credentials from `GOOGLE_SERVICE_ACCOUNT_KEY` (inline JSON) or `GOOGLE_APPLICATION_CREDENTIALS` (keyfile). **The project id is passed per-call by the caller** — it is not hardcoded in `bq.ts`.
- App data project: `muse-square-open-data` (EU). `ms-database-472505` is billing-only — never queried for app data.
- Datasets observed in code: `semantic` (`vw_*` views — the app's read layer), `mart` (`fct_*` facts), `raw` (source/ingested + user state), `analytics` (app-write state: logs, drafts, commitments, channel configs, sensitivity store), `dims` (`dim_*`), `open_data`.
- External services called: Anthropic API (`api.anthropic.com/v1/messages`), Browserless (`production-sfo.browserless.io`), Google Places / Business Profile, Meta Graph, Slack, Resend (email), BestTime, French BAN (`api-adresse.data.gouv.fr`), dbt Cloud (`ym384.us1.dbt.com`).

---

## API endpoints

Columns: **Route** · **Method(s)** · **What it does** · **Primary data source (literal identifiers)**

### `insight/` — core insight surfaces

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `insight/action-log.ts` | POST | Records user action-log events | `analytics.action_log` |
| `insight/action-request.ts` | POST | Logs user action requests + webhook notify | `insight_event.action_requests` |
| `insight/analogs.ts` | GET | Historical similar-day revenue comparison | `mart.fct_client_day_analogs` |
| `insight/sales-breakdown.ts` 🆕 | GET | **Card-specific** sales drill-down ("Ce qui a fait la journée") — top category MOVERS: signal-day vs same-weekday trailing median (n≥3), plus `revenue_share` (mix %) + `revenue_rank`. Reads ONLY the offering mart; **no `assembleDayContext`**; honest-absence (no category rows → `found:false`) **18/08 (journée dédiée) : WRAPPER MINCE sur le provider famille `insightFamilies/sales.ts`** — réponse superset (+ is_down/signal_types résolus du SIGNAL TIRÉ du jour, mart candidates — l'unique définition de la direction, jamais re-dérivée) | `mart.fct_client_offering_daily` |
| `insight/weather-sensitivity.ts` **THIN wrapper** over `insightFamilies/weather.weatherFamily` (same provider as report + grounded Q&A); byte-identical. 🆕 | GET | **Card-specific** weather drill-down ("Votre réponse à la météo") — the venue's OWN weather response from its TRAILING history (Engine-2 direct comparable-day association, NOT the thin `b_sensitivity_store`, NOT the brain). Day's dominant condition (`fct_location_context_daily.lvl_*`) → (1) forward forecast window (`fct_location_weather_forecast_daily_detail`, +peak/extreme day), (2) CHAIN decomposition condition-days vs typical day — footfall→conversion→panier→CA (`fct_client_daily_performance`), (3) product movers ride/don't-ride the condition (`fct_client_offering_daily`, star+laggard). All `transaction_date <= @date`. Honest-absence: `n_cond < 5` → `chain:null` (forecast only, no fabricated effect) **and product movers suppressed (same floor)**; `n_extreme` flags a thin peak day. Accepts `signal_type`: HAZARD card types (`weather_hazard_onset`/`weather_worsened`/`extended_bad_weather_3d`) exclude heat from dominant-condition detection (never lead a wind/rain card with heat) | `mart.fct_location_context_daily`, `mart.fct_location_weather_forecast_daily_detail`, `mart.fct_client_daily_performance`, `mart.fct_client_offering_daily` |
| `insight/audience.ts` **THIN wrapper** over `insightFamilies/audience.audienceFamily` (same provider as report + grounded Q&A); byte-identical. 🆕 | GET | **Shared card-detail** "Votre audience" — who/when/how-long, from the SEMANTIC layer: `vw_insight_event_ai_location_context` (primary_audience_1/2, geographic_catchment, capacity_sensitivity, besttime_dwell_time_*) + `vw_insight_event_day_surface` (ft_peak_hour, ft_avg_busyness_pct, audience_availability_label). Honest-absence per field; `found:false` when all empty. NO per-customer CA (no customer key exists) | `semantic.vw_insight_event_ai_location_context`, `semantic.vw_insight_event_day_surface` |
| `insight/dispositif.ts` 🆕 | GET | **LIGNE « En marge » REFONDUE 22/08 (owner).** Elle listait ce que la MACHINE avait écarté au lieu de dire le RÉSULTAT : « journées sans facteur météo ni calendrier » — faux de surcroît (le filtre est `!d.heat && !d.vacances && !d.weekend` : la pluie n'est pas écartée, les jours fériés non plus). Deux passes le même jour, la première refusée (« Incompréhensible ») parce qu'elle nommait encore les trois contrôles. Forme retenue : **période + nombre de journées + écart SIGNÉ portant son référentiel + l'aveu d'ignorance + la question**. Sur f10c3e58 : « En marge : du lundi 29/06 au vendredi 03/07, 5 journées de ce motif à +691 € vs votre résultat habituel, que les données disponibles n'expliquent pas. […] Avez-vous une explication ? Notez-la · sinon, laissez. » Lexique appliqué : **« la normale » est BANNI** (ligne 24 — et la page l'employait ligne 316 : une chaîne rendue n'est pas pour autant conforme, la règle 4 ne dispense pas du tableau des mots), règle 2 (un montant porte son référentiel), règle 6 (jours en toutes lettres ; `frDate` rendait déjà JJ/MM, seule l'abréviation `.slice(0,3)` fautait). Les 5 jours inexpliqués sortent en BLOC consécutif (le week-end est exclu du filtre, donc une semaine ouvrée) → dits en PÉRIODE, repli sur la liste si non contigus. « Poids faible » retiré sur arbitrage owner : contredisait le montant. **THIN wrapper** over `insightFamilies/dispositif.dispositifFamily` — la matière d'ENQUÊTE d'un motif structurel (atelier des mécanismes, page « Reproduire le dispositif gagnant », `dispositif.astro`). **Classes couvertes (03/08) : les 3 motifs d'IDENTIFICATION** — `traffic_high` (tercile haut visiteurs), `followed_activity_high` (tercile haut activité non nulle des suivis, sv_distinct>1), `competition_low` (tercile bas competition_index_local) — appartenance-jour copiée à l'identique de `dayClassAggregateSql`, libellés + question de fond par classe dans `class_meta` (la page et le system enquête les lisent là, zéro copie « affluence » en dur) ; autres classes → found:false + raison. Classe suivis : les jours inexpliqués NOMMENT les événements suivis actifs (vw_insight_event_competitor_signals, tri durée croissante, identité par clé lieu+plage, 3 max + total — dans les faits, l'infobulle de marge, et la marge ne dit plus « variation ordinaire » quand ils existent). **Cercle 1 (03/08) : movers produits des jours inexpliqués, toutes classes** — écart € par famille vs sa moyenne journalière du lieu (référentiel dit ; familles à zéro incluses ; top 3 |écart| ; requête 5e du lot parallèle). Rend : narrative (n jours, part environnement — CO-occurrences), top_days, unexplained_days AVEC vérifications internes déjà faites (amplitude horaire + mix produits du jour vs habituel — écarte « journée écourtée » et « effet produit » avant de questionner), impact du motif par getDayClassImpacts (LE chemin de politique, jamais réimplémenté). La page POSTe l'objet dispositif réel (/api/best-practices, pièce 1) avec origin structural_<classe>. CTA pulse « Reproduire le dispositif » (data-struct-page) navigue ici. **03/08 : le provider émet aussi `facts` (FamilyFact[], racine du résultat — la liste blanche chiffrée du mode enquête de `insight/prompt.ts` ; mêmes valeurs que `data`, une seule source ; le wrapper GET n'expose toujours que `data`)** | `mart.fct_location_context_daily` × `mart.fct_client_day_residual` × `mart.fct_client_daily_performance`, `raw.client_transactions`, `analytics.day_class_impacts` |
| `insight/tourism.ts` 🆕 | GET | **THIN wrapper** over `insightFamilies/tourism.tourismFamily` (same provider as the family report + grounded prompt Q&A); response byte-identical. **Card-specific** TOURISM drill-down (Bucket B) using the analyst's NEW tourism models — "who visits my region, who's surging, how to capture them". Region NUTS2 (`vw_insight_event_ai_location_context.region_code_nuts2`, e.g. FR10) → foreign nationalities by nuitées + YoY (`mart.fct_region_foreign_country_profile`) + in-season signal (`mart.fct_foreign_tourism_context_daily`). Leads on volume+YoY (drops the mis-scaled share); frames as "votre région" not the venue's visitors. Growing segments (YoY≥20%) + rule-based Prochaines étapes (anglais/cibler croissance/caler sur pics) | `semantic.vw_insight_event_ai_location_context`, `mart.fct_region_foreign_country_profile`, `mart.fct_foreign_tourism_context_daily` |
| `insight/competitor.ts` 🆕 | GET | **THIN wrapper** over `insightFamilies/competitor.competitorFamily` (same provider as the family report + grounded prompt Q&A); response byte-identical. **Card-specific** COMPETITOR drill-down — "what competitors DO that impacts me + what to do". TRUTH-FIRST: followed entities all below the real-competitor bar (overlap < 40%) → **État A** honest empty state (state the overlap ceiling + "follow real competitors", no fabricated rivalry). **État B** (real competitors ≥ 40%): actionable positioning (terrain commun / votre atout / le leur, from audience+event_type match + crawled differentiators + top competitor rating) + recent price/offering moves (`int_competitor_offering_changes`) → rule-based specific response per move (price cut → don't match; new offering → counter). Event moves excluded (belong to Events). `f10c3e58` = État A (max overlap 33%) | `mart.fct_competitor_threat_profile`, `intermediate.int_competitor_offering_changes` |
| `insight/events.ts` 🆕 | GET | **THIN wrapper** over `insightFamilies/events.eventsFamily` (same provider as the family report + grounded prompt Q&A); response byte-identical. **Card-specific** EVENTS drill-down ("Paysage événementiel") for event organizers — classifies the nearby landscape **cannibalise / à capitaliser / neutre** from `audience_overlap × category` (high overlap + same category = cannibalise; high + different = capitalise; low = neutre), + crawled `event_examples` (my types, block 2, no attendance benchmark — honest gap), + weekly calendar density (quiet/busy windows, forward), + Prochaines étapes (seeds M'engager). Landscape = events in a window around the card date (`[−30, +120]` days); high-overlap cutoff 40% | `semantic.vw_insight_event_ai_location_context` (my types/crawl), `mart.fct_competitor_events_conflicts` (overlap + dates) |
| `competitive/places.ts` 🆕 (lib) | `lookupPlace`, `distanceMeters`, `PlaceLookup` | **The ONE Google Places lookup** for competitive surfaces. `searchText` → `{place_id, display_name, lat, lon, rating, rating_count}` — location (→ REAL distance, not an LLM guess) + the GBP standing signals used to prioritise WHO to follow. FieldMask adds `rating`/`userRatingCount` on top of what add-competitor needed. Distinct from `competitive/geocode.ts` (free BAN address API, no GBP). Degrades to null on missing key/quota/no-match — enrichment is never critical. `distanceMeters` = haversine (shared home; the same math is still inlined in search-db.ts + competitor-surveillance.ts). Used by `insight/prompt.ts` (discovery follow candidates) + `competitive/add-competitor.ts` (which now wraps it instead of its own inline copy) | Google Places API (`GOOGLE_PLACES_API_KEY`) |
| `insight/corrections.ts` 🆕 | GET/POST | **Phase 2.3 increment 3 — "Ce que je retiens de vous".** GET `?location_id` → the venue's ACTIVE identity corrections; POST `{location_id, correction_type}` → appends a `clear` EVENT (never a delete — the learning corpus stays intact). Ownership-guarded (`requireLocationOwnership`, 403). Wraps `lib/ai/corrections.ts` (the one owner of the log) — no direct table access, no re-derivation. Rendered by the `ie-memory` panel in `prompt.astro` / `ie-prompt.js` (refreshes on load + after each answer, since a turn may have captured a correction) | via `lib/ai/corrections.ts` → `analytics.consulter_correction_events` (item 4: type `declared_margin_pct` valid for clear; 16/07: events carry `declarant_name` — Destinataires-roster identity, never login — + `corrected_at` surfaces to the panel/answers) ; **POST {value} (16/08)** : déclaration DIRECTE des métriques déclarées (marge 1-90 %, assert/supersede, source piloter_inline — champ inline Piloter) |
| `insight/footfall.ts` 🆕 | GET | **Card-specific** FOOTFALL drill-down ("Fréquentation · votre horloge du CA") — SALES-ANCHORED. Leads on the venue's OWN hourly revenue curve for the card's weekday (`semantic.vw_insight_event_client_hourly_profile.avg_revenue`, built from real `transaction_hour`) + weekly CA by day (busy/quiet). BestTime peak (`mart.fct_location_foot_traffic_daily.peak_hour`) is a SECONDARY cross-check, FLAGGED when it diverges ≥4h from where the money is ("fiez-vous à votre CA, pas à l'affluence externe"). `f10c3e58` = morning money-clock (pic 6h) vs BestTime evening (18h) → diverge. Types: `best_day_of_week` + 6 `ft_peak_*`/`ft_quiet_*` (NOT `footfall_vs_basket_decomposition` → that's renderSales). **Now a THIN wrapper over `lib/insightFamilies/footfall.ts` (the shared provider) — response byte-identical.** | `semantic.vw_insight_event_client_hourly_profile`, `mart.fct_location_foot_traffic_daily` |
| `insight/sales-discount.ts` **THIN wrapper** over `insightFamilies/salesDiscount.salesDiscountFamily` (same provider as report + grounded Q&A); byte-identical. 🆕 | GET | **Card-specific** drill-down for `sales_discount_no_lift` ("Remises sans effet"). Do discount days earn MORE? Compares CA-vs-30d-avg on the venue's high- vs low-discount days over ~90d. `f10c3e58` = high-discount days +1,3% vs low-discount +12,0% → discounts don't lift CA (wasted margin). ASSOCIATIVE caveat (may discount BECAUSE slow). One of the 5 GENERATED perf cards — now `renderSalesDiscount`. NOTE: `sales_traffic_not_converting` (the other gap) is NOT built — conversion/visitor data is 100% NULL for f10c3e58 (Kaggle set has no visitor counts) | `mart.fct_client_sales_signals_daily` (discount_rate, discount_rate_baseline, discount_rate_delta_pct, revenue_vs_30d_avg_pct, daily_revenue, daily_discount_total) |
| `insight/weather-window.ts` 🆕 | GET | **Card-specific** drill-down for `extended_bad_weather` ("Météo prolongée — votre fenêtre") — treats it as a WINDOW to plan around, INCLUDING heat (a July canicule is the archetype; excluding heat was the bug). Detects the run of consecutive extreme days from the card date (any `lvl_* ≥2`), its dominant feature + peak, then MEASURES the venue's OWN CA on past days with that condition vs without (truth-first: for a café, heat is an OPPORTUNITY). `f10c3e58` = 7-day canicule 13→19/07, CA **+29%** (n=11, txns+18/basket+8) → capitalize. n-gated (≥5). → `renderWeatherWindow`. NOTE: phantom `extended_bad_weather_3d` in the `_isWeather` detector is dead (real SPECS type is `extended_bad_weather`) | `mart.fct_location_context_daily` (lvl_*), `mart.fct_location_weather_forecast_daily_detail` (apparent tmax), `mart.fct_client_sales_signals_daily` |
| `insight/sales-decomp.ts` **THIN wrapper** over `insightFamilies/salesDecomp.salesDecompFamily` (same provider as report + grounded Q&A); byte-identical. 🆕 | GET | **Card-specific** drill-down for `footfall_vs_basket_decomposition` ("Ventes ou panier — d'où vient le mouvement"). Decomposes the day's CA-vs-30d-avg into TRANSACTIONS (trafic) vs PANIER, dominant driver highlighted, PLUS the venue's own ~45d trend (which driver persistently moves the business + basket trajectory, honestly flat vs recul). `f10c3e58` = trafic-driven (21j vs 11j), basket flat → "le panier est votre marge inexploitée". One of the 5 GENERATED perf cards (`V1_ALERT_ACTION_TYPES`) — was on the old brain, now `renderSalesDecomp` | `mart.fct_client_sales_signals_daily` (daily_revenue, revenue_30d_avg, revenue_vs_30d_avg_pct, daily_transactions, transactions_delta_pct, avg_basket, basket_delta_pct, primary_revenue_driver) |
| `insight/family-report.ts` 🆕 | POST | **Generalized family report** — "sales-report but for every card family". **Section D (16/07): grounded EXECUTIVE SUMMARY over the pooled family facts — the SAME grounded_day packager + validator as the chat (D4 one gate), whitelist = pooled facts (day facts blanked), one feedback regen; on reject the summary is ABSENT and the deterministic sections ship unchanged (D3 — a report never carries ungated prose); response gains `exec_summary {headline, answer, register:'vetted', facts_cited}`; telemetry `report-exec-summary` sunk. Gate: ≥3 pooled facts.** `{location_id, date, families?}` → runs the registered `lib/insightFamilies` PROVIDERS → `{sections:[{family,title,render,data,sources}], facts}`. Rendered by the `family-report.astro` page via `MSCardKit.<render>(data)`; pooled `facts` feed an optional grounded exec-summary. **UNWIRED from chat** (the prompt Q&A now renders the card inline) — kept as the foundation for a future MULTI-family printable/on-demand digest (the true sales-report analog: aggregate ≥2 families into one shareable document). Footfall = only registered family today | via `lib/insightFamilies/*` providers |
| `insightevent/family-report.astro` 🆕 | page | **Family report SURFACE** (the sales `rapport.astro` analog, generalized). Reads `?location_id&date&family`, POSTs `insight/family-report.ts`, renders each section via `MSCardKit.render<Family>` (reconstructs `{ok:true,...}` — provider `data` has `found` but not `ok`). **Not linked from chat today** (kept for the future multi-family digest). Clerk-gated; loads `card-kit.js?v=8` | `insight/family-report.ts` |
| `insight/track-record.ts` 🆕 | GET | **Shared card-detail** "Ce qui a marché" (Engine 1) — for a card `action_type`, did past user commitments beat baseline? Counts `done/beat/missed` at COMMITMENT grain from the PRE-EXPLODE outcomes (no factor fan-out double-count) + returns `best` (best past plan text/dispositif/retro_worked → insight "Plan à reprendre"). Honest-absence `found:false` when none (a residual, never "proven") **18/08 : noyau extrait dans `lib/trackRecordCore.ts`** (endpoint = wrapper ; le provider sales lit le MÊME noyau via le signal résolu — une seule définition) | `mart.fct_client_commitment_outcomes`, `analytics.action_commitments` (best plan) |
| `insight/best-in-class.ts` 🆕 | GET | "Lieux comparables" for the insight "Plan à essayer" cards — resolves the venue vertical → maps `action_type` → lever → vetted external case-study analogs (move X → reported outcome Y, cited) via `bestInClassStore`. Honest-absence `found:false`. An analog to try, never a promised result | `semantic.vw_insight_event_ai_location_context` (industry), `analytics.best_in_class_plays` (via `bestInClassStore`) |
| `insight/days.ts` | GET | Composes multi-day surface: signals, alerts feed, action candidates. **31/07 : les « points clés » retirés** — calculés par jour et jetés par le client ; l'IIFE `special_days` et les aides `nonEmptyString` / `normalizeCommercialEvents`, qui ne servaient qu'à eux, partent avec (−47 lignes). `interpretation` et `competition_context` **également retirés** (recensement complet : 0 consommateur — les 2 hits `.interpretation` du dépôt sont `blocks[i].interpretation` de `packagerValidators.ts`, autre schéma). days.ts : 1086 -> 951 lignes. Le `.map` ne pose plus que `top_competition_events`. **Deux appelants de l'endpoint, pas un** : `days.astro` et `cron/daily.ts` (email quotidien) — ce dernier ne lit que des champs BRUTS du jour (`alert_level_max`, `delta_att_*`, `holiday_name`, `vacation_name`, `opportunity_score_final_local`, `events_within_*`, `lvl_wind`), tous issus du `...day` : vérifié avant retrait. `top_competition_events` **retiré aussi** (instruit : son unique lecteur est `action-cards.js:120`, en repli de `top_competitors` — or la matrice des surfaces montre que la SEULE qui reçoit ce champ, `days.astro`, est la seule à NE PAS charger action-cards.js, et les quatre qui la chargent tirent leurs jours de `monitor.ts`, qui produit `top_competitors` et jamais ce champ ; `cron/daily.ts` ne lit ni l'un ni l'autre). Le `.map` d'enrichissement disparaît donc entièrement — la réponse expose `days: days_deduped` brut. `computeTopCompetitionEvents` partie avec ; la QUASI-JUMELLE VIVANTE `getTopCompetitionEvents` (ligne 71, appelée ligne 304) reste, mêmes buckets et même filtre industrie. **Bilan de la série du 31/07 : 1086 -> 910 lignes**, quatre champs retirés, tous calculés à chaque requête et lus par personne. Laissé en place volontairement : le repli `|| d.top_competition_events` de `action-cards.js:120` — fichier partagé par 4 surfaces, à nettoyer lors d'un prochain passage, pas pour un `||`. `recommendations` config (disabled themes) resolved ACCOUNT-level (owner 19/07: site-first-else-account, no location filter) | `semantic.vw_insight_event_selected_days_surface`, `…_ai_location_context`, `…_change_feed`, `…_action_candidates`, `analytics.channel_configs` |
| `insight/enrich-context.ts` | POST | ÉTAPE 5 (08/08) : cœur extrait vers `lib/ai/webContext.ts` (getWebDayContext — cache 30 j + crawl + écriture ; PARTAGÉ dossier de carte + chat Explorer) ; wrapper HTTP inchangé pour insight.astro. Chat : déclenché sur jour PASSÉ inexpliqué (aucun moteur actif), amorcé PENDANT la génération, plafond 12 s (un crawl plus lent raccroche en cache → enrichissement progressif au passage suivant) ; enveloppe `web_context {takeaway, key_factors, sources[]}` → bloc `websources` card-kit (ambre « Web — non vérifié », liens https-only, hostname, rel=noopener — seuls liens externes du chat) ; JAMAIS la liste blanche (zéro tier → registre causal verrouillé). Pièges mesurés : sans city_name (profile_raw, pas le profil curé) l'agent répond en prose ; facteurs OBJETS coercés + filtrés à la lecture du cache. E2E 14/07 : défilé 7 000 participants + 4 sources gouv.fr, cached=true instantané | `analytics.context_enrichment` |
| `insight/enrich-event.ts` | POST | Claude web_search competitor-event enrichment; caches 30d (écriture ATTENDUE depuis C5 — un insert non attendu se perdait sous waitUntil ; `audience_profile` désormais alimenté par le public principal, la colonne que lit la surface enrichie — mesurée 0/2084 avant). Appelé à la demande (monitor.astro) ET par la passe nocturne C5 (`runEventEnrichment`, cron snapshot-competitors). Profile enums NAMED via `profileLabels` before reaching the model (`monitor.astro` is the caller that sends them); `industry_code` is already French in the warehouse — not mapped | `dims.dim_event_enrichment` |
| `insight/find-dates.ts` | POST | Finds optimal dates by criteria (delegates to `findDates` lib) | via lib (BQ surfaces) |
| `insight/generate-action-draft.ts` | POST | Generates channel copy per signal. **Phase 3b: GROUNDED on the brain** — `assembleDayContext('brief', excludeEngines)` → `toGroundedDayPayload` is the ONE source (profile from `profile_raw`; external facts from citable_facts). The copy asserts only citable_facts; `validate_grounded_draft` rejects a fabricated competitor/event/weather/attendance/outcome (regenerate once → reject). Legal sources: citable_facts + venue identity + user instruction + **cardFacts (18/07: the card's own mart-rendered what/sowhat + draft_seed + signal payload + channel label — first-party; a faithful sales-card note was rejected by construction without it)**; seeded prompts get the analyzed date explicitly (model hallucinated its training year). Engines NEVER in a draft. The ad-hoc profile + 3 conditional fetches + raw fetch + `buildFactsBlock` DELETED | `dayContext.assembleDayContext` → (day context, single reader); `analytics.saved_drafts` (style), `insight_event.action_draft_log` |
| `insight/generate-sowhat.ts` | POST | One-sentence impact per signal via Claude Haiku. Declared-profile enums + `opportunity_regime` (A/B/C) named via `profileLabels` — never handed to the model raw | none (input-only) |
| `insight/map.ts` | GET | Map surface: nearby events, competitor alerts, mobility/road/subway disruptions | `semantic.vw_insight_event_map_signals`, `…_competitor_alerts`, `…_competitor_signals`, `open_data.event_industry_keywords_normalization`, `dims.dim_client_location` |
| `insightFamilies/evenement.ts` 🆕 (lib) | `evenementFamily(bq, location_id, saved_item_id)` | **LE provider du dossier d'événement** (spec § 2) — item + stage (decider/avant/apres) + `days[]` (les 5 QUESTIONS par date à venir : audience_availability_label ; mobilité DEUX populations delta_att_mobility_pct clients / delta_ops_mobility_car_pct fournisseurs, seuils legacy >=0/-4 ; voisins 500 m/5 km + same_bucket ; météo lvl_* × event_nature + hors-horizon J+n jamais prédit ; pression concurrence ×r) + objectif par date (apport PROPRE : attendu-du-dow 90 j × pct, ou cible € − moyenne famille) + `apres.rows` (CA vs attendu, tickets/panier vs base, famille vs moyenne, verdict de l'ENGAGEMENT ancré — jamais recalculé) + `serie` (somme mesurée jamais extrapolée) + facts (liste blanche chat). 2 étages : item→lot parallèle unique ; ~3,5 s à froid mesuré. Colonne piège attrapée par harnais : `opportunity_score_final_local` (pas opportunity_score — c'était l'ALIAS de snapshot.ts). **+ `listUserEvenements(bq, loc, clerk, limit)`** (04/08) : liste des événements de l'utilisateur avec dernière occurrence MESURÉE (residual, jointure JS — jamais de sous-requête corrélée) — sert la branche chat déterministe + `ai/facts/buildEventFacts` **05/08 (automatisation inc. 3)** : item porte les 5 champs consigne (`consigne_arrival/_store_info/_interactions/_send_offset/_enabled`) + `data.consigne_sends` (5 dernières traces d'envoi, lecture ajoutée DANS le lot parallèle) ; harnais Corner réel 8/8, 1,2 s **05/08 (inc. 8 — faits NOMMÉS + liens, feedback owner)** : 2 lectures ajoutées AU lot (perturbations nommées `vw_insight_event_mobility_disruptions` — disruption_date, ligne/arrêt/retard ; touristes étrangers `mart.fct_region_foreign_country_profile` — projection SAISONNIÈRE citée DATÉE, région NUTS2 via city_id_commune→city_to_region, UN accommodation_type, ratios 0-1 ×100) ; questions : « Le public du jour » (+ nationalités nommées), Accès nommé (pire perturbation, absence DITE), « Activité autour de vous » (label owner) + `href/link_fr` vers la carte (?location_id&date) et le détail jour (?selected_dates) ; harnais Corner 7/7, 2,0 s **10/08 (4 Fs owner)** : STAGE réparé — un non-récurrent SANS `selected_date` aux dates toutes passées devient `apres` (avant : « decider » À VIE, le mesuré jamais rendu — cas réel « Lancement SaaS », seule date 19/06 + selected_date null) ; candidates futures seulement pour rester decider ; vérifié handler direct (766 € vs 881 € attendu enfin rendus). **10/08 (2) — chaîne bilan→dispositif** : le bouton « Documenter en dispositif » PARALLÈLE au bilan supprimé (même acte en deux formulaires ; il écrivait aussi une pratique VIDE — titre pour tout contenu, cas réel archivé) ; l'offre « En faire un dispositif ? » suit le SUCCÈS du bilan (recette obligatoire si l'événement n'a pas de dispositif, evidence = mesuré réel, succès → lien Dispositifs) ; idempotence serveur : lecture `apres.documented` ajoutée AU lot (clé = suffixe d'écrivain « (événement « titre ») ») ; bilan déjà envoyé → état dit + chaîne accessible ; happy-dom 3 scénarios 9/9. **10/08 (3) — chiffres FONCTIONNELS (owner : « labels not precise »)** : chaque box porte SOURCE + RÉFÉRENTIEL (infobulle) et son REGISTRE (· mesuré / · déclaré) ; « habituel » tickets/panier passe au MÊME référentiel que le CA attendu — moyenne de VOS mêmes jours de semaine 90 j (lecture ajoutée AU lot, repli 28 j DIT si n_dow < 4 ; réel : −11 % en 28 j toutes-journées devenait −3 % vs vos 11 vendredis) ; question visiteurs renommée (« Combien de personnes sont venues ? » — tickets = acheteurs, visiteurs = venus) + conversion LIVE à la saisie + garde-fou visiteurs < tickets ; `attendance_approx` RELU (premier consommateur d'event_outcomes) → box « Transformation · déclaré » ; `daily_visitors` mesuré → question SUPPRIMÉE + box « · mesuré » ; happy-dom 11/11. **10/08 (4) — « Pour mémoire »** : l'Après ouvre sur la mémoire de l'événement (dispositif/consigne, absence DITE sinon, bilan relu EN MOTS — lecture event_outcomes élargie aux 6 champs) ; recette sur événement sans description → AUSSI enregistrée comme description (update) ; chip de type vide supprimée ; happy-dom 8/8 + zéro chip vide. **10/08 (5) — recette au CONTRAT** : 2 champs requis (geste concret + occasion — la structure convertit l'intention en recette rejouable), microcopie « rien ne démarre encore », puis le test se PROPOSE, jamais ne se lance seul. **10/08 (6-8) — l'OCCASION décide la fenêtre** : (a) occurrence à venir → `window_kind: day_of` + `window_start_date` + `saved_item_id` LIÉ (l'engagement n'est plus orphelin/invisible) ; (b) aucune occurrence → AUCUN test proposé (« attend son occasion » + ajouter une date) — l'armement sur signal n'est pas offert ici (dispatch gated `HEAT_DETECTABLE`) ; fenêtre/seuil ÉDITABLES via `commitments/edit` (+ `window_start`/`window_end`/`threshold_value`, gardes ≤ 90 j & 1-200 %, gel après résolution) ; un replay `cancelled` rend la pratique re-prouvable (`replay_status` exposé) ; vérifié réel 17/17. **10/08 (9) — audit dossier** : consigne/bilan montés DANS leurs panneaux (`[data-ev-mount]` — ils étaient `body.appendChild`, donc visibles quel que soit l'onglet) ; série sur le KPI DÉCLARÉ (`serie.kpi_key/kpi_unit/kpi_target/kpi_n_met/kpi_median/kpi_values/trend_readable`) ; `apres.reconciliation` (KPI vs journée), `apres.target_scale` (cible N× l'ordinaire), `apres.next_commitment` (état « en cours ») ; verdicts en français ; aperçu email replié ; 14/14 réel. **10/08 (10) — LINK OUT vers la boucle de décision EXISTANTE** (owner) : le dossier ne duplique pas le moteur de `engagement.astro` + `card-kit` (move recommandé, `move_stats`, diagnostic) — il renvoie vers `engagement?id=` ; rows et `next_commitment` portent `commitment_id` ; **bilan gated sur occurrence CLOSE** (jamais pendant qu'un test tourne) ; série au titre littéral « Vos N derniers <jour> testés » + décision au bout ; **« attendu » banni** partout au profit de « habituel » (dossier, provider, event-form) ; 10/10 réel. **10/08 (11) — partage depuis Évaluer** : geste « Prévenir l'équipe » ouvrant le workspace PARTAGÉ `MSDraftWorkspace` (draft-workspace.js?v=5, même module que pulse/insight) amorcé par le résultat mesuré + la Lecture + la date d'occurrence + `detail_url` ; `signal_type=event_result` (hors ROUTING_MAP → destinataire jamais imposé) ; contrat d'appel vérifié 10/10 | `raw.saved_items` × `raw.saved_item_dates`, `semantic.vw_insight_event_day_surface`, `mart.fct_client_day_residual`, `mart.fct_client_sales_signals_daily`, `raw.client_transactions`, `analytics.action_commitments`, `analytics.consigne_sends` |
| `insight/dashboard.ts` 🆕 | GET | **Tableau de bord (V7 14/08 : héros-grille insécable — tuiles Prochaine occasion + Veille-trouvailles · À faire en table à colonnes fixes · radar = UNE surface avec vignette-carte)** — atterrissage de l'onglet Piloter (`/tableau`). 6 blocs, un job chacun : Impact (écart mesuré € des fenêtres engagées jugées — mart outcomes, jamais extrapolé ; cibles atteintes/jugées ; prouvés) · Opérations en cours (occurrences ≤ 60 j + engagements ouverts, cible SUR la ligne, drapeaux concurrent/météo) · Équipe (opérations NOMMÉES par personne + tenue période) · Dispositifs prouvés · Opérations automatisées (reçu : contextes gelés 7 j, prochaine à s'armer J-7, verdicts programmés, alertes 7 j) · Débloquer (marge = correction declared_margin_pct active, bilans manquants nommés, faits actifs). GARDE-FOU owner : aucune métrique brute sans son verdict. `?period=30|90|365` = affichage seul. UN lot parallèle de 8 lectures — 1,9 s mesuré sur f10c3e58 | `raw.saved_items` × `saved_item_dates`, `analytics.action_commitments`, `mart.fct_client_commitment_outcomes`, `analytics.best_practices`, `raw.competitor_alerts`, `raw.event_outcomes`, `intermediate.int_consulter_corrections_current`, `raw.saved_item_snapshots`, `semantic.vw_insight_event_day_surface`, `analytics.day_class_impacts` (10/08) **05/08 : MULTI-SITES par défaut** (tous les sites du compte, pastille par ligne, ?location_id= filtre optionnel — le mono-site cachait 2 opérations sur 3) ; Impact à DEUX registres (« jugées » = tous les verdicts du journal ; « € » = mart seul, contrat « fait par défaut » : compté sauf déclaré pas-menée) ; € mesurés par personne (Équipe, regroupement par nom normalisé rôle/casse) ; next_judgment = prochaine fenêtre FUTURE ; 9 lectures un lot, 1,6 s réel multi-sites **05/08 : tiebreak canonique du dernier-état** (`updated_at DESC, terminal DESC, verdict non-null DESC, created_at DESC`) appliqué aux 10 sites app qui dédupliquent le journal (bestPractices ×3, actionCommitments, evenement, commitments/index, commitment-resolve, track-record, monitor, dashboard) + les 2 modèles dbt (int_client_commitment_latest, mart outcomes — owner) : à timestamp égal l'état TERMINAL gagne — 2 engagements réels sortaient « open » d'un côté et « resolved » de l'autre ; € confounded exclus de l'Impact, comptés à part **05/08 (2)** : Débloquer devient le bloc des GESTES (position 2 de la page) — verdicts tenus non documentés, déclarés sans rejeu, rejeux en cours (calculés verdicts × best_practices, requête pratiques sans filtre de période — la page filtre l'affichage) ; fetch page SANS location_id par défaut (le multi-site s'active enfin depuis la page) ; noms owner normalisés via transitions 'edited' (3 faits, 1 pré-contrat skippé par TERM_COLUMNS) **05/08 (3) — CTAs partout (owner : « data triggers actions »)** : Débloquer = lignes [geste · sous-texte · bouton] (Documenter→Agir, Faire le bilan→dossier ?mode=bilan par événement, Déclarer→Explorer ?q= pré-rempli — support ?q= ajouté à ie-prompt.js v32, jamais d'envoi auto, Suivre→Agir) ; Dossier→/Agir→ sur chaque ligne d'opération ; opérations de l'Équipe en liens ; **Rejouer** (V1.1) sur les dispositifs = POST /api/commitments (7 j, +10 %) puis PATCH /api/best-practices replay_commitment_id — deux endpoints existants, zéro nouveau ; bilans/practices renvoient leurs ids pour les liens **05/08 (4) — maquette v3 (owner)** : page réécrite sur le design system RÉEL (police héritée, eyebrows, cartes ombrées — plus aucun style inventé, leçon pulse-ui) ; UN bouton plein (Nouvel événement), le geste du moment = ligne d'action .aline (priorité bilan>documenter>prouver>marge — position, jamais un costume), pli par OBJECTIFS (« À débloquer : … »), Cette semaine ≤ 7 j en colonnes (signaux compressés en chip infobulée), volets fermés Dispositifs·Équipe·Automatisé (titre = réponse) ; fiches dispositifs « **Prouver · Automatiser** » (Prouver = chaîne rejeu existante ; Automatiser = formulaire événement pré-rempli titre+dispositif via ?titre=&dispositif=, event-form v6) ; Automatisé = reçu + « À activer » (2 interrupteurs réels : /notifications, Communiquer) ; bilans avec date de fin (« attend depuis N jours ») ; 15/15 assertions vm sur payload réel **05/08 (5) — gestes d'ONBOARDING (owner)** : état de config du compte dans le lot parallèle (team_members / channel_configs enabled / notification_preferences par user) → gestes « équipe · canaux · alertes » dans À faire maintenant, priorité MESURE D'ABORD (bilan>documenter>prouver>équipe>canaux>alertes>marge) — sur un compte neuf (tableau à zéro) la config remonte seule en tête ; chaque geste n'apparaît que si le manque est RÉEL en base (prouvé : compte owner = zéro geste, compte vide simulé = les 3). « Retour » des pages dossier/dispositif = history.back() si référent app, repli tableau **05/08 (6) — audit dummy-content (owner)** : « À activer » = interrupteurs réellement ÉTEINTS vérifiés en base (alerts_critical des préférences, signal_routing du roster) — compte owner : section absente (tout allumé) ; le « · 2 à activer » en dur supprimé, compte calculé **05/08 (7) — automatisation inc. 5** : 11e lecture DANS le lot (consignes d'opération ACTIVES : prochaine occurrence + dernière trace `consigne_sends`) → `automated.consignes` (next_send calculé = occurrence − offset) ; volet Automatisation liste chaque consigne (trace réelle « dernière envoyée le … à N » sinon prochain envoi, pastille site, lien Dossier — la définition et Désactiver restent AU dossier) ; harnais réel 3 sites 7/7, 1,8–2,5 s **05/08 (8) — automatisation cas 1** : 12e+13e lectures DANS le lot (dernier tir par pratique `dispositif_triggers` ; contexte signal chaleur par site — n jours ≥ 3 sur 30, prochain annoncé) → practices portent `armable` (vérité SERVEUR, miroir HEAT_DETECTABLE v1) + `arm_*` + `arm_signal_ctx` ; `automated.armed_dispositifs` (armés + dernier tir) ; tableau : lien « Armer sur signal » (origine détectable seulement) → panneau chiffres réels + garde-fous + PATCH arm ; chip « armé sur signal » + Désarmer ; ligne volet Automatisation « armé sur chaleur annoncée — N déclenchements / en veille » ; harnais réel 6/6, 1,7–1,8 s **05/08 (9) — onboarding P1 fraîcheur** : 14e lecture DANS le lot (MAX(transaction_date) par site) → `debloquer.sales_stale` (pire site figé ≥ 7 j — un max FUTUR type seed n'est pas figé) + `sales_missing` (sites sans aucune ligne) ; gestes tableau « Importer vos ventes récentes — elles s'arrêtent au JJ/MM · N jours » (juste après les bilans) et « site aveugle » ; prouvé Olivades (figé 27/07 · 9 j + Esprit de Fabrique aveugle) et owner (zéro faux positif, seed futur) **05/08 (10) — onboarding P2 premier verdict guidé** : `debloquer.first_test` (ventes présentes × AUCUN engagement non annulé JAMAIS — calculé sur le journal déjà lu, zéro lecture nouvelle) → geste « → Engagez votre premier test mesuré » (après les gestes de données) ouvrant le formulaire PARTAGÉ MSCommitForm en panneau sous l'étage 1 (origin `onboarding_first_test`, ajouté à commitmentOrigins ; tableau charge commit-form.js v=11) ; au succès le tableau se recharge et le geste disparaît ; prouvé : Olivades first_test=Les Olivades, owner null, < 3 s **10/08 — REFONTE GLANCE V4 (maquette Avant/Après `public/tableau-proto.html`, 5 tours de retours owner)** : (a) **fetch UNIQUE `period=365`** — le mart est lu à 365 j avec `resolved_date` et renvoyé brut (`impact_rows`) + `judged_meta` (verdict, created_d) : les périodes 30/90 se DÉRIVENT côté client (bascule instantanée, volets préservés, dérivation prouvée = serveur au harnais) ; le serveur filtre en JS pour la période demandée (rétro-compat) ; (b) **tier CANONIQUE des dispositifs** (JOIN rejeu → verdict 'met' au dernier état, registre de bestPractices.ts) — l'ancien `status='proven'` n'est écrit NULLE PART : « Dispositifs prouvés » était structurellement à 0 et un rejeu confounded restait « en rejeu » à vie ; + `practice_counts` par tier SANS LE LIMIT 20 (le compteur baissait au-delà de 20 pratiques) ; (c) **`occasions`** (jours chauds réels 30 j `ARRAY_AGG` day_surface × fenêtres engagées → joués/manqués par site, prochain jour chaud + fourchette €/j du store) et **`learnings`** (top 3 classes d'ENVIRONNEMENT du store enjeu `analytics.day_class_impacts`, porte n ≥ 5, |€/j| ≥ 50, familles internes card/sales/suivis exclues, `covered` si dispositif actif structural_<class>) — les deux cartes moat ; (d) `last_verdict` + `met_recipe` (récence + recette prouvée), `operations[].prev_occ` (score de série : verdict de l'occurrence précédente), `sales_depth`/`learned_classes` (provenance), `verdicts_scheduled` FUTURS seulement (une date passée s'annonçait « à venir ») ; (e) `personKey` = PRÉNOM (« Julen » et « Julen de Ajuriaguerra · CEO » = 2 lignes pour la même personne). 17 lectures, UN lot, **1,6 s réel** ; harnais endpoint 17/17 + rendu happy-dom sur payload réel 21/21 (scripts locaux non committés). **10/08 (2) — registre UNIFIÉ avec les chantiers structurels** : `learnings` passent par `rowsToImpactsWithImmaterial` (dayClassRegistry, EXPORTÉ pour ça — médiane, |t| ≥ 1, matérialité vs CA annualisé lu groupé sur `mart.fct_client_daily_performance`, 18e lecture) ; €/an + tier en infobulle ; CTA = M'engager origin `structural_<class_key>` (même machinerie que Pulse) ; état « en test » si engagement structurel ouvert ; ordre de page Impact → Cette semaine → Occasions → Appris ; 2,5 s réel. **10/08 (3)** : la prochaine occasion préfère le SITE au € chiffré (registre) — « à récupérer » porte toujours un nombre, sinon « impact pas encore chiffré sur ce site ». Côté `tableau.astro` : couche 1 = chiffres/points/chips (une seule phrase visible : l'action), prose au clic (Lecture, dernier verdict, comment-lire), rangées « Cette semaine » UNE ligne [date·●·titre·chips·cible·avatar·›] entièrement cliquables + verdict en retard visible (J+N), volets à titres en chips, cartes Occasions/Appris voix maison € d'abord, « aujourd'hui » en heure LOCALE, bouton Nouvel événement en contour. **Spec consolidée : `docs/tableau-de-bord-spec.md`** (jobs, garde-fous, moteur de gestes, contrats fait-par-défaut/tiebreak, labels tranchés) ; **16/08** : trous = vérité LIVE (JOIN directory vivant + exclusion suivi live même site/clé, location_id par trou) ; `glance.mesures` = OPÉRATIONS EN COURS 3 variantes (17/08 : kind occurrence/fenetre/serie, famille saved_items, KPI par jour des fenêtres multi-jours, frise série via `raw.saved_item_dates` + verdicts — 3 requêtes batch même Promise.all, futurs exclus) ; **17/08 (2) — correctif 8 points (Autour de vous)** : 3 lectures batch de plus (même lot) → `glance.audiences` (primary_audience_1/2 du profil par site — le référent « même public que vous »), `glance.gap_facts` (data_payload de la carte competitor_positioning_gap du JOUR, JSON parsé → top_item_description + revenue_share — la ligne « produit signature »), `glance.tests` (dernier état du rejeu par best_practice active : verdict, KPI, cible, dates — le FAIT du dernier test des dispositifs) ; **17/08 (3) multi-sites** : fiches portent `site` (siteLabel) — le volet groupe et COMPARE par site ; **18/08 — HÉROS v10 (bandeau 5 KPI, proto validé + titres arbitrés owner)** : Impact N jours (mesuré plage, inchangé) · CA 7 jours vs HABITUEL (2 lectures batch de plus même lot : daily_perf 100 j par site → médiane même jour de semaine côté serveur, payload `ca7` — jamais vs hier) · Signaux traités (% enjeu couvert/total des learnings, calcul client) · Opérations en cours (compte ; ≈ €/an sur du JUGÉ seulement via `ops_value` = gap moyen par saved_item des outcomes met/missed non-confondus) · Dispositifs prouvés ce mois (tests met du mois) ; les tuiles-portes À faire/Prochaine occasion/Veille quittent le héros — sv devient carte de grille + carte Veille (2e porte vers co), grille à 7 cartes ; **17/08 (4) fiche ENRICHIE (proto validé)** : la requête fiches JOIN `mart.fct_competitor_threat_profile` (overlap_pct, km mesurés) + lit `secondary_audience`, `competitive_analysis_json`, `commercial_news_json/at` — parsés SERVEUR en `fiches[].analyse {value_prop, prix[], gaps}` + `fiches[].actu {lead, mises[], autres_offres, sources, lu_le}` (JSON illisible → section absente, jamais un throw) ; le volet rend un panneau dépliable par fiche : Offre · Publics/Clients visés · Actualité commerciale (registre Web — non vérifié) ; **19/08 événements publics** : LECTURE des tables matérialisées `analytics.location_public_events(+_coverage)` (le calcul géodésique coûtait 10-17 s à la requête — mesuré, interdit budget 3 s) + catchment live sur la dim ; comptes VRAIS serveur (n14/n_zone, même CASE que dayClassRegistry), lignes = 100 plus proches/site → `glance.evtpub`  + `glance.watched_par_site` (23/08 : suivis réels par site, `raw.watched_competitors` × location_id — porte de l'amorçage veille)  + mémoire 30 j par fiche (23/08 : `nuits_30j` via `raw.competitor_offering_history` sans filtre prix, hausses/baisses/nouveautés via `mart.fct_competitor_offering_changes`, prochain événement via `mart.fct_competitor_events_conflicts`) ; suivis = `raw.competitor_tracking` (jamais watched_competitors)  ; **23/08 soir** : lit le store via `readDayClassStore` et le CA annualisé via `annualRevenueByLocation` (dayClassRegistry) — plus de copie locale |
| `insight/evenement.ts` 🆕 | GET | **Dossier d'événement-dispositif + LISTE** (spec `docs/evenement-dossier-spec.md`). Incrément 2b : `?create_context=1` sert le formulaire MSEventForm — types par MÉTIER (`lib/eventTypes` × `company_activity_type` du profil), attendu par jour de semaine (AVG expected_revenue 90 j — le référentiel de la cible à deux lignes, le même modèle que M'engager), familles produits (référentiel movers) ; 3 lectures en UN lot parallèle. **Incrément 3 (03/08)** : `?saved_item_id=` → provider `evenementFamily` (dossier 3 états) **05/08 (automatisation inc. 7)** : branche `?consignes=1` — consignes déjà écrites sur le lieu (« Reprendre la consigne d'un autre événement ») **18/08 : la liste porte kpi_target_pct/eur** (pilule binaire « Objectif : +X % » / « Objectif non fixé » — le type meurt) | `raw.insight_event_user_location_profile`, `mart.fct_client_day_residual`, `raw.client_transactions` **04/08 : branche `?list=1`** — les événements du lieu (titre, type FR, série/prochaine date via saved_item_dates, track record n_resolved/n_beat/sum_gap_eur) — PREMIER consommateur de `semantic.vw_insight_event_user_events` (annexe spec : la vue naît avec lui) ; prouvé sur f10c3e58 (5 événements, Corner en tête, 1,2 s) |
| `insight/monitor.ts` | GET | Monitoring dashboard: profile, signals, risk matrix, change feed, action candidates, BestTime traffic, `sales_summary` (last-8-day CA rows for the Pulse Performance volet — `mart.fct_client_sales_signals_daily`, fails soft to null on no-import accounts). **Phase 1: `day`(signals)+`profile` re-sourced from `dayContext.assembleDayContext` (`day_surface_raw`/`profile_raw`, slice `context`) — the brain is the single reader of day_surface + ai_location_context; monitor's own `profileQuery`/`signalsQuery` + `_profileCache` DELETED (parity-verified 82/40 cols). **Perf: only `selected_dates[0]` gets full `assembleDayContext` (~18 BQ reads); the remaining dates are batch-fetched via ONE `day_surface` query (~126→~27 reads for a 7-day pulse). The `_dc`-sourced per-day fields below are null/[] for batched days — no client renders them off the day object (competitor detail is re-fetched via `/competitive/competitor-signals` on pill-click). monitor.astro sends a single date → batch is empty → unchanged.** **Phase 1b: each day object also carries the brain's additive-rich context — `followed_competitors` (venues w/ google_rating + rating_trend), `weather_alert`/`weather_alert_fr` (acute, contextCopy), `commercial_events`, `foreign_visitors` (French), `attribution`/`attribution_fr` (delta_att ESTIMÉ chips via contextCopy) — for insight.astro to render (honest-absence per section)** | `dayContext.assembleDayContext` → `semantic.vw_insight_event_ai_location_context`, `…_day_surface` (no longer read directly); still direct: `…_change_feed`, `…_competitor_alerts`, `…_action_candidates`, `…_user_active_goal`, `…_user_activity`, `raw.saved_items`, `raw.watched_competitors`, `analytics.channel_configs` (`recommendations` config resolved ACCOUNT-level since 19/07 — site-first-else-account), `analytics.action_commitments` (Phase 1: suppress `action_candidates` whose `suppression_key` = an ACTIVE commitment's `origin_suppression_key`; 16/07 — the CHANGE FEED is suppressed by the same set; 17/07 — the key is DERIVED IN MONITOR'S QUERY (`CONCAT(change_subtype,':',location_id,':',affected_date)`) from stable view columns, NOT read from the view: the dbt-side column was wiped by a nightly main-branch rebuild (branch edit not merged) and 400'd the whole Pulse page. The dbt model edit is now OPTIONAL/droppable — the app is self-sufficient. Key flows through `entry.item` into Pulse's commit POST; seed-proven 370→369→370). **Triage par site (26/07 soir, docs/pulse-actions-triage.md)** : pulse rend le feed en BLOCS PAR SITE (contextuel puis chantiers compacts), tri horizon-puis-|enjeu|, plis, 3 filtres ; `enjeuForCandidate` hérite la classe du jour pour les anomalies ventes (« Motif de fond », `inherited:true`). **Chantiers structurels (26/07)** : la réponse expose aussi `day_class_impacts` (motifs du store passant les gates + copy `structuralCardCopyFr`, tri |€/an| desc) — rendus par pulse `renderStructuralSection` (section sous « À piloter », M'engager → origins `structural_<class_key>`). **Enjeu €/an (24/07, registre 24/07)** : chaque candidate porte `enjeu: DayClassImpact|null` — calcul + policy dans `lib/dayClassRegistry.ts` (voir sa ligne + `docs/enjeu-day-class-registry.md`), monitor = lecteur pur (`getDayClassImpacts` store-first dans le Promise.all, `enjeuForCandidate` au map). Couvre `weather_hazard_onset` (condition payload) + `weather_worsened`/`extended_bad_weather*` (condition de la date affectée) **Cartes événement (03/08)** : + `buildEventLifecycleCards` (lib dédiée, ADDITIF — voir sa ligne) concaténées aux candidates avant filtres (suppression/matérialité s'appliquent). **24/08 barreau 2 du coin** : chaque candidate porte `funnel_corner` (% mesuré de l'étape funnel de la carte sur SA classe — `kpiRegistry.CARD_FUNNEL_STEP` table owner + `dayClassRegistry.funnelCornerForCandidate` ; `readDayClassStore` lit TOUTES les métriques en une requête, `metric=null`) ; pulse le rend quand ni enjeu, ni € du jour, ni périmètre n'occupent le coin (relatif au coin, absolu en infobulle). |
| `insight/month.ts` | GET | 30-day window summary + AI window/special-days narration (packager @ Sonnet 5, structured outputs — schemas in `packagerSchemas.ts`), deterministic fallback. Météo bullets NAME the alert via `contextCopy.weatherNatureFr` (`lvl_*`, selected explicitly) — the operator never reads a bare "alerte 2" | `semantic.vw_insight_event_30d_window_surface`, `…_30d_surface`, `…_ai_location_context`, `…_ai_region_context` |
| `insight/prompt.ts` | POST | **Core AI prompt endpoint** (6200+ lines): **MODE ENQUÊTE « dispositif » (03/08, pièce 2b)** — `body.dispositif = {class_key, location_id}` → early-return complet (comme isUnknownIntent) AVANT tout routage : matière = `dispositifFamily` (facts, la liste blanche chiffrée), system = la discipline d'enquête (spec atelier § Hiérarchie — ce qui MARCHE d'abord, exceptions en notes de marge, humilité statistique, AUCUNE arithmétique), sortie JSON `{say_fr, fiche|null}` via `callClaudeMessagesAPI` + schéma structuré, porte `validateEnqueteOutput` (contracts/dispositifEnqueteChecks — lie-bait même commit) : nombre absent des FAITS et des mots de l'exploitant → relance avec feedback → repli déterministe. Producers `enquete_claude`/`enquete_fallback_deterministic`/`enquete_not_found`. Consommé UNIQUEMENT par `dispositif.astro` (chat-first). Ownership vérifié si location ≠ session. **Continuité (03/08)** : les dispositifs existants du motif entrent dans les FAITS (via provider) + règle 9 (jamais re-documenter ; poursuivre/ajuster/autre). **Accès Consulter aux dispositifs (03/08)** : (a) question qui NOMME dispositifs/bonnes pratiques → réponse DÉTERMINISTE `deterministic_dispositifs_v1` (liste exacte depuis `listClassDispositifs`, patron métriques déclarées — le routage jour/mois tombait sur le repli mois) ; (b) `buildPracticeFacts` (couche déclarative : fiche + test + liaison) entre dans la liste blanche des réponses jour, branches family-led ET non-family-led, en parallèle des autres builders. **Incrément 6 (04/08) — VOS ÉVÉNEMENTS** : (a) question POSSESSIVE (mes/mon/nos événements|lancements — jamais « les événements demain », qui reste la famille events) → réponse DÉTERMINISTE `deterministic_evenements_v1` (liste via `listUserEvenements` : type, prochaine occurrence, dernière mesure residual) ; (b) `buildEventFacts` (fiche + prochaine occurrence + dernière mesure) dans la liste blanche jour, les deux branches. month/days/selected-days narrative + entity web-search. `MS_AUTH_BYPASS=1` bypasses auth here only. **Phase 5 inc-① — SSE stream mode (opt-in `body.stream===true`; JSON default untouched)**: POST = wrapper → `handleCore(ctx)` (commit-A pure extraction — all ~10 early-return sites untouched); wrapper probes via `request.clone()`, runs handleCore inside `runWithStageEmitter`, emits `stage` events (route/context/sales in-core; generate/verify from runPackager via ALS) then ONE `result` event `{status, body}` — the ERROR CONTRACT: once the stream opens HTTP is 200 forever; the client reconstructs `resLike={ok,status}` and runs its verbatim branching. Proven live: stages arrive at distinct wall-clock seconds (generation = the long phase). **inc-② live-proven on a REAL regen**: verify start → reject (no done) → `regen` row (« Correction en cours ») → generation re-runs → verify done « — validée · 7 faits cités ». The wrapper composes the verify-done label from `stageVerifyDoneFr`; `emitStage("regen")` wraps the feedback-regen retry. **Envelope (additive)**: `ai.output.cited_fact_ids` now ships on grounded answers so the client extends the vetted pill (C1). **Étape 1 attribution (07/08, docs/explorer-attribution-spec.md)** : sur un pass grounded MODÈLE (jamais les planchers), l'enveloppe porte AUSSI `sentence_provenance` (déjà produit/validé, jusqu'ici jeté) + `facts_catalog` `[{id,label}]` (libellés owner résolus serveur via `lib/fr/factOrigins.fr.ts` ; seuls les faits à origin connue y entrent — id absent = pas de chip côté client, jamais un chip deviné) ; `FAMILY_FACT_ORIGIN` tague les facts providers (footfall/audience null exprès), `tagFactOrigin` les builders (identity/day-perf→ventes, practices→bonnes_pratiques, events→evenements_user). Zéro changement schéma/prompt/validateur — lie-bait 39/39 inchangée ; générateur de rapport (raccourci chat + family-report grounded_day + sales-report) intouché. **inc-③ telemetry**: ONE `[telemetry][grounded]` log line per grounded request — `rejected_first / recovered / floored / family_led / facts_cited` (flags + counts, never model text) — the data that gates any KV-cache revisit (owner decision). **Phase 2: the DAY_WHY (day-horizon) answer is GROUNDED on the brain** — `assembleDayContext` → `toGroundedDayPayload` → packager `grounded_day` (cites only citable_facts; validator rejects ungrounded; regenerate-once → deterministic `renderDayWhyV1` floor). The old `narrative_input_day` derivation is DELETED. Model IDs via `modelFor`; classifier routed through the one client. **Phase 2 — conversation-aware routing (complete)**: (#1) `meta.resolved_family` + `thread_context.last.family` — the conversation FRAME is `thread_context.last` (routing metadata only, never facts), client round-trips it; (#2) follow-up detector — a high-precision continuation (`^et\b`, ≤48 chars, no explicit date, no family keyword, not a V1-rule shape) inherits the frame's horizon/intent/family and applies the delta (weekday → next such day AFTER the frame's date, frame-relative not today-relative); bias to fresh; fresh routing byte-identical; (#3) `classifyIntentWithHaiku(qRaw, history, frame)` — the Haiku classifier reads the conversation + frame line (still emits only {intent,horizon,day}); (#4) the missing-dates dead-end is now a CLARIFYING QUESTION (`clarification:{kind,chips}`, deterministic template, no digits/entities in the question text; chips carry frame top_dates or the upcoming weekend and re-submit as a normal message via `submitQuestion`). Proven E2E on f10c3e58: "et le dimanche ?" after samedi 18/07 → day/DAY_WHY/19-07; footfall frame inherited → renderFootfall on the new date; chip round-trips to COMPARE_DATES. **Phase 1 #2 — feedback-driven regeneration**: on a grounded reject, attempt 2 carries the validator's errors as `validation_feedback` in the payload (feedback text is NOT in the grounding whitelist, so a rejected number can't legitimize itself); a second failure still floors. **Phase 1 #3 — honest-absence floor**: zero citable facts → `composeHonestAbsenceFr` (deterministic, zero LLM text, mode `deterministic_honest_absence_v1`). **FACTS FLOOR (07/08, retour owner « Jour défavorable (catégorie C) = useless »)** : quand des faits existent et que le grounded échoue deux fois → `deterministic_day_facts_floor_v1` — le plancher EST les faits (fact_fr VERBATIM, zéro texte inventé) : day-perf d'abord (CA réalisé, analogues), puis le contexte hors bruit de feed (`observed_change` écarté), headline = 1er fait, key_facts = 4 suivants ; l'IR `deterministic_daywhy_ir_v1` ne reste que sans aucun fait exploitable. **Packager calls now run on Sonnet 5 with structured outputs** (`output_config.format`, per-mode schema in `packagerSchemas.ts`): the API guarantees the JSON shape, the grounding validator stays the truth gate — the transport (`runtime/claude.ts` + `capsFor`) handles Sonnet 5's removed `temperature` and implicit adaptive thinking, so this endpoint needed no edit. Month/window still on `DecisionSignals` (deferred to the period-brain). **Family-LED answers: a dateless card-family question ("quand je vends le plus ?" → footfall) is promoted from the month default to the grounded day path (`resolveHorizonFromText` classifier promotion) and answered PRIMARILY from the family provider — its claim-typed facts become the citable whitelist, the day-brain's competing facts are dropped, so the answer LEADS with the asked dimension. Producer `family_grounded_claude`; a `family_deterministic` floor means the family answer is never lost to a grounded reject. Non-family day/month questions unchanged. **The response also carries `family_card:{render,data}` (the full provider card) with `primary:null` — `ie-prompt.js` renders it INLINE in the chat via `MSCardKit.<render>` (the complete hourly curve + weekly + decisions), so the detailed analysis IS the answer — no LLM summarization, no click-through. `prompt.astro` loads `card-kit.js` for this.** **Competitor discovery ("un nouveau concurrent près de moi ?"): the business brief fed to the discovery/entity prompts follows the identity hierarchy — **persistent user correction (Phase 2.3, `getActiveCorrections`/`correctionsBrief`, "CORRIGÉE PAR VOUS — fait autorité") > measured sales (`measuredBusinessBrief` from `buildIdentityFacts`) > crawled `auto_enriched_description` (`venueBusinessBrief`) > declared `company_activity_type`**. A persisted correction (e.g. the owner said "librairie") overrides even the measured café and is acknowledged out loud ("d'après ce que vous m'avez indiqué"). So discovery grounds on what the venue actually sells, not a stale crawl (f10c3e58 sells coffee though its crawl says "SaaS"). A `discovery` flag in the web-search contract surfaces the interpretation answer even when `found=false`. **Phase 2 increment 1: the web-search paths (discovery/entity/lookup) now carry `conversation_history` — `runWebSearch(system, userText, history)` → `callClaudeWithWebSearch({conversationHistory})` prepends prior turns, so a follow-up ("et le week-end suivant ?") or a correction ("je suis un café") is honored. Packager paths already had memory.** **Offering family (Phase 1 increment 2.5): WHAT-I-SELL questions ("quels produits je vends le plus ?", "mes meilleures ventes") route via the `offering` family provider — the dateless-family promotion now fires for ANY `familyForQuestion` match (the `===month` gate dropped), horizon=day short-circuits the lookup + Haiku classifiers. Family-led: day facts blanked, whitelist = the offering provider's concentration fact + the measured identity facts (`buildIdentityFacts`). Answer names products + basket + concentration (4/4 grounded on f10c3e58), no "Ouvrir le mois" CTA. `renderOffering` card built (deep-page mix card). **Increment 4 (elicit-don't-degrade): a SALES-DERIVED family question (offering / footfall / salesdiscount / salesdecomp) on an account with insufficient measured sales (provider `found:false` + identity `insufficient`) returns a deterministic elicitation ("Ajoutez vos ventes…", producer `deterministic_offering_elicit_v1`, no register pill) WITH the upload CTA (`upload_csv` → the chat's file picker) instead of a hollow answer — generalized in the Day-3 cold-start audit 16/07 (a fresh account's first question is often « je vends à quelle heure ? »). Web answers (entity + lookup fallback) normalize model-emitted `<br>` to newlines (the safe renderer showed them as literal text).** **Batch 2 (16/07) — elicit GENERALIZED: `detectMissingDimension(q)` runs BEFORE any routing — a question about a dimension the warehouse verifiably lacks (marge/coût/bénéfice, par-client, stock, personnel — INFORMATION_SCHEMA sweep across semantic+mart+intermediate+raw, zero matching columns) short-circuits to `MISSING_DIMENSION_FR` copy (contextCopy.ts, owner-editable) naming what's missing + how to add it, producer `deterministic_missing_dimension_elicit_v1` (null register). Figurative « marge de manœuvre/progression/d'erreur » excluded (œ ligature survives NFD — both spellings matched). Batch 1 (16/07) — router truth: `IMPACT_MARKERS` shield + y-a-t-il event-noun regex in `isEventLookupQuestion` (entity-impact/discovery questions no longer hijacked onto lookup; the empty-lookup web fallback still deliberately reports meta `ENTITY_IMPACT`/web); grounded prompt rule 5bis (absence-in-facts ≠ absence-in-DB).** **Item 2 (16/07) — PREMISE CHECK on ENTITY_IMPACT: `parseCaClaim(q)` (possessive revenue marker + direction verb, optional %) → the operator's OWN sales verdict (`mart.fct_client_day_residual` actual vs dow+trend normale; explicit single date → that day, else worst/best of the last 30 days) composed by `contextCopy.premiseCheckFr` and PREPENDED to the answer in BOTH scenarios (in-data packager + web); on the empty-handed decline the premise owns the headline (« Pas de chute de 47 % dans vos ventes »). Confirms/refutes the CA MOVE, never attributes cause. Telemetry `[telemetry][premise-check]`. 17/07: the premise ships as a NATIVE `segment` block (register vetted) so it never wears the entity answer's « Non vérifié » pill (R2).** **VOICE FLOOR (17/07): single-hit lookups answer in a SENTENCE (« Black Friday a lieu le vendredi 27/11/2026 — dans 4 mois. », weekday + relative time, deterministic); multi-hit keeps the list; « Ouvrir le mois » suppressed on PAST-day answers (planning affordance under a performance answer).** **Phase 4 (16/07) — DAY-PERFORMANCE depth: `buildDayPerformanceFacts` fetched in parallel with identity facts, folded into the grounded whitelist on non-family-led day answers; prompt rule 1bis (PERFORMANCE D'ABORD) → « Pourquoi le samedi 11/07 ? » now leads « CA de 1 454 € contre 1 110 € habituel, +31 % » + analogs + component movers, context as éclairage. `extractDateMentions` gains the bare FRENCH `DD/MM` form (cue-gated: le/du/au/ce or weekday before, so « les 3/4 » never parses; year = anchor's, LITERAL — never rolled forward: past-performance questions mean the past date; previously « le 11/07 » silently resolved to TODAY).** **R2-4 (07/08, bug owner) : « <jour> dernier/passé » et « <jour> de la semaine dernière » résolvent AU PRÉCÉDENT, strictement avant aujourd'hui (un samedi, « samedi dernier » = J−7 ; avant : aujourd'hui). + la RÉCLAMATION CA de la question (parseCaClaim) devient un fait citable DÉCLARÉ (« Vous évoquez une baisse de 40 % — votre estimation, pas une mesure », origin declarations) sur le chemin jour : le modèle peut la citer pour la RÉFUTER — l'écho du chiffre de l'utilisateur était un reject nombre-non-fondé mesuré (reject→reject→plancher). E2E : « Mon CA a chuté de 40 % samedi dernier ? » → 01/08, grounded pass, « −9 %, nettement plus faible que les 40 % évoqués ». **R6 (08/08, cas owner « tu ne réponds pas » → resucée) : détecteur d OBJECTION déterministe (motifs + historique ≥ 2 + date du frame) AVANT missing-dimension → tour de DÉSACCORD `deterministic_objection_v1` (zéro LLM) : reconnaissance + 3 hypothèses vérifiables + VÉRIFICATION DE COMPLÉTUDE live du jour disputé (transactions vs même jour de semaine 90 j, seuil 60 % — premier « va chercher ce qui manque » de l étape 4 ; client BQ local, motif des early-returns). E2E transcript owner : 1 s, « 327 tx vs ~245 — couverture normale ». R8 au RUBRIC_FR.** **R4-2 (juge 2,7→4,4) : la résolution « <jour> dernier » émet un fait « Date interprétée : samedi 01/08/2026 » + règle prompt 1quater (l'interprétation ouvre la réponse). R4-3 : règle anti-répétition (un chiffre du chapeau ne réapparaît pas en puce ; key_facts ≠ copie d'answer) ; la question passe au provider (`_fam.run(..., qRaw)`).** **Item 3 (16/07) — ANSWER CONTRACT (verdict → facts → next step) closed on the last two offenders: (a) the entity-decline's next step is now ACTIONABLE — the asked entity ships as ONE name-only `follow_candidates` entry (verbal clause stripped: « Le X a-t-il fait chuter… » → « Le X »; garbage-guarded — question residue never becomes a button; discovery declines excluded), so « Ajoutez-la à votre veille » is a Suivre button; (b) the honest-absence floor (`composeHonestAbsenceFr`) ends on a DATE-AWARE next step — past day → « importez-les », today/future → « interrogez un jour couvert » (an upload invite on a future day would be false advice). All other producers already satisfy the contract (grounded day, family cards, v3 datecards, discovery, web-entity, elicit, clarification).** **Item 4 (16/07, GENERALIZED same day) — DECLARED-DATA loop: the metric REGISTRY `lib/ai/declaredMetrics.ts` (one entry per declarable metric: correction_type, high-precision parser — frequency-forms/unqualified counts refused, bounds, label, value format) drives `parseAnyDeclaration` BEFORE the missing-dimension check — CLAUSE-AWARE since 17/07 (owner bug: « j'ai 300 clients réguliers : quel est mon CA par client ? » neither captured nor answered): a pure declaration confirms; a MIXED declare-and-ask turn strips the question clauses, captures silently (`with_question`) and FALLS THROUGH so the same turn answers the estimate with the fresh value handed forward (no read-after-write); a declaration inside the question itself (« est-ce que ma marge est de 62 % ? ») never captures; the value persists to `analytics.consulter_correction_events` as `declared_margin_pct` / `declared_client_count` (adding a metric = one registry entry + contextCopy answer + MEMORY_LABELS + VALID_TYPES; dbt `int_location_declared_metrics_current` passes any `declared_%` automatically) (same assert/supersede/clear lifecycle, clearable in the memory panel « Marge déclarée », NEVER folded into the identity brief — `correctionsBrief` filters `declared_*`); each metric's dimension branch answers with a computed ESTIMATE over 30-day measured CA (marge: CA × % via `declaredMarginAnswerFr`; par-client: CA ÷ effectif via `declaredClientCountAnswerFr`) — attributed « déclarée par X le JJ/MM/AAAA », labelled estimation, deterministic, no LLM, no validator surface. `getDeclaredMetric(location_id, type)` is the one read (`getDeclaredMarginPct` = back-compat wrapper). Producers `deterministic_declared_capture_v1` / `deterministic_declared_margin_v1` (null register, system-dialogue render class). Elicit copy invites the in-chat declaration. Telemetry `[telemetry][declared-capture]` / `[declared-answer]`.** | `dayContext.assembleDayContext` (DAY_WHY facts) + `semantic.vw_insight_event_selected_days_surface`, `…_day_surface`, `…_30d_surface`, `…_30d_window_surface`, `…_ai_location_context`, `…_competitor_signals`, `…_mobility_disruptions`, `…_eventcalendar_event_lookup`, `semantic.vw_ms_insight_ai_decision_policy_rules`, `mart.fct_client_day_residual` (premise check), `raw.saved_items`, `raw.saved_item_dates`, Anthropic API |
| `insight/reactions-today.ts` | GET | Type B + **four-tier context-decision service**: `tiers = { mesure, estimation, concurrence, action }` — measured / estimated / observed competition / measured action (reconduire-gated). **PURE SHAPING of the brain (`assembleDayContext`) payload — reads NOTHING directly** (Engine 1 & 2, estimation, competitors all from the brain); suppress-in-2 applied here. Legacy `factors[]` kept for engagement until it migrates | `src/lib/dayContext` (the brain — sole data source), `src/lib/sensitivityFeatures.json`, `src/lib/contextCopy`; dev `?src=seed` handled inside the brain |
| `insight/sales-report.ts` | POST | Sales report: daily series, comparison periods, category mix, context, action candidates, correlations. Named events + foreign visitors via shared `src/lib/dayContext` (no fork). **08/08 R1 (docs/rapport-canaux-spec.md)** : accepte `scope` ('group' = tous les sites du compte) ; réponse + `channels` (section « Vos canaux » via `lib/insightFamilies/channels.channelsData` — null si < 2 flux réels ; amorcée en parallèle, échec soft LOGGÉ jamais silencieux). **R2 (08/08)** : `channel` (channel_key) → RAPPORT SPÉCIFIQUE mono-canal — branche précoce, seuls les blocs du canal (les séries site entières mentiraient : `daily` absent), 4 questions omises, seuil ≥ 2 flux non appliqué (un canal demandé existe dès qu'il a des données) ; les tiers-canaux (COMPTOIRSEG, futurs corners : `party_directory.channel` non nul) exclus des listes de comptes partout | `mart.fct_client_daily_performance`, `…_client_sales_signals_daily`, `…_client_offering_daily`, `…_location_context_daily`, `…_location_daily_action_candidates`, `…_location_events_radius_daily`, `dims.dim_client_location`, `src/lib/dayContext` (named events/foreign) |
| `insight/sensitivities.ts` | GET | Returns venue sensitivity profiles (Type B) at tier level (reactions.astro harness) | `src/lib/dayContext` (the brain — `dc.sensitivities`); no direct store read |
| `insight/set-goal.ts` | POST | Creates/clears operational goal (acquisition/revenue/reputation/veille) | `analytics.goal_state` |

### `import/` — sales & location ingestion

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `import/locations.ts` | GET | Lists user-owned establishments for CSV import selection. **P3.1-c (19/08)** : chaque site porte `pos` (caisse déclarée du profil jointe à `analytics.pos_systems` — pos_key, label_fr, ingestion_mode, import_source = clé parseur sales-csv, export_note_fr ; null si non déclarée) — le flux d'import d'ie-prompt.js saute la question « De quel logiciel ? » et route dessus | `dims.dim_client_location`, `raw.insight_event_user_location_profile` × `analytics.pos_systems` |
| `import/sales.ts` | POST | Inserts JSON sales payload (transactions + line items) | `raw.client_transactions` |
| `import/sales-csv.ts` | POST | Parses CSV/Excel, maps columns, idempotent delete-supersede load by source+dates. **P3.1-d (19/08) : écrit `source_file` (nom du fichier déposé, tronqué 200) sur chaque ligne — traçabilité des imports (colonne ALTERée, additive, rien en aval ne la lit ; preuve scripts/sales-csv-sourcefile-verify.mts).** **29/07 : écrit aussi `invoice_number` / `item_code` / `item_description`** — trois colonnes de destination que la couche d'écriture laissait NULL depuis toujours, alors que le grain déclaré de `stg_client_transactions` est `location_id × transaction_date × invoice_number × item_code`. Le `load` est un WRITE_APPEND sans schéma explicite (BigQuery applique celui de la table), les trois colonnes sont STRING nullable — vérifié en direct | `raw.client_transactions` |

### `competitive/` — competitor & event tracking

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `competitive/add-competitor.ts` | POST | Insert/update competitor — dedup by `google_place_id` FIRST (name+city fuzzy only when no key; the name match birthed the 04/2026 duplicate fiches), crawl site, extract via Claude, audiences mapped from French prose via `audiencesFromProse` (constants.ts — the inline English whitelist left 27/30 active fiches without audience, so without measurable overlap), geocode via Google Places ; C4 (19/08) : body.target_location_id gardé isAdmin — l'identité stockée devient celle du PROPRIÉTAIRE de la location cible (clé « invite:<uuid> » incluse, basculée à la réclamation) | `raw.competitor_directory`, `…watched_competitors`, `…competitor_tracking`, `…competitor_events`, Google Places, Browserless, Anthropic |
| `competitive/add-event.ts` | POST | Insert watched event w/ dedup, confidence, boost logic | `raw.watched_events` |
| `competitive/check-competitor.ts` | POST | Lookup competitor by name+city | `semantic.vw_insight_event_competitor_lookup`, `raw.competitor_directory` |
| `competitive/check-event.ts` | POST | Lookup event by name+city+date w/ tolerance | `semantic.vw_insight_eventcalendar_event_lookup` |
| `competitive/competitor-crawl-history.ts` | GET | Crawl-run history (30) for a competitor | `raw.competitor_events` |
| `competitive/competitor-events.ts` | GET | User-confirmed competitor events (30d/20) | `raw.competitor_events` |
| `competitive/competitor-profile.ts` | GET | Full competitor profile + threat + cached Claude analysis ; **17/08 : + `actu` + `tarifs`** (tarifs RELEVÉS par la veille — dernier prix par article d'offering_history, 8 max, même lot ; actu commerciale parsée serveur — lue de `raw.competitor_directory.commercial_news_json/at` dans le MÊME lot parallèle, la vue dbt n'expose pas les colonnes de cache ; illisible = null, jamais un throw). Consommé par `competitor.astro` **refondu 17/08 dans la grammaire ENGAGEMENT** (proto validé : en-tête kicker/titre/liseré, verdict headline, sections Offre · Publics/Clients visés · Différenciation · Actualité commerciale, gestes Communiquer/Consulter — onglets Profil/Actualités morts) ; vérité rendu : `scripts/competitor-render-verify.mjs` (happy-dom, payload réel Guimet) | `semantic.vw_insight_event_competitor_lookup`, `mart.fct_competitor_threat_profile`, `semantic.…_ai_location_context`, `…_competitor_alerts`, `…_competitor_signals`, `raw.watched_competitors`, `…competitor_directory`, Anthropic |
| `competitive/competitor-signals.ts` | GET | **`?location_id` optionnel (radar v5, 17/07) — ownership vérifié via `requireLocationOwnership`, défaut = site de session ; la vue agrégée de Pulse fetch par site (signaux/suivis/menaces sont PAR SITE côté serveur).**  Signals for date (±7d), followed list (now incl. `lat`/`lon` for the Pulse Concurrence mini-map), top unfollowed threats | `semantic.vw_insight_event_competitor_signals`, `raw.watched_competitors`, `mart.fct_competitor_threat_profile`, `semantic.…_competitors_followed` |
| `competitive/search-competitor.ts` | POST | Claude web_search for competitor candidates | Anthropic (claude-haiku-4-5) + web_search |
| `competitive/discover-competitors.ts` 🆕 | POST | Découverte SANS nom (23/08) : décrit le site (raw profile + semantic vw_insight_event_ai_location_context) à l'agent web, exclut les suivis du site et le site lui-même, même forme de sortie que search-competitor → add-competitor ; consommé par la rangée « Cherchez vos concurrents » de Piloter (tableau.astro, n_watched < 3) | Anthropic + web_search ; `raw.insight_event_user_location_profile`, `raw.watched_competitors` |
| `competitive/search-db.ts` | POST | Local BQ search of event calendar + competitor directory, merged/deduped | `semantic.vw_insight_eventcalendar_event_lookup`, `…_competitor_lookup`, `mart.fct_competitor_directory`, `raw.watched_events`, `…watched_competitors`, `dims.dim_client_location` |
| `competitive/search-event.ts` | POST | Claude web_search for event candidates | Anthropic (claude-haiku-4-5) + web_search |
| `competitive/search-web.ts` | POST | Unified web search (events/competitors), caches to enrichment dim. **Zone = préférence de tri, JAMAIS un filtre (24/07)** : une marque/enseigne/URL est retournée même hors zone (siège + point de vente le plus proche + boutique en ligne, `location:null` pour un pur en-ligne) — le prompt zone-dure faisait rendre `[]` déterministe pour toute marque nationale cherchée depuis un petit bourg (Les Olivades / Saint-Étienne-du-Grès). **Retry-once-on-empty** : premier essai vide ou en échec → un second appel (variance web_search). Le client (`prompt.astro` runConcSearch) distingue désormais « Recherche indisponible » (erreur technique) d'« Aucun résultat » (vide authentique) | `semantic.vw_insight_event_ai_location_context`, `dims.dim_event_enrichment`, Anthropic + web_search |
| `competitive/suivis.ts` | GET | All watched events + competitors for user (all locations) | `raw.watched_events`, `…watched_competitors`, `semantic.…_competitor_lookup`, `raw.competitor_events`, `…insight_event_user_location_profile` |
| `competitive/unfollow-competitor.ts` | POST | Soft-delete competitor follow + clear tracking | `raw.watched_competitors`, `…competitor_tracking` |
| `competitive/unfollow-event.ts` | POST | Soft-delete watched event | `raw.watched_events` |
| `competitive/update-competitor-url.ts` | POST | Update source URL, crawl inline, discover agenda path | `raw.watched_competitors`, `…competitor_directory`, `…competitor_events`, Browserless |

### `channels/` — outbound publishing & OAuth

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `channels/automation.ts` | GET/POST/DELETE | CRUD automation rules (signal→channel routing) | `analytics.automation_rules` |
| `channels/config.ts` | GET/POST | Read/write channel configs (OAuth tokens). GET resolves ACCOUNT-level (owner 19/07: one Compte/Communication setup for all venues) — latest row per channel across ALL the user's rows, requested-location row wins ties (per-site page tokens preserved); POST still stamps the active location | `analytics.channel_configs` |
| `channels/gbp-callback.ts` | GET | Google Business Profile OAuth callback | `analytics.channel_configs`, Google Business Account/Info APIs |
| `channels/gbp-connect.ts` | GET | Start GBP OAuth | none |
| `channels/internal-alert.ts` | GET/POST/DELETE | CRUD internal alert rules (note_interne/slack/email), allowlisted action_types | `analytics.internal_alert_rules` |
| `channels/internal-send.ts` | POST | One-off internal alert dispatch. Channel config resolved ACCOUNT-level (19/07: site-first-else-account) | `analytics.saved_drafts`, `…channel_configs` (loadChannelConfig extrait vers lib/channels/internalSend 26/07) |
| `channels/meta-callback.ts` | GET | Meta OAuth callback (FB + IG dual configs) | `analytics.channel_configs`, Meta Graph |
| `channels/meta-connect.ts` | GET | Start Meta OAuth | none |
| `channels/publish.ts` | POST | **note_interne supported (18/07): internal write (same saved_drafts insert as the internal-send rail, `user_instruction='internal_send'` → surfaced by list-drafts on pulse/monitor) — no external side effect; internal-send rail itself stays Barrier-2 sealed.** Publish to channel (Slack/email/SMS/WhatsApp/GBP/FB/IG) w/ handler routing + logging. Channel config resolved ACCOUNT-level (19/07: site-first-else-account — aligned with config.ts GET so a venue the workspace offered a channel for can actually send) | `analytics.channel_configs`, `…action_log`, `…publish_log`, Slack/Resend/Google/Meta |
| `channels/slack-callback.ts` | GET | Slack OAuth callback | `analytics.channel_configs`, Slack OAuth |
| `channels/slack-connect.ts` | GET | Start Slack OAuth | none |
| `channels/team.ts` | GET/POST/DELETE | CRUD team members + routing config. GET resolves ACCOUNT-level (owner 19/07: one roster for all venues, no per-site team yet) — latest version per member_id across ALL the user's rows, requested-location row wins ties | `analytics.team_members` |

### `commitments/` — "M'engager" engagement tracking

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `commitments/disposition.ts` | POST | Update disposition (fait/pas_encore) + note; also accepts `execution_quality` (complete/partial/none — the diagnosis self-check that routes the recommended move) | `analytics.action_commitments` |
| `commitments/edit.ts` | POST | Edit committed action text + owner | `analytics.action_commitments` |
| `commitments/evolution.ts` | GET | Day-grain revenue/context/residual series for commitment window; `commitment` payload also returns structured retro (`retro_worked`/`change`/`repeat`) + `created_at`/`action_done_at`/`threshold_value` (owner+date header, read/edit pre-fill) + `execution_quality`; also returns `move_stats` (per-move hit-rates for the diagnosis advice) + `best_in_class` (vetted "lieux comparables" analogs for the diagnosis panel — resolves venue vertical → lever → plays via `bestInClassStore`) + `site_name` (19/07, same ai_location_context query — the évolution page names its venue) | `mart.fct_client_day_residual`, `…location_context_features_daily`, `analytics.action_commitments` (latest snapshot), `mart.fct_location_action_moves` (move hit-rates), `semantic.vw_insight_event_ai_location_context` (industry), `analytics.best_in_class_plays` (via `bestInClassStore`) ; **bloc `kpi` (15/08, KPI-vrai)** : `mart.fct_client_daily_performance` (K2-K6, colonne du registre), `raw.client_transactions` + `raw.saved_items.kpi_family` (K8 famille) — série jour + pairs même-jour-de-semaine + baseline/objectif dans `measured_metric`, fenêtre STOCKÉE (jamais le jour de création), jours futurs exclus ; expose `verdict_basis`/`kpi_noise_se` (référentiel du verdict, infobulle card-kit) ; **réorg 17/08** : `glance.fiches` (fiche par suivi : note/avis/audience/fourchette tarifs via directory+offering_history) + `evts14.score` (conflit du mart) |
| `commitments/index.ts` | GET/POST/DELETE | List / create / soft-cancel commitments. **Objectif libre (18/07): POST accepts `threshold_basis:'pct'` + `threshold_pct` (1–100 → threshold_value; legacy modeste/net kept); `window_kind` gains `30d`; GET `?goal_context=1&window_kind=` returns the form's honest €/% translation (window baseline, detectability floor, real-noise presets) from `mart.fct_client_day_residual` via recoverable sigma + floored ρ + `vif` imported from commitmentResolve — never the global 0.19 constant.** **Origins now include `chat_decision_<family>` (10 chat-décision types, themes via `recoThemeMap.CHAT_DECISION_THEME` so origin_factor lands non-NULL).** Sets `origin_factor` (Engine-1 A↔B bridge): explicit `body.origin_factor` else derived from the action_type theme via `themeForActionType` (non-NULL). Adjustment "how" loop: POST accepts `adjustment_move`/`adjustment_note`/`parent_commitment_id` (re-commit a child on a fresh window); DELETE takes the move from the body (`stop` = Arrêter → system card reappears via suppression). **Étape 3 (26/07)** : `measured_metric` = `kpiKeyForOrigin(type, driver)` (lib kpiRegistry) + `kpi_baseline` (30 j pré-fenêtre, non bloquant, null honnête) à la création. **Notification d'assignation (26/07 soir, owner)** : après création, le responsable du roster (résolution par nom, site d'abord) est prévenu sur son canal de contact — email d'abord sinon Slack — via `lib/channels/internalSend` (`loadChannelConfig` partagé) ; NON bloquant (échec → `assignment_notified:false`, l'engagement existe) ; réponse + `assignment_notified`/`assignment_channel` (pulse affiche « notification envoyée ») ; l'auto-assignation notifie aussi (assumé). **Événements (03/08, spec evenement-dossier § 1.3)** : POST accepte `saved_item_id` (ancrage — colonne ALTERée le 03/08) + `window_start_date` (Y-m-d : fenêtre ancrée sur l'occurrence au lieu d'aujourd'hui ; absent → comportement historique inchangé) ; origins `event_<type>` éligibles (préfixe dans commitmentOrigins, registre lib/eventTypes = source de vérité) ; **incrément 4** : `event_kpi` (+`kpi_family`) → `measured_metric` via `kpiKeyForEventKpi` (foyer unique kpiRegistry), baseline famille 30 j pré-fenêtre (`measureFamilyBaseline`) | `analytics.action_commitments`, `analytics.team_members`, `analytics.channel_configs`, `src/lib/recoThemeMap`, `src/lib/kpiRegistry`, `src/lib/channels/internalSend` **04/08 : `window_days` (1–31) optionnel** — surcharge la durée nominale du kind pour les événements multi-jours (fenêtre [lancement, lancement+durée−1]) ; absent → inchangé ; la résolution lit les colonnes window_*, jamais le kind |
| `best-practices/index.ts` 🆕 | GET/POST/PATCH | « Vos bonnes pratiques » (validé 26/07, proto methode-proto.html) : POST crée une pratique DÉCLARÉE depuis la branche positive de M'engager (kpi + outcome_lever calculés serveur via kpiRegistry/bestInClassStore — le client n'invente rien) ; GET renvoie les pratiques appariées à une origine (même kpi + même levier dérivé OU même classe de jour), prouvées d'abord ; PATCH lie l'engagement de réplication créé par la chaîne « rejouer » **et/ou pose l'ARMEMENT sur signal (05/08, cas 1)** : body `arm {enabled (bool requis), recipient_name?, recipient_contact?, channel? ('email' v1), cooldown_days? (1..30)}` → updateArming ; harnais réel coupon 5/5 (armer/désarmer, COALESCE, 400 non-booléen). Auth = motif commitments (Clerk + requireLocationOwnership) | `analytics.best_practices`, `src/lib/bestPractices`, `src/lib/kpiRegistry`, `src/lib/bestInClassStore` |
| `commitments/retro.ts` | POST | "Documenter" (Spec 2): STRUCTURED retro `retro_worked`/`retro_change`/`retro_repeat` (+ legacy `retro_note`), partial-safe merge | `analytics.action_commitments` |

### `analytics/` — logging, drafts, admin metrics

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `analytics/admin-dashboard.ts` | GET | Admin dashboard of all sites + stats | `mart.fct_admin_dashboard` |
| `admin/invite.ts` 🆕 | GET/POST | **P3.1-a onboarding sur invitation (18/08)** — invitations Clerk réelles : POST {email, activity_hint?, pos_hint?} crée (métadonnées → pré-remplissage profil P3.1-b + routage caisse P3.1-c ; redirect sign-up, Clerk envoie l'email), POST {revoke_id} révoque, GET liste les pending ; garde isAdmin (motif admin-dashboard) ; client @clerk/backend EXPLICITE (clé secrète — indépendant du runtime Astro, testable en direct) ; UI = bloc « Inviter un compte » sur admin.astro ; harnais scripts/invite-verify.mts (--send = création+révocation réelles) ; GESTE OWNER restant : instance Clerk en Restricted. **C2 (19/08)** : champs structurés facultatifs (site_name, company_address, activity, website_url, audience_1/2) validés contre INDUSTRY_LABEL + PROFILE_AUDIENCE_OPTIONS (enum inconnu = 400) → publicMetadata ; UI admin = 2 rangées supplémentaires ; consommés par C3 (pré-provisionnement). **C3 (19/08)** : avec une adresse, le profil est PRÉ-PROVISIONNÉ à l'invitation — clé en attente « invite:<uuid> » (publicMetadata.provision_key), invocation directe du VRAI profile/save.ts en mode create avec identité injectée (couture provision_identity) → géocodage + dim_client_location + chaîne dbt à J0 ; couverture événements mesurée (<15 km/30 j, int_events_event_daily_enriched) et montrée à l'admin (0 = base pauvre → pré-choisir des concurrents, C4) ; réclamation au premier login via lib onboardingClaim (profile.astro) ; non bloquant. **C4 (19/08)** : bloc admin « Suivre des concurrents pour ce compte » (visible après pré-provisionnement) → add-competitor avec target_location_id. **P3.1-c (19/08)** : la demande de fichier de ventes part À L'INVITATION (goulot mesuré humain, J+9 Olivades) — email Resend (rail internalSend, reply_to = l'inviteur) avec la consigne d'export de la caisse pressentie (pos_hint apparié sans casse à `analytics.pos_systems`, repli « autre ») ; non bloquant, `file_request_email` dans la réponse ; prouvé réel (--send : sent) | Clerk API, Resend (internalSend), `analytics.pos_systems` |
| `analytics/admin-errors.ts` | GET | Error + crawl-failure logs for user/location | `analytics.api_error_log`, `…crawl_log` |
| `analytics/admin-feedback.ts` | GET | User feedback on signal confirmations | `analytics.signal_confirmations` |
| `analytics/admin-page.ts` | GET | Admin HTML page (Clerk-gated) | none |
| `analytics/card-states.ts` | GET | Per-card outcome states (30d) | `analytics.action_log` |
| `analytics/confirm.ts` | POST | Record signal confirm/dismiss | `analytics.signal_confirmations`, `…action_log` |
| `analytics/party-role.ts` 🆕 | POST | Qualification du RÔLE d'un compte client depuis la carte client_dormant (geste « Préciser ce compte », spec client-patterns § R.3) — vocabulaire § R validé (5 valeurs + channel_kind obligatoire pour un canal), résolution (site routé → source_location_id) via le mart clients, écriture par MERGE DML (jamais de streaming — table load-job), provenance `user_card:<user>`. Prouvé par harnais tsx 07/08 : 400×3/404/200 + ligne annuaire vérifiée puis provenance restaurée | `mart.fct_location_client_patterns` (lecture), `analytics.party_directory` (MERGE), `…action_log` |
| `analytics/list-drafts.ts` | GET | List active saved drafts | `analytics.saved_drafts` |
| `analytics/pending-feedback.ts` | GET | Recent draft events (7d) | `analytics.action_log` |
| `analytics/save-draft.ts` | POST | Create/save a draft | `analytics.saved_drafts` |
| `analytics/signal-accuracy.ts` | GET | Signal accuracy metrics by type | `semantic.vw_insight_event_signal_accuracy` |
| `analytics/track.ts` | POST | Log user events (views/drafts/confirmations/outcomes ; allowlist VALID_EVENTS — tout nouvel événement client DOIT y être ajouté, sinon rejet 400 silencieux côté appelant). 26/07 : + `bp_context_exit`/`bp_unexplained_exit` (sorties honnêtes bonnes pratiques) | `analytics.action_log` |

### `saved-items/` — saved events lifecycle

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `saved-items/alerts.ts` | POST | Max alert levels + top categories for saved items on dates | `semantic.vw_insight_event_change_feed` |
| `saved-items/bilan.ts` | POST | Record event outcome survey. **10/08 (F4 owner)** : + `action_carried` ('oui'/'partiel'/'non', nullable — « le dispositif prévu a-t-il été appliqué ? », la clé d'attribution que le bilan ne demandait jamais) ; colonne AJOUTÉE à `raw.event_outcomes` (ALTER 10/08, additive). NB : `stg_event_outcomes` (dbt) ne la sélectionne pas encore et reste un modèle FEUILLE — aucun consommateur aval ; à brancher côté dbt Cloud IDE avant de promettre « calibre vos prévisions » | `raw.event_outcomes` |
| `saved-items/create.ts` | POST | Create saved item + dates. **03/08 (spec evenement-dossier § 1.1)** : accepte les champs événement-dispositif (author_person_name, event_nature, hour_start/end, kpi/kpi_family/kpi_target_pct/eur, recurrence/dow/start/end — tous nuls par défaut, client existant inchangé) ; récurrent → occurrences GÉNÉRÉES (`lib/eventOccurrences`, plafond 52, « Du X au Y ») écrites dans saved_item_dates à la place des candidats, réponse expose `occurrences` | `raw.saved_items`, `…saved_item_dates` **04/08 : `duration_days` (1–31, NULL=1 j)** — durée d'un événement multi-jours (ALTER additif exécuté) ; passthrough get.ts + provider evenementFamily ; event_end_date + fenêtre de mesure calés au Choisir du dossier |
| `saved-items/delete.ts` | POST | Soft-delete saved item + dates | `raw.saved_items`, `…saved_item_dates` |
| `saved-items/get.ts` | POST | Fetch one saved item. **03/08** : expose les 12 colonnes événement-dispositif (SELECT + GROUP BY explicites) + dates + profile | `raw.saved_items`, `…saved_item_dates`, `…insight_event_user_location_profile` |
| `saved-items/list.ts` | POST | List saved items (filter by stage, paginated) | `raw.saved_items`, `…saved_item_dates`, `…insight_event_user_location_profile` |
| `saved-items/snapshot.ts` | GET/POST | Capture/retrieve opportunity + weather snapshot at date. **04/08 : 3 dérives silencieuses de la vue corrigées** (le POST échouait en silence — fire-and-forget depuis update.ts — attrapé par le cron event-occurrences) : `delta_att_mobility_car_pct` renommée `delta_ops_mobility_car_pct`, `opportunity_score_final_local` re-typée STRING → `SAFE_CAST FLOAT64`, `mobility_status_region` disparue → `CAST(NULL AS STRING)` honnête | `semantic.vw_insight_event_day_surface`, `raw.saved_item_snapshots`, `…event_outcomes` |
| `saved-items/update.ts` | POST | Update saved item fields + optional snapshot. **03/08** : + les champs événement-dispositif (8 éditables ; la RÈGLE de récurrence n'est PAS éditable en v1 — la changer regénérerait les occurrences, chantier séparé) **05/08 (automatisation inc. 2)** : + les 5 champs consigne d'opération (`consigne_arrival`/`_store_info`/`_interactions` STRING, `consigne_send_offset` 1..7, `consigne_enabled` BOOL explicite — absent ≠ false) ; harnais réel 14/14 (create jetable → update → delete hard) **05/08 (inc. 6)** : + `consigne_deroule` ; sémantique d'EFFACEMENT des 4 champs texte consigne (fourni vide "" = SET NULL, absent = intact — le formulaire envoie toujours ses 4 champs) ; harnais 7/7 **05/08 (inc. 7 — bug owner « Transaction is aborted »)** : la TRANSACTION n'enveloppe plus que le chemin AVEC dates (2 tables) — un UPDATE seul part nu ; deux écritures rapprochées ne s'avortent plus (harnais : 2 updates concurrents OK, chemin dates intact) | `raw.saved_items`, `…saved_item_dates` |
| `saved-items/participants.ts` 🆕 | GET/POST/DELETE | **Participants d'une occurrence (destinataires de la consigne d'opération, automatisation inc. 2)** — contacts EXTERNES possibles (jamais d'accès app, RGPD : servent uniquement à l'envoi). Journal append-only streaming latest-wins par `participant_id` (patron automation_rules), DELETE = tombstone `deleted=true` ; POST exige la DATE (un participant appartient à UNE occurrence) ; propriété = même contrat que update (clerk_user_id × location du contexte). Harnais réel 05/08 : CRUD 14/14, 404 inter-site prouvé **inc. 6** : participants de TOUTE la série (`serie: true` → date NULL, reçoivent chaque occurrence) ; GET ?date= = union occurrence + série (flag `serie`, série d'abord) — même union que le cron à l'envoi | `raw.saved_item_participants`, `raw.saved_items` (ownership) |

### `notifications/` — user preferences

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `notifications/get.ts` | GET | Fetch notification preferences | `raw.notification_preferences` |
| `notifications/save.ts` | POST | Save/merge notification preferences | `raw.notification_preferences` |

### `cron/` — scheduled jobs (GET-triggered)

| Route | Purpose | Data source |
|---|---|---|
| `cron/alerts.ts` | Send critical alerts via Resend email. **The LIVE loop reads `raw.competitor_alerts`** (the `raw.alerts` loop is DEAD — 0 rows ever; kept as-is). Staleness guard on BOTH queries (`affected_date >= CURRENT_DATE()` — proven 1→0 on a past-dated seed, prerequisite for the owner's scheduler repoint `insight-alterts` → `alerts`). **Subject names WHY the alert fired** (`frAlertSubtype(change_subtype)` from `contextCopy.ts`: proximity → "Concurrent à proximité", audience_overlap → "Concurrent sur votre public"; unknown subtype → "Concurrent détecté"). Narrative prose rendered via `safeMarkdown.mdInlineToSafeHtml` | `raw.competitor_alerts` (live), `raw.alerts` (dead), `…saved_items`, `…insight_event_user_location_profile`, `…notification_preferences` |
| `cron/bilan.ts` | Post-event retro email 7d after event end. **04/08 : « terminé » = `COALESCE(event_end_date, MAX(saved_item_dates.date))`** — les récurrents du modèle événement-dispositif n'ont pas d'event_end_date et n'auraient jamais eu de bilan ; CTA → le DOSSIER (`/evenement?…&mode=bilan`, bloc 3 questions) — monitor.astro démis. NOTE : pas dans le scheduler cron-job.org (constat 04/08) | `raw.saved_items`, `…saved_item_dates`, `…insight_event_user_location_profile`, `…event_outcomes` |
| `cron/commitment-resolve.ts` | Resolve commitments whose window closed. **Étape 3 (26/07)** : en plus du verdict K1 (inchangé), `commitmentResolve` mesure le KPI du commitment (`measured_metric` non-K1) sur la fenêtre → `kpi_window_value` + `kpi_delta_pct` (additif, échec soft) | `analytics.action_commitments`, `src/lib/kpiRegistry` |
| `cron/competitor-alerts.ts` | Alerts for competitor events (conflict ≥2) overlapping saved dates | `mart.fct_competitor_events_conflicts`, `…fct_competitor_directory`, `raw.watched_events`, `…insight_event_user_location_profile` |
| `cron/day-class-impacts.ts` 🆕 | Nightly rebuild du store day-class (Enjeu €/an) : UN `CREATE OR REPLACE TABLE analytics.day_class_impacts AS <dayClassAggregateSql(false)>` — agrégats bruts toutes locations × classes (météo+concurrence+tourisme), AUCUNE policy ici (elle vit dans le lib, à la lecture). **01/08** : le store porte aussi `med_gap_eur`/`n_log`/`avg_log`/`sd_log` (régime log+médiane) ; ALTER idempotent sur l'historique (CREATE IF NOT EXISTS ne modifie jamais un schéma existant). **01/08 (temps 2 périmètre)** : construit AUSSI `analytics.catchment_hypothesis_days` (`catchmentHypothesisSql` — jours mesurables par hypothèse 1 km/20 km, table SÉPARÉE du store des pilules à dessein). Bearer CRON_SECRET soft (miroir commitment-resolve). Idempotent, déterministe, pas d'IA. **Archive chaque batch dans `analytics.day_class_impacts_history`** (append par batch_date — le préalable de l'état « Résolu » : sans archive, un motif ne peut jamais être constaté disparu). **Ping quotidien à enregistrer par l'owner (cron.org)** **17/08 : réponse immédiate + waitUntil** (pinger externe gratuit plafonné à 30 s raccrochait en « failed » alors que le batch finissait — santé réelle = computed_at du store)  **18/08 : runDayClassBatch + salesFresherThanDayClass EXPORTÉS** (rattrapage post-import via competitor-surveillance) **22/08 : colonne `metric` (six KPI)** — le store passe de 81 à 231 lignes ; l'historique reçoit `metric` par ALTER idempotent (lignes d'avant : NULL = `revenue_residual`, ce qu'elles étaient). CREATE/ALTER/INSERT de `day_class_impacts_history` bougent ENSEMBLE (schéma explicite : un INSERT non aligné échoue en silence dans le waitUntil) | `mart.fct_location_context_daily` × `fct_client_day_residual` × `fct_location_context_features_daily` → `analytics.day_class_impacts` + `analytics.day_class_impacts_history` |
| `cron/crawl-best-in-class.ts` 🆕 | **Demand-drain** for the "lieux comparables" store (NOT a calendar refresh): per run crawls ≤`n` (default 1, cap 3) stale/missing cells, only for verticals with ACTIVE commitments, only cells missing or older than `ttl`/`BIC_TTL_DAYS` (90). **Selects cells + returns 200 `{status:started}` in ~1.5s, then crawls+loads in the BACKGROUND via `waitUntil`** (web_search is ~20-40s/cell — a sync response times out external pingers like cron.org free; mirrors competitor-surveillance.ts). Supersedes only cells that produced fresh plays (no wipe on transient empty). Shared crawl contract = `bestInClassCrawlCore.mjs` | Anthropic `web_search_20250305`; `analytics.action_commitments` + `semantic.vw_insight_event_ai_location_context` (active verticals) → `analytics.best_in_class_plays` |
| `cron/competitor-surveillance.ts` | Crawl competitor URLs (Browserless) → extract via Claude → write events. **Round-robin key = `competitor_directory.last_crawl_attempt_at`, stamped for EVERY competitor the loop touches (success/empty/error) BEFORE crawling** — never the extracted-event yield: ordering on `MAX(crawled_at)` from `competitor_events` starved 14/23 competitors at the queue head for ever (no events ⇒ never marked crawled) and the 9 that DO yield events were never re-crawled. Sequential loop, `break` at 55s (Vercel 60s), 12h re-crawl guard ⇒ ~1–2 competitors/run  **Fix 26/07 (prouvé sur Les Olivades)** : (1) vides honnêtes ≠ échecs — réponse modèle vide ou `[]` parsé (catalogue sans agenda) → `extraction_status='partial'` (`empty_model_response_no_events`/`no_events_on_page`), le catch PRÉSERVE partial (avant : tout non-fetch_error écrasé en failed — la sémantique du gate n'atteignait jamais la table) ; (2) re-enrichissement PÉRIODIQUE TTL 7 j (`enriched_at` horodaté dans auto_enriched_description) — avant, une seule passe à vie → snapshots d'offres figés → deltas d'offres structurellement impossibles (0 pour tous).  **18/08 : + rattrapage day-class POST-IMPORT en waitUntil** (porte salesFresherThanDayClass — ingestion mart > dernier batch → rebuild sous 15 min, les motifs structurels d'un compte neuf arrivent le jour même ; file onboarding ③)  **19/08 (P3.1-f) : + suivis proposés par menace en waitUntil** — `lib/proposedFollows.runProposedFollows` : compte < 14 j × menaces en mart × 0 suivi × sans marqueur → top 5 threat_score écrits dans watched_competitors + competitor_tracking (`proposed=TRUE` → chip « suivi proposé — ajustez » sur la fiche tableau), marqueur action_log `suivis_proposed` (posé même à 0 menace — absence dite) ; preuve scripts/proposed-follows-verify.mts 6/6 | `raw.competitor_directory`, `…competitor_tracking`, `…competitor_events`, `raw.watched_competitors`, `mart.fct_competitor_threat_profile`, `analytics.action_log`, Browserless, Anthropic |
| `cron/daily-briefing.ts` | **Point du jour (Phase 3: GROUNDED on the brain)** — per location, `dayContext.assembleDayContext('brief')` is the ONE source of day context; the narrative is grounded (`toGroundedDayPayload` → packager `grounded_day` @ `modelFor('briefing')` Haiku, validator rejects ungrounded). **Deliberately schema-OFF**: Haiku's `structuredOutputs` send-policy is false in `models.ts` — with the grounded-day schema it derives arithmetic (1500−1240=260, ungrounded) and the validator rejects 4/4; the briefing stays on the legacy parser path. Material-signal gate (no email on quiet days); yesterday score = one read (delta as citable fact). Ad-hoc score/weather/competitor queries + the raw Claude fetch + hardcoded model DELETED; crawl-health/stale dropped from the brief **17/08 : réponse immédiate + waitUntil** (run ~90 s > 30 s du pinger — santé réelle = last_daily_email_sent_at) | `dayContext.assembleDayContext` → (day context, single reader); still direct: `raw.insight_event_user_location_profile`, `dims.dim_client_location`, `semantic.…_ai_location_context` (users), `…_day_surface` (yesterday score), `raw.saved_items`, `raw.notification_preferences` |
| `cron/daily-dispatch.ts` | Dispatch automation rules from today's change feed. **05/08 (automatisation cas 1)** : + passe `runArmedDispositifs` AVANT les règles (indépendante, échec soft) — dispositifs `arm_enabled` × détecteur v1 « chaleur annoncée DEMAIN lvl_heat ≥ 3 » (origines `HEAT_DETECTABLE`, v1 structural_traffic_high ; autres → détail honnête) × idempotence (practice_id, target_date) × cooldown MAX(sent_at) → email consigne auto-suffisant (internalSend, note délai de prévenance) + engagement `origin_kind='signal_armed'`, clé `armed:<practice_id>:<date>`, réglages du REJEU (dernier état tiebreak), kpi_baseline mesurée + trace DML `dispositif_triggers` ; réponse `armed{scanned,triggered,details}` sur les deux chemins. **Fix latent** : action_log n'a JAMAIS eu metadata/signal_type (schéma réel vérifié — la requête frequency 500-ait tout le cron, l'insert échouait muet) → `reason` porte le rule_id, requête + insert alignés. E2E réel 05/08 : coupon armé → 1 email + engagement conversion +20 % fenêtre 06/08 (baseline 0,43 mesurée) + trace ; rejeu → 0 ; nettoyage complet (test). PAS ENCORE dans cron-job.org. **05/08 (onboarding P1)** : + passe `runFreshnessReminders` (indépendante, échec soft, `?dry=1` = liste sans envoyer) — comptes aux ventes FIGÉES ≥ 7 j (MAX réel < aujourd'hui), garde 1 email / 7 j / site via action_log `freshness_reminder`, email au compte (APP_BASE_URL/prompt) ; dry réel : 1 seul tenant éligible (Olivades, figé 27/07, 9 j), 0 écrit | `analytics.automation_rules`, `semantic.vw_insight_event_change_feed`, `analytics.best_practices`, `analytics.dispositif_triggers`, `analytics.action_commitments`, `semantic.vw_insight_event_day_surface`, `analytics.action_log`, `src/lib/channels/internalSend`, `src/lib/actionCommitments`, `src/lib/kpiRegistry` |
| `cron/daily.ts` | Daily emails 0–7d before saved event dates | `raw.saved_items`, `…insight_event_user_location_profile`, `…notification_preferences` |
| `cron/event-occurrences.ts` 🆕 | **Engagement + snapshot de CHAQUE occurrence d'événement à l'approche** (spec evenement-dossier § 1.3) — quotidien via cron-job.org (Bearer CRON_SECRET). Fenêtre GLISSANTE [J, J+7], IDEMPOTENT (engagement : aucun VIVANT pour saved_item_id×window_start, jugé au DERNIER état — **24/08 (owner) : un engagement ANNULÉ ne bloque plus, l'occurrence se ré-arme tant que le jour est dans la fenêtre, SAUF annulation « Arrêter » (`adjustment_move='stop'`) qui reste définitive** ; cas fondateur : occ. 15/08 du Corner armée à J-7 puis annulée, jamais ré-armée ; snapshot : aucun pour saved_item_id×date ; hors-horizon → no-op retenté demain). Héritage de SÉRIE (seuil/texte/responsable/metric du 1er engagement de la série, repli kpi_target_pct défaut 10), `kpi_baseline` gelée (famille K8 via `measureFamilyBaseline`), écriture via `readMergeWrite` (TERM_COLUMNS garantis), `window_kind day_of` ancrée sur l'occurrence, échec soft par occurrence, plafond 50 créations/run. Prouvé 04/08 sur données réelles : 2 engagements harnais + 3 snapshots (dont l'occurrence 08/08 du Corner) ; rejeu → 0 création. A attrapé les 3 dérives de vue notées sur `saved-items/snapshot.ts` **05/08 (automatisation inc. 4)** : + l'ENVOI de la consigne d'opération — due si `consigne_enabled` × J-offset atteint × AUCUNE trace pour l'occurrence (idempotence = `consigne_sends`) ; destinataires = participants de l'occurrence (email) + responsable (roster par nom normalisé, repli email profil — patron alerts) ; email texte AUTO-SUFFISANT (participants externes sans app), sections vides omises ; rail `internalSend.sendEmail` + `loadChannelConfig` ; plafond 20/run, échec soft, « aucun destinataire » dit et retenté demain. E2E réel 05/08 : envoi Corner 08/08 à 1 destinataire (roster résolu), rejeu → 0, trace en base | `raw.saved_items` × `raw.saved_item_dates`, `analytics.action_commitments`, `raw.saved_item_snapshots`, `semantic.vw_insight_event_day_surface`, `raw.saved_item_participants`, `analytics.consigne_sends`, `analytics.team_members`, `raw.insight_event_user_location_profile`, `src/lib/actionCommitments`, `src/lib/kpiRegistry`, `src/lib/channels/internalSend` |
| `cron/digest.ts` | Weekly digest email (7d trends + competitor events) | `raw.insight_event_user_location_profile`, `…notification_preferences`, `dims.dim_client_location`, `semantic.…_ai_location_context`, `…_day_surface` |
| `cron/internal-alert-sweep.ts` | Process internal alert cards → Slack/email → persist. Channel config resolved ACCOUNT-level (19/07: site-first-else-account) | `analytics.channel_configs`, `…saved_drafts` |
| `cron/snapshot-competitors.ts` | Snapshot Google Places details for competitor locations  **17/08 : + balayage fiche-enrich en waitUntil** — cap 2 suivis/nuit (motif crawl-best-in-class ?n=1) : actu commerciale via `webContext.getCompetitorCommercialNews` (lecture web, cache 7 j sur `raw.competitor_directory.commercial_news_json/at` — colonnes ALTER IF NOT EXISTS) + analyse manquante via le GET de competitor-profile invoqué en direct (locals du suiveur) ; prouvé réel 17/08 : Guimet + Micromania enrichis ; C5 (19/08) : runEventEnrichment — enrichissement CIBLÉ des événements (portes gratuites rayon 15 km + même bucket industrie, cache 30 j, plafond 3/nuit, rail enrich-event) → audience_profile vivant pour le critère « publics » ; runEventSurface (19/08) : matérialisation nocturne de la surface événements publics (entonnoir bucket+15 km, dédup public-lu d'abord, CREATE OR REPLACE atomique ~30 s) — le tableau ne fait plus que lire | `raw.insight_event_user_location_profile`, Google Places |
| `cron/snapshot-homepage.ts` | Extract structured signals from institutional/media homepages via Browserless + Claude Haiku | `raw.competitor_directory`, `…competitor_tracking`, `…competitor_snapshots`, Browserless, Anthropic |
| `cron/snapshot-own-locations.ts` | Snapshot Google Places for own locations w/ place_id | `raw.insight_event_user_location_profile`, Google Places |
| `cron/sync-besttime.ts` | **23/08 — capture `venue_info.rating` / `venue_info.reviews`** (colonnes additives `venue_rating` FLOAT64, `venue_reviews` INT64 via `ALTER TABLE ADD COLUMN IF NOT EXISTS`). La réponse `forecasts/week` portait la note Google du lieu depuis toujours ; le cron ne lisait que `analysis`. Conséquence en aval : `stg_besttime_venues_filter` mettait `cast(null as numeric) as rating` sur la branche cron, donc `besttime_rating` NULL sur toute la chaîne jusqu'à `vw_insight_event_ai_location_context` — et un comparatif « votre note vs la leur » impossible. Prouvé E2E : 3 sites, `fetched: 3, failed: 0`, Muse Square 4,1 / 47 avis. Reste côté dbt : lire les 2 colonnes dans la branche cron du staging (2 lignes). | Weekly foot-traffic forecast from BestTime | `raw.besttime_foot_traffic`, `dims.dim_ai_context_location`, BestTime |
| `cron/sync-fb-performance.ts` | Facebook post insights → analytics | `analytics.channel_configs`, `…publish_log`, Facebook Graph |
| `cron/sync-gbp-performance.ts` | GBP performance metrics → analytics | `analytics.channel_configs`, `…publish_log`, GBP Performance |

### `profile/` — user & location management

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `profile/change-password.ts` | POST | Update password via Clerk | Clerk |
| `profile/crawl-website.ts` | POST | Crawl location site (Browserless) → extract via Claude → store enrichment | Browserless, Anthropic, `raw.insight_event_user_location_profile` (write) |
| `profile/location-add.ts` | POST | Create location, geocode via BAN, idempotent dedup | `raw.insight_event_user_location_profile`, BAN |
| `profile/location-delete.ts` | POST | Delete non-primary location | `raw.insight_event_user_location_profile` |
| `profile/location-rename.ts` | POST | Rename non-primary location | `raw.insight_event_user_location_profile` |
| `profile/location-set-primary.ts` | POST | Set location primary | `raw.insight_event_user_location_profile` |
| `profile/locations.ts` | GET | All user locations, primary first | `raw.insight_event_user_location_profile` |
| `profile/save-user.ts` | POST | Update identity in Clerk + BQ | Clerk, `raw.insight_event_user_location_profile` (write) |
| `profile/save.ts` | POST | UPSERT profile: geocode (BAN), BestTime register, website crawl, dbt trigger, dim sync ; champ `pos_system` (caisse, clé de `analytics.pos_systems`) ; couture C3 `locals.provision_identity` (identité injectée pour le pré-provisionnement par admin/invite.ts — locals est posé par le middleware côté HTTP, non injectable de l'extérieur) | `raw.insight_event_user_location_profile` (MERGE), `dims.dim_client_location` (MERGE), BAN, BestTime, Anthropic |
| `profile/set-active-location.ts` | POST | Set active location cookie (`ms_active_location`) | none |
| `profile/set-catchment.ts` **🆕 30/07** | POST | **Périmètre de clientèle déclaré** (`docs/perimetre-client-spec.md`) : `{location_id, catchment}` avec `catchment` STRICTEMENT `commune` \| `beyond` (toute autre valeur → 400, jamais coercée : un rayon deviné réintroduirait l'instabilité mesurée, 9 lieux sur 30 basculaient). `commune` → rayon **1 km**, `beyond` → rayon **20 km**, absence de réponse (`NULL`) → aucun rayon, comportement actuel préservé. Endpoint DÉDIÉ et non une extension de `save.ts` : la réponse vient d'un clic (carte Pulse ou sélecteur du profil), `save.ts` ferait un MERGE de ~40 champs + géocodage pour un mot. Écrit la table de profil (source de la chaîne dbt) PUIS synchronise `dim_client_location` en UPDATE non fatal — sans cette synchro la valeur n'atteindrait la dimension qu'au prochain run dbt et la carte reposerait la question entre-temps. Garde `requireLocationOwnership` | `raw.insight_event_user_location_profile` (UPDATE), `dims.dim_client_location` (UPDATE, non fatal) |

### `admin/`, `metro/`, `legacy/`, debug

| Route | Methods | Purpose | Data source |
|---|---|---|---|
| `admin/users.ts` | GET | List all users + aggregated location data | `raw.insight_event_user_location_profile` |
| `metro/search.ts` | GET | Search Île-de-France transit stops by prefix | `dims.dim_idf_stops_lines` |
| `legacy/legacy_days_compared_dates.ts` | GET | (legacy) selected-days comparison + points clés | `semantic.vw_insight_event_selected_days_surface`, `…_ai_location_context` |
| `legacy/legacy_month_data.ts` | GET | (legacy) 30-day window + calendar grid + points clés | `semantic.vw_insight_event_30d_window_surface`, `…_30d_surface`, `…_ai_location_context`, `…_ai_region_context` |
| `_debug-auth.ts` / `auth-inspect.ts` / `debug-auth.ts` | GET | Auth/locals inspection for debugging | none |

---

## Libraries — `src/lib/`

### Core / shared

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `kpiRegistry.ts` 🆕 (lib) | `KpiKey`, `KPI_LABEL_FR`, `KPI_DAILY_COL`, `KPI_PERF_KEYS`, `kpiCaseSql`, `kpiKeyListSql`, `kpiKeyForOrigin`, `isKpiMeasurable`, `measureKpiBaseline`, `measureKpiWindow`, `kpiDeltaPct` | **22/08 — KPI → COLONNE : foyer unique.** La correspondance était écrite QUATRE fois (ici, deux `CASE measured_metric WHEN …` dans dashboard.ts, et le dépivot `vals` du moteur de classes) ; `KPI_DAILY_COL` la porte seule, `KPI_EXPR` en est DÉRIVÉE, et les trois consommateurs la lisent via `kpiCaseSql`/`kpiKeyListSql`/`KPI_PERF_KEYS`. Un KPI de plus s'ajoute ICI. Équivalence prouvée : texte généré IDENTIQUE aux littéraux retirés (dashboard) ; ancien vs nouveau SQL dans la même requête, 231/231 clés, 4 écarts qui sont UNE ligne (`competition_high` marginal d'un site) déclinée sur ses 4 métriques — terciles `APPROX_QUANTILES`, non déterministes. **DETTE SIGNALÉE (22/08, arbitrage owner en attente)** : `KPI_LABEL_FR` n'a AUCUN consommateur, et les mêmes KPI portent d'autres mots dans `rapport.astro` (`DRIVER`), `salesDecomp.ts` (`DRIVER_FR`) et `public/event-form.js` (libellés du menu) — `transactions` s'y appelle « tickets/jour », « le nombre de transactions », « le trafic (nombre de ventes) » et « Tickets ». `docs/lexique.md` n'arbitre aucun de ces mots : concept sans mot ⇒ on le SIGNALE, on n'invente pas. Orphelin lié : `EVENT_KPI` accepte `visitors` alors que le menu n'offre pas cette option. | **Boucle multi-KPI (étape 3, 26/07)** — LE dictionnaire des KPIs mesurables (docs/kpi-enjeu-mapping.md §1). Clés = le vocabulaire EXISTANT (DRIVER_SET + metrics Type B : footfall/conversion/basket/transactions/discount/reputation/revenue_residual — jamais un 3e vocabulaire). kpi = f(type de carte, origin_driver), JAMAIS de la stratégie ; `measured_metric` du commitment EST la clé (plus jamais codé en dur). K1 garde sa machinerie (résiduel/VIF/verdict) ; les non-K1 mesurés en colonnes additives kpi_baseline/kpi_window_value/kpi_delta_pct (baseline = 30 j pré-fenêtre uniforme — mart baselines volontairement non réutilisées, ancrées à J ; distinct de declaredMetrics = déclaré, pas mesuré). 'reputation' : clé posée, mesure NULL (aucune série own-rating en entrepôt). `bq.date()` obligatoire (piège DATE/STRING). Verdict par KPI : LIVRÉ 15/08 (bande de bruit par KPI établie, voir colonne sources) | `mart.fct_client_daily_performance` ; **verdict-par-KPI (15/08)** : `measureKpiDailySd`/`measureFamilyDailySd` (écart-type 30 j pré-fenêtre, ≥ 5 j) + `kpiVerdict` PURE (provisoire ≥ objectif ; portes asymétriques bruit 1×SE VIF-corrigée + vacances ; testée `kpiVerdict.test.ts`) |
| `proposedFollows.ts` (lib) | `runProposedFollows` | **P3.1-f (19/08) : suivis proposés par menace à l'onboarding** — compte < 14 j dont la chaîne géo a abouti (menaces en mart) × 0 suivi × sans marqueur → top 5 `threat_score` écrits dans `watched_competitors` + `competitor_tracking` (`proposed=TRUE` → chip « suivi proposé — ajustez » sur la fiche tableau, infobulle critère) ; MÊMES tables et gardes d'existence que add-competitor (un suivi proposé EST un suivi : fiches, veille, crawls) ; idempotence = marqueur `action_log` `suivis_proposed` par site, posé même à 0 menace (absence dite, pas re-scannée) ; caps 3 sites/passage × 5 propositions ; appelé par cron/competitor-surveillance (waitUntil, cadence 15 min → J0+15 min) ; preuve `scripts/proposed-follows-verify.mts` 6/6 (tenant jetable + rejeu 0 + fiche dashboard) | `raw.insight_event_user_location_profile`, `mart.fct_competitor_threat_profile`, `raw.competitor_directory`, `raw.watched_competitors`, `raw.competitor_tracking`, `analytics.action_log` |
| `dayClassRegistry.ts` 🆕 (lib) | **LECTURE TOLÉRANTE AUX DEUX SCHÉMAS (23/08)** — le store est écrasé chaque nuit par le cron qui exécute le code de PRODUCTION ; `metric` n'existe que sur dev, donc la table oscille et `AND metric = 'revenue_residual'` échouait. Les `.catch` rendaient la panne MUETTE : repli live à **47 451 ms** (budget dur 3 s) côté registre, carte supprimée côté dashboard. On tente la forme à colonne, on relit sans elle sinon. Après : 1 418 ms à froid, 597 ms à chaud, mêmes 6 pilules. Cause racine non traitée : pas d'isolation dev/prod sur le store analytics. | **`classNeverMeasured` (23/08)** — second critère de la couture de suppression de `monitor.ts`, à côté d'`immaterial` (29/07). « Immatériel » = on SAIT et ça ne pèse rien ; « jamais mesurée » = on ne sait pas. Suite de l'audit du 22/08 (la majorité des tirs adossés à une classe reposaient sur des classes qui ne passent sur AUCUN site). DEUX EXEMPTIONS voulues : (a) `no_history` — sites sans vente laissés en l'état, consigne owner ; (b) cartes dont la classe se résout PAR DATE (`weather@date`) — une alerte météo vaut par sa PRÉVISION, pas par son prix. Sans (b) le filtre retirait 21 tirs sur 52, dont `weather_hazard_onset` (13). Effet mesuré sur la base : **751 cartes de sites sans historique intactes, 192 gardées, 31 filtrées** (`competition_proximity` 12, `sales_discount_no_lift` 10, `same_bucket_saturation` 8, `competition_pressure_spike` 1). Le prédicat vit ICI, avec la politique — jamais réinventé chez l'appelant. | **`tourism_high` DÉTACHÉE DES 4 CARTES (22/08)** — classe inmesurable PAR CONSTRUCTION : `tourism_index_region` est un indice MENSUEL (12 valeurs distinctes / 484 j), donc le tercile haut prend des MOIS entiers (juin+juillet = 61 j sur 141) ; le contrôle de cellule étant (lieu × mois × week-end), `ctrl_n = 0` sur les 4 cellules et la base 'marginal' ne produit RIEN ; reste la base 'pure' à n = 1-2, sous le plancher n >= 5. Audit 22/08 : passe sur 0 site sur 6. Effet : `mapped` faux → **silence** au lieu de « mûrit avec les saisons », promesse qui ne pouvait pas être tenue. `COMBO_TYPE_CLASSES` NON touché à dessein (un token sans impact y est ignoré, les autres portent le coin — vérifié : tourism_weather_vacation garde −18 897 €/an, tourism_comp_squeeze +6 891 €/an). Les 3 autres cartes ont été retirées de dbt (PR #45). | `WEATHER_DAY_CLASSES`, `TERCILE_DAY_CLASSES`, `dayClassAggregateSql`, `computeDayClassImpacts` (live fallback), `getDayClassImpacts` (store-first), `enjeuForCandidate`, `DAY_CLASS_STORE`, `DayClassImpact` | **Day-class registry — the ONE home of « Enjeu €/an »** (spec + décisions + backlog : `docs/enjeu-day-class-registry.md`). **RÉGIME LOG + MÉDIANE (01/08, GO owner — preuves `docs/residu-bruit-diagnostic.md`)** : la significativité se teste sur le résidu LOG (`ln(rev)−ln(attendu)`, centré par construction — le t linéaire fabriquait des faux positifs à +2,0..+2,8 sur des classes d'effet nul), l'€/an = MÉDIANE des gaps € × fréquence réelle (insensible au biais de retransformation +1 942 €/j des Olivades et aux factures — une seule portait 76 % des € de competition_high) ; garde `sign(t_log)=sign(médiane)` sinon absence honnête ; `discount_no_lift` (classe COÛT) reste linéaire ; repli legacy sur lignes sans stats log. Avant/après rejoué par le vrai chemin : 19 pilules → 16, les 3 mortes = les 3 fantômes des Olivades, zéro mort sur les 4 lieux propres. JAMAIS extrapolé du jour. **Temps 2 périmètre (01/08)** : `catchmentHypothesisSql` + `CATCHMENT_HYP_STORE` (`analytics.catchment_hypothesis_days`) — jours mesurables par hypothèse (`commune` 1 km / `beyond` 20 km), lus en 4e requête parallèle de `getDayClassImpacts` → `DayClassResult.catchmentHypotheses` → `monitor.catchment_days` (cartes `needs_catchment` seulement) → item action-cards (v=39) → ligne neutre sous les boutons de l'étape 1 M'engager (pulse `_openCatchmentStep`). Olivades 0/21 (= la mesure validée 31/07), Muse Square 39/32. **Doctrine valeur d'action (01/08 soir, 4 commits)** : famille `card` au store (populations de tirs des cartes B/C — `pop_revenue_down`/`surge`/`underperformance` (non branchée, arbitrage ouvert)/`traffic_not_conv`), `CARD_POPULATION`/`CARD_CONTEXT_CLASS`/`CARD_VALUE_TYPES`, `motifContextForCandidate` (l'ex-Motif de fond = CONTEXTE), `corner_day_mode` (amendement 6), matérialité dispensée famille card (amendement 5), monitor filtre `family !== 'card'` du structurel + transmet `context_motif`/`corner_day_mode` ; `structuralCardCopyFr` → registres correctif/identification (amendement 7) ; pulse : `_structFullCardHtml` (motifs promus en cartes pleines, plafond 2, règle 5 via `data-t-class`, tri unique horizon puis \|coin\|, CTA « Reproduire le dispositif » → MSBpForm) + coin « € ce jour » (action-cards v=41). Spec : `docs/doctrine-valeur-action.md`. Gates : n>=5+span>=60j+**|t|>=1** (estimé — le plancher t existe car les classes terciles passent n>=5 par construction ; bruit t=0,08 prouvé filtré), n>=10+|t|>=2+**span>=300j** (mesuré) ; négatif-only (ambre) ; pas de mesure → null → pas de pill. **Incrément 1 (24/07 soir)** : STORE OFFLINE `analytics.day_class_impacts` (agrégats BRUTS location×classe, policy appliquée à la LECTURE dans `rowsToImpacts` — un changement de gate ne re-batch jamais), rebuild nightly par `cron/day-class-impacts` ; `getDayClassImpacts` = store-first + fallback live (compte pas encore batché) ; historique borné 730 j (partition features). Classes (étape 2, 24/07 nuit) : météo 5 + terciles `competition_high`/`tourism_high`/`events_high` + `mobility_disruption` + `followed_activity_high` + calendrier contrôlé (`school_holiday`/`public_holiday`, mois×type-de-jour, ctrl>=3 j). **CONTRASTES PROPRES** : jours PURS uniquement (n_memberships=1) — un jour n'est jamais facturé deux fois ; sur 90 j d'historique les classes d'été se recouvrent → moins de pills (comportement voulu, elles reviennent avec l'historique). Pill VERTE « À capter » = écart positif. Combinés = classe au plus grand |€/an| mesuré. **Étape 4 (26/07)** : + classes `traffic_high` (tercile visiteurs mesurés) et `discount_no_lift` (classe COÛT, € remisés négatifs, hors pureté/saison — fait du jour) ; attache sales_traffic_not_converting/sales_discount_no_lift. **Étape 2.5** : store à DEUX bases par classe (`basis` 'pure'/'marginal' — marginale = tous les jours de la classe, gap − contrôle HORS-CLASSE mois×type-de-jour, >=3 j/cellule) ; lecture pure-d'abord, sinon marginale plafonnée 'estimé' + étiquette « estimé, facteurs mêlés » (`tier_label_fr`/`entangled`) — l'intrication est dite, jamais cachée. Attache : payload (`weather_hazard_onset`) / date affectée (`weather_worsened`, `extended_bad_weather*`) / mapping type→classe (concurrence, tourisme). Consumer : monitor.ts → pill pulse `buildMetricsStrip`. **MÉTRIQUE = DIMENSION (22/08, point 3 du plan owner)** : le moteur ne mesurait QUE le résidu de CA alors que `kpiRegistry` en propose huit — deux vocabulaires pour la même question. CTE `vals` (dépivot) → `class_metric` → cellules et projection finale groupées par `metric` : `revenue_residual` (deux bases) + `transactions`/`basket`/`discount`/`footfall`/`conversion` en base **'marginal' seulement** (ces cinq n'ont pas d'attendu par jour ; leur seul comparatif est le contrôle de cellule déjà en place — un « 289 tickets » en base pure serait un nombre vide). Log = LN(valeur) − moyenne(LN(contrôle)), donc log-ratio centré : portes t/cohérence/matérialité inchangées. ABSENTS : `reputation` (aucune série client, besttime_rating 100 % NULL) et `family_revenue` (grain PRODUIT, autre jointure). **`rowsToImpactsWithImmaterial` est le SEUL point d'entrée de lecture et filtre `metric` (défaut `revenue_residual`, NULL compris)** — sans ce filtre six lignes se disputeraient la même `class_key`. Équivalence PROUVÉE : ancien vs nouveau SQL dans la même requête, 81/81 clés, 1 seule ligne mouvante — et l'ancien SQL diffère DE LUI-MÊME sur exactement cette ligne (terciles `APPROX_QUANTILES`, non déterministes). Backlog ordonné dans le doc (store offline nightly, classes concurrence/suivis/événements/mobilité/tourisme via impactContrast + policy chevauchement, pill verte, cartes structurelles) | `mart.fct_location_context_daily`, `mart.fct_client_day_residual`, `dims.dim_client_location` (**31/07** — `client_catchment`, le périmètre DÉCLARÉ : `commune`→1 km, `beyond`→20 km, absence→500 m l'existant. Lue sur la DIMENSION et non sur `fct_location_context_daily`, qui ne porte pas la colonne — l'y lire faisait échouer `dateResolutionQuery` ET `dayClassAggregateSql` en entier, donc aussi la résolution météo/calendrier par date de toutes les cartes) |
| `insightFamilies/` 🆕 | `FAMILIES`, `familyForQuestion`, `footfallFamily`, `offeringFamily`, `competitorFamily`, `eventsFamily`, `tourismFamily`, `weatherFamily`, `audienceFamily`, `salesDiscountFamily`, `salesDecompFamily`, `calendarFamily`, `FamilyResult`/`FamilyProvider` | **"One provider, three consumers"** dispatch table for insight card families. Each provider reads ONE family's data ONCE → `{data (→ MSCardKit.render), facts (claim-typed → toGroundedDayPayload extraFacts), sources}`. Three consumers reuse ONE provider: (1) deep-page endpoints (`insight/<family>.ts`, thin wrappers over `.data`); (2) `insight/family-report.ts` (multi-family digest); (3) the grounded prompt Q&A — a dateless family question is answered family-LED and its **full card renders INLINE in the chat** (`prompt.ts` attaches `family_card`, `ie-prompt.js` renders it via `MSCardKit`; provider `facts` also fold in as grounded `extraFacts`). `familyForQuestion` normalizes accents ("A quel moment" == "À"); matchers require a sales/traffic context so event-date-picking ("meilleurs jours du mois") does NOT route here. Registered: **footfall** (WHEN I sell) + **offering** (WHAT I sell — `offering.ts`, reuses `fetchIdentityAggregates`; facts = concentration/long-tail + **temporal layer** (weekday/weekend + seasonal mix shifts, `analyzeTemporal`, noise-band gated: material floor 3pp AND ≥2×SE for weekday/weekend, ≥5pp monthly range for seasonal → **honest-absent on flat data**, e.g. the uniform Kaggle seed f10c3e58 emits nothing); mix/items/basket come from the generic identity injection; `renderOffering` card built in `card-kit.js` (mix table + concentration + best-sellers + temporal honest-absent + panier; verified via `card-harness.html` on real f10c3e58). Reads `mart.fct_client_offering_daily` for the temporal query). Footfall FIRST so timing matches before offering. Other families roll on mechanically | the `semantic`/`mart` sources of each family **`competitor` (🆕): reads `semantic.vw_insight_event_competitor_offering_changes` (NOT the intermediate — layering fixed 15/07). EXTRACTED from `api/insight/competitor.ts` (now a thin wrapper) — États A/B + the 40 % `REAL_BAR` live in the provider, so deep page / report / chat can never disagree (the chat must not rank a 33 % near-miss while the card says « aucun concurrent n'a d'impact mesurable »). Matchers TIGHT to the possessive/set framing: the router short-circuits the Haiku classifier, so a bare `/concurrent/` would hijack `ENTITY_IMPACT` and kill web_search discovery for NAMED entities.** **`events` + `tourism` (🆕): same extraction — endpoint becomes the wrapper. `events` facts deliberately DIVERGE from card copy: the card may say « peu disputé » / « territoire dégagé » (an inference from ABSENCE), a fact must not — crawled-event absence is not event absence. **MEASURED-IMPACT ENGINE v1 (16/07) — shared math in `impactContrast.ts` (gates ≥10 j/side + hi>lo, |t|≥2 quantifies, tier capped emergent, `IMPACT_NOTE_FR`), used by events AND competitor. Events: `measureEventDensityImpact`: high-vs-low nearby-event-density days compared on `fct_client_day_residual.residual_pct` (dow+trend already controlled) × `fct_location_events_radius_daily` (all + same-bucket counts ≤500 m; terciles — binary event/no-event is useless in a dense city). Gates: ≥10 days/side AND hi>lo variation; |t|≥2 quantifies (claim_type observed_difference + tier, capped at emergent — |t|≥3 & n≥30/side; one window never earns établi); below gate the NULL is stated WITH its numbers (a measured « aucun écart mesurable » IS the verdict); cold start states WHY (days covered / none). Facts phrase associatively (« se situe au-dessus de sa normale ») — the tier lets the model causally upgrade only under rule 3bis. Card block `data.impact` → renderEvents « Impact mesuré sur votre CA » section (additive; absence-with-reason shown). E2E: « vont-ils cannibaliser mes ventes ? » routes events (`\bcannibalis` matcher) → family_grounded_claude verdict citing the measured facts. Sources: + `mart.fct_client_day_residual`, `mart.fct_location_events_radius_daily`. COMPETITOR (`measureCompetitorImpact`, 16/07): the TWO competitor definitions measured separately — (1) SUIVIS: daily count of ACTIVE followed-competitor events (`vw_insight_event_competitor_signals.entity_is_followed`, event_date..end ranges; intensity terciles — presence is useless, exhibitions run for months); (2) MÊME SECTEUR ambient: `competition_index_local` terciles (`fct_location_context_features_daily` — the app's ONE canonical daily pressure; no ad-hoc fork). Facts name THEIR variable (followed activity ≠ same-bucket events ≠ ambient index — correlated, never blended into one claim); attached to BOTH États + `data.impact` → shared `msImpactBlock` in card-kit (renderEvents + renderCompetitor). f10c3e58: followed +21,6 pp (36 vs 31 j, t≈5, émergent — positive, animation); ambient −5,7 ± 4,7 (measured null).** `tourism` facts stay REGIONAL (« en <région> », never « vos visiteurs »): it measures the region, not the venue's door.** **`weather` R4-1 (08/08, juge 2,6→4,1) : la condition DEMANDÉE prime la dominante du jour (`run` gagne un 4e param optionnel `question` — contrat FamilyProvider étendu, autres familles inchangées ; « quand il pleut ? » un jour de canicule analysait la chaleur) ; niveau 0 aujourd'hui → fait « Pas de pluie aujourd'hui — l'analyse porte sur vos jours de pluie passés » ; le plancher MIN_COND_DAYS et sa phrase honnête s'appliquent à la condition demandée.** **`weather` (🆕): registered FIRST — footfall's /(quand…).{0,35}(vend…)/ otherwise answers "quand il pleut, je vends moins ?" with a peak HOUR. Chain = `observed_difference` (comparable-day association), NEVER `measured` (that is Engine 2's causal store). Facts respect MIN_COND_DAYS=5 for the chain AND the product split — the card shows a thin split labelled, a FACT must not (n=1 « −50 % » is noise sold as insight). **SALES not extracted**: renderSales(j, isDown, date) needs `isDown` from the firing SIGNAL; a question-scoped family has none, and deriving it invents a 2nd definition of "down" — owner decision.** **`audience` (🆕): registered LAST — tourism owns the foreign-visitor framing, footfall owns affluence/timing. Facts separate DECLARED profile (« public déclaré ») from BESTTIME estimates (named as such): a bare « votre pic est à Xh » would contradict the footfall card, which exists to say « fiez-vous à votre CA, pas à l'affluence externe ».** **`salesdiscount` + `salesdecomp` (🆕): NOT the blocked `sales` card — neither needs `isDown` from a firing signal, so both are question-scoped. `salesdiscount` facts are strictly ASSOCIATIVE (a day may be discounted BECAUSE it is slow) and the reverse-causality caveat is itself a citable fact, so the model cannot drop it while quoting the number it qualifies.** **`calendar` (🆕): built from scratch (no deep-page endpoint) — chat + report only; `renderCalendar` does not exist and the client skips a missing render. Does NOT copy weather's naive condition-vs-all split: calendar conditions are PERIOD-CLUSTERED (vacances ARE July/Aug, half the fériés ARE May), so a naive split reports a seasonal TREND as a holiday effect — measured on the seed, the naive « fériés −23 % » collapsed to −8 % ± 7 (n=6, t=−1,2) once each occurrence was controlled by its OWN month + day-type. Gates: MIN_OCCURRENCES=5, MIN_CONTROL_DAYS=3, |t|>=2; below them it states WHY it is silent. The control costs coverage (74 vacances days -> 11 usable) — that is the price of not measuring the season.** |
| `actionCommitments.ts` | `readLatestSnapshot`, `readMergeWrite`, `assertTermsPresent` | Read-merge-write core for commitments (CAS-retry). `COLUMN_SPEC` includes `origin_factor` (Engine-1 A↔B bridge) + structured-retro cols `retro_worked`/`retro_change`/`retro_repeat` (Spec 2) + adjustment "how" cols `adjustment_move`/`adjustment_note`/`parent_commitment_id` — drives INSERT column list + typed nulls. **+ gel enjeu (26/07)** : `creation_enjeu_eur_year/tier_label_fr/label_fr/class_key/entangled/inherited` — provenance VERBATIM de la pill, rendue telle quelle par la page évolution | `analytics.action_commitments` |
| `bestPractices.ts` 🆕 | `insertBestPractice`, `listMatchedPractices`, `listClassDispositifs`, `linkReplayCommitment`, `ensureBestPracticesTable` | **03/08 — `listClassDispositifs(bq, location_id, day_class_key\|null, limit)`** : les fiches actives (par motif ou toutes) + tier à la lecture + état de l'engagement lié (`commitment_status='open'` = test en cours) ; sert la continuité de l'enquête (provider dispositif) et `buildPracticeFacts` (Consulter). **Objet dispositif (01/08, atelier)** : colonnes `mechanism_factors`/`evidence_refs`/`confirmation_test` (ALTER idempotent dans ensure — CREATE IF NOT EXISTS ne modifie jamais un schéma), servies par listMatchedPractices, acceptées par POST /api/best-practices — une pratique avec confirmation_test EST un dispositif, tier déclaré→prouvé par le rejeu existant. Base « Vos bonnes pratiques » du lieu : table app-write `analytics.best_practices` (COLUMN_SPEC, CREATE IF NOT EXISTS au premier usage). Le TIER est CALCULÉ À LA LECTURE : déclarée, ou « prouvée » quand son replay_commitment a `verdict='met'` (JOIN — aucun cron de promotion) ; la lecture UNIONNE aussi les engagements résolus 'met' existants (retro_worked/committed_action_text, `measured_metric` NULL coalescé → 'revenue_residual', l'historique pré-colonne). Vocabulaire d'appariement 100 % existant : kpi (kpiRegistry), outcome_lever (bestInClassStore.leverForActionType, dérivé jamais demandé), day_class_key (dayClassRegistry) ; seul `means_lever` est choisi par l'utilisateur. **05/08 (automatisation cas 1)** : colonnes `arm_*` (enabled/recipient_name/recipient_contact/channel/cooldown_days — ALTER dans ensure) + `updateArming` (table DML → UPDATE sûr, COALESCE champs non fournis, défauts email/7 j) — l'armement sur signal vit SUR la pratique | `analytics.best_practices`, `analytics.action_commitments` |
| `admins.ts` | `ADMIN_USER_IDS`, `isAdmin` | Admin allowlist + checker | none |
| `bq.ts` | `makeBQClient` | BigQuery client factory (SA key / keyfile) | client setup |
| `dbt-trigger.ts` | `triggerDbtJobs`, `triggerSalesRefresh` | Fire-and-forget dbt Cloud job triggers | dbt Cloud API |
| `error-logger.ts` | `logCrawl`, `logApiError` | Fire-and-forget BQ error logger (never throws) | `analytics.crawl_log`, `…api_error_log` |
| `insighteventUrl.ts` | `buildMonthUrl`, `buildDaysUrl`, `parseSelectedDatesCsv`, `serializeSelectedDates` | Insight-event deep-link URL builder/parser | none |
| `rate-limit.ts` | `rateLimit`, `rateLimitResponse` | In-memory per-user rate limiter (per-instance) | none |
| `requireLocationOwnership.ts` | `requireLocationOwnership` | Access guard (admin or owned location_id) | none |
| `scope.ts` | `resolveOperationalScope` | Multi-site scope resolver (single / all) | none |

### Recommendation taxonomy

| Module | Key exports | Purpose |
|---|---|---|
| `reco-taxonomy.ts` | `RECO_BUCKETS`, `ACTION_TYPE_THEME`, `isFeedOnly`, `isThemeDisabled` | 5 buckets × 9 themes × 73 action cards; theme suppression |
| `vitest.config.ts` 🆕 (racine) | `test.exclude` | **Exclut `.claude/worktrees/**` de la découverte des tests.** Les worktrees d'agents vivent SOUS le dépôt et contiennent une copie complète de `src/` : sans exclusion vitest découvrait chaque test 3 fois et tout comptait triple (« 27 failed » pour 9 échecs réels le 31/07), sur des copies figées à un ancien commit. Exclusion et non suppression — ces worktrees appartiennent à des sessions en cours (`git worktree list`) | — |
| `insightFamilies/channels.ts` 🆕 | `channelsData(bq, locs, start, end)` (cœur période libre), `channelsFamily` (provider, période canonique 90 j ancrée data_end), `channelsProvider` (registre — DERNIER : motifs à mot de canal/compte explicite, routage prouvé par harnais 8 cas) | Famille CHANNELS (R1, docs/rapport-canaux-spec.md) : hiérarchie Groupe→Site→Canal (site mono-flux présenté au niveau site), 4 questions (gabarits `lib/fr/rapportCanaux.fr.ts` owner-editable), semaines/mois jugeables (états des cartes), comptes nommés + totaux SQL (décision 10), dormants = MIROIR du filtre carte, facts claim_type 'observed' (jamais 'measured' — réservé moteur). Prouvé : harnais tsx 18 assertions sur données réelles (groupe 211 698 € au centime, muse absent) + rendu interactif vérifié navigateur | `staging.stg_client_transactions`, `mart.fct_location_channel_weekly/monthly`, `mart.fct_location_client_patterns`, `analytics.party_directory`, `analytics.sales_channel_labels` 🆕 (app-write, libellés par compte), `dims.dim_client_location` |
| `recoThemeMap.ts` | `RECO_THEME_ACTION_TYPES`, `filterDisabledThemes`, `themeForActionType`  **Étape 4 (26/07)** : exporte `DEMOTED_TO_FEED` (informationnelles + signaux concurrents non-réputation) — `filterDisabledThemes` les retire des candidates (monitor + days d'un coup) ; le Fil d'actualité les sert toujours. | Canonical theme→action_type map (parity w/ `public/action-cards.js`); `themeForActionType` derives a commitment's `origin_factor` (theme) at capture **Parité rétablie 31/07** : `foreign_tourism_signal` (client → « Calendrier & affluence », entré 15/06) et `footfall_vs_basket_decomposition` (client → « Performance ventes », 10/06) figuraient dans le taxonomy CLIENT sans exister ici — or « uncovered action_types always pass », donc couper le thème dans /profile ne les aurait PAS filtrés ; le bouton mentait, sur la carte la PLUS fréquente du parc (128 tirs / 32 lieux, 90 j). Latent au 31/07 (aucun compte n'a de thème désactivé EFFECTIF : la config la plus récente prime et la sienne est vide). `recoThemeMap.parity.test.ts` était rouge depuis. **31/07 — `foreign_tourism_signal` démise puis ANNULÉE le même jour.** Le verdict « Démettre au Fil » de `docs/card-truth-audit.md` portait sur le PAYLOAD (identique sur les 32 sites), pas sur la stack : la mesure existe dans `mart.fct_region_foreign_country_profile` (27/32 lieux couverts) et les champs `countries_named` / `share_total_pct` / `n_countries` du payload sont réservés et NULL. Branchement manquant, pas mesure absente — voir le § RÉEXAMEN du 31/07 dans l'audit, qui reclasse les 9 verdicts par la NATURE de ce qui manque (déclaratif / non branché / copie / règle / inexistant / trop mince) et conclut que **2 des 4 démotions du 28/07 étaient fautives**. Mécanique confirmée au passage : une carte démise sort des Actions du jour mais RESTE au Fil et dans Consulter — `mergedFeed` n'est pas filtré par `DEMOTED_TO_FEED`, seul `action_candidates` l'est (`monitor.ts:976`, `days.ts:563`). |

### Commitments ("M'engager")

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `commitmentConstants.ts` | `WINDOW_DAYS`, `THRESHOLD_Z`, `MATERIAL_SHARE`, `GRACE_DAYS`, `RHO_FLOOR` | Single-source resolution gate constants | none |
| `commitmentContext.ts` | `assembleEvolutionExtras` | Evolution-page extras: holiday norm, context, advice. action_type track record via the brain's **`getActionRollup`** scoped sub-accessor (the ONE outcomes read, pre-explode commitment-grain COUNTIF, no double-count) — not the full assemble. Range-based named-context queries stay (legit period view); named_events are RELEVANCE-GATED (drop standing admin listings: `event_duration_days > 90` / theme `Education / Formation`) | `mart.fct_client_day_residual`, `…location_context_features_daily`, `…location_events_topn_daily`, `…foreign_tourism_context_daily`, `…client_daily_performance`, `src/lib/dayContext` (`getActionRollup`) |
| `commitmentCopy.ts` | `EVOL_COPY` | Canonical French copy for engagement UI | none |
| `commitmentOrigins.ts` | `COMMITMENT_ORIGIN_ACTION_TYPES`, `isCommitmentOrigin` | Allowlist of action types that seed commitments — COMPLET depuis 26/07 (totalité du registre SPECS de action-cards.js + `structural_*` + `chat_decision_*` + `onboarding_first_test` origin de geste 06/08), menu Agir universel. **07/08 : + `client_dormant`** (C1, docs/client-patterns-spec.md), arrivé AVEC ses plans reco-library comme l'exige recoCoverage.guard | none |
| `commitmentResolve.ts` | `resolveCommitment`, `vif` | Statistical resolution: window metric, z w/ autocorr VIF, holiday gate. **Basis `'pct'` (18/07): verdict = window residual % ≥ the user-set %; a pct 'met' with corrected z < 1.0 → `confounded` (non concluant — never an attributed gain); `residual_z` rows keep the legacy path.** Also computes `window_active_factors` (CSV) — registry factors active on ≥ `WINDOW_FACTOR_SHARE` (0.50) of window days, via the single-source registry predicates against its declared `context_table` | `mart.fct_client_day_residual`, `…location_context_features_daily`, `mart.fct_location_context_daily` (factor predicates), `src/lib/sensitivityFeatures.json` |

### Sensitivity (Type B)

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `sensitivityFeatures.json` | `revenue[]` (feature → `tier` → `predicate`/`impact_col` → `mechanism` → `fittable`) | Type B / context-decision SINGLE-SOURCE registry — one tier-tagged list drives the engine fit list/build (**`fittable` only**), the endpoint `ACTIVE_EXPR`, and the Tier-2 `impact_col` map. No second list | none (data map) |
| `dayContext.ts` | `assembleDayContext`, `getActionRollup`, `namedEventsRange`, `foreignVisitorsRange`, `isCleanFrench`, `TOURIST_COUNTRIES` | **THE BRAIN** — the ONE assembler every consumer reads; nothing reads the store/learning/opportunity marts directly. **R3 altitude des faits (08/08, retour owner « too basic ») : (a) faits calendrier jour-type — « Jour en vacances scolaires » / « Jour férié : <holiday_name surface> » depuis les flags de `fct_location_context_daily` (requête tourisme ÉTENDUE, zéro aller-retour ajouté ; week-end omis exprès — test « déjà su ») ; (b) fait tourisme QUANTIFIÉ — profil pays régional `fct_region_foreign_country_profile` (en-tête dbt lu : projection SAISONNIÈRE dite dans la phrase, fenêtre saison+année portée, jointure par CLÉ NUTS2 jamais le libellé, latest ≤ date car la spine s'arrête aux années ingérées — IdF max 30/09/2025 mesuré —, un accommodation_type par pays via nights, parts = RATIOS 0-1 convertis à l'affichage : 61 % non-résidents, É-U 25 %). Toutes les sorties re-testées sur données réelles (leçon dateResolutionQuery).** Composes Engine 2 (`getSensitivities`, w/ `active_today`), Engine 1 (learning → `actionTrackByFactor`; outcomes rollup → `actionTrackByType` via `getActionRollup` sub-accessor), Tier-2 estimation (`delta_att` → `impacts`, **suppress-in-2 applied — measured drops the prior**), competitors, **the Engine1×Engine2 `decomposition` (observed_difference, no-action baseline, independent-N tier), the static `profile` (venue context), + the `llm` contract (CLAIM-TYPED `citable_facts` measured/observed_difference/observed_proximity + `driver` observed_ranking + `forbidden` — bans causal verbs on driver/competitors/tourism; declared_weather_sensitivity ≠ measured; **`forbidden[0]` carve-out (Phase 1 #4): same-unit sum/diff of two CITED numbers is licensed, validator recomputes; `forbidden[2..4]` (Phase 1 #5): causal verbs banned on untiered facts, licensed on a CITED measured/observed_difference fact carrying its tier token in the same sentence, future promises banned at every tier. `citable_facts` now carry `tier` on those two claim types (Engine 2 `confidence_tier` / decomposition `tier`) — previously computed then dropped at the mapping, so the model never saw the register its facts rest on**)**. `opts.devSeed` swaps demo fixtures | Engine 2 `analytics.b_sensitivity_store` (via `sensitivityStore`); Engine 1 `mart.fct_location_commitment_learning` + `…fct_client_commitment_outcomes`; `mart.fct_location_opportunity_components_daily` (delta_att); `semantic.vw_insight_event_ai_location_context` (profile); `semantic.vw_insight_event_day_surface`, `mart.fct_location_impact_daily_mobility`, `…mobility_disruption_changes`, `…daily_action_candidates`, `…events_topn_daily`, `…competitor_threat_profile`+`…directory`, `intermediate.int_competitor_offering_changes`+`…snapshot_deltas`, `…foreign_tourism_context_daily` (school **+ public** holiday origins, deduped union); reservoir: `mart.fct_region_day_annotations_daily` (commercial events, deduped vs named events), `…weather_alerts_daily` + `…weather_forecast_daily_detail` (acute alert — distinct register, NOT suppress-in-2'd vs measured heat); **signals[] REUSED from detection: `semantic.vw_insight_event_change_feed` (observed_change) + `…vw_insight_event_action_candidates` (observed cards) — distinct grain from context{}, never re-detected**; decomposition reads `analytics.action_commitments` (windows). **Scoped `opts.slice` (full/context/brief/signals)** — light surfaces skip Engine-1/decomposition; **`brief` (Point du jour / cron) also skips competitor enrichment + delta_att estimation** (~30% faster, same citable_facts). **Phase 0.5/1: day_surface read is `SELECT *` → structured `day_surface` projection (opportunity/competition/weather-surface/attribution) + raw `day_surface_raw`; profile read is an explicit used-column projection (SAFE_OFFSET on nearest_transit_line_name) → `VenueProfile`(+`transit`,`enriched`) + raw `profile_raw`; these RAW rows are monitor's Phase-1 compatibility bridge (single-reader). Foreign origins French via `contextCopy.frCountry`. In-process SLICE-AWARE memo (2-min TTL, per loc/date/slice/devSeed; a broader cached payload serves a narrower request). SCOPE: in-process only — guaranteed win is within-invocation (multi-date + repeat in a warm process); cross-endpoint collapse on Vercel is best-effort (separate invocations), guaranteed sharing needs a shared cache (deferred)** |
| `fr/evenement.fr.ts` + `fr/evenement.fr.guard.test.ts` 🆕 (lib) | **LE lexique du dossier d'événement, éditable par l'owner** (même convention que `fr/factOrigins.fr.ts` et `fr/rapportCanaux.fr.ts` : un fichier par surface). Deux exports : `MOTS_BANNIS` (interdit → mot maison : attendu→habituel, « sur la série »→libellé littéral, « à la cible »→« à votre objectif », « cible chiffrée »→objectif, rejeu→test, 17/08 : rejouable→réutilisable, leur page →/Sa page →→Consulter →, Vos dispositifs→Mes dispositifs) et `EVT_FR` (onglets, verdicts, série, en-cours, décision, partage, bilan) + `tEvt(key, vars)`. **Le garde-fou est le livrable** : `evenement.fr.guard.test.ts` scanne les CHAÎNES VISIBLES (littéraux hors commentaires, hors identifiants techniques `expected_*`) de `insightFamilies/evenement.ts`, `evenement.astro` et `public/event-form.js` — il a attrapé 9 occurrences d'« attendu » que la correction manuelle avait ratées, et il MORD (vérifié : réintroduction volontaire → échec). Le lexique lui-même est exclu du scan (il NOMME les mots bannis) mais ses valeurs sont testées. **10/08 (2) — PORTÉE ÉTENDUE (décisions owner)** : (1) « attendu » banni SEULEMENT au sens RÉFÉRENCE (« vs attendu », « l'attendu », « attendu du jour ») — le sens PRÉVISION reste correct et intact (« affluence attendue », « effet attendu ») ; (2) « cible » ET « objectif » acceptés — aucun ban ; (3) « rejeu » banni PARTOUT, clés internes comprises (`practice_counts.rejeu` → `en_test`, `tier "rejeu"` → `"en_test"` : producteur dashboard.ts + unique consommateur tableau.astro, aucun contrat externe) ; (4) un littéral d'UN SEUL token minuscule est traité comme CLÉ, pas comme prose. SURFACES = 11 fichiers (provider, evenement/tableau/pulse/insight.astro, commitmentCopy, event-form, action-cards, card-kit, draft-workspace, commit-form, **card-harness.html**). Le harnais est SOUS GARDE parce que ses fixtures DUPLIQUENT la copie réelle et dérivaient en silence (4 chaînes périmées alors que la source était corrigée). 14/14 ; rendu réel f10c3e58 via card-harness : 18/18 zones, zéro « attendu » de référence. `npx vitest run src/lib/fr/` | — |
| `contextCopy.ts` | `CONTEXT_FALLBACK_FR`, `CONTEXT_LABELS`, `STAGE_FR`, `formatDisruption`, `weatherNatureFr`, `formatWeatherAlert`, `ALERT_SUBTYPE_FR`/`frAlertSubtype`, `COUNTRY_FR`/`frCountry`, `formatEstimatePct`, `fillContextFallback`, `structuralCardCopyFr`, `STRUCTURAL_CHANTIER_FR` | Owner-final French for context surfaces. `weatherNatureFr` = THE one place per-nature alert levels (`lvl_*`) become a French label (names the NATURE, never a bare level; vocabulary from `sensitivityCopy.FEATURE_FR`). `frAlertSubtype` = competitor-alert email subjects (proximity / audience_overlap — the ONLY non-owner-authored strings in the file, delegated 2026-07-15, first to re-word). `frCountry` = EN→FR country names for foreign-origins facts. `STAGE_FR` + `stageVerifyDoneFr(n)` = the Consulter loading-stage labels (Phase 5 SSE; owner-approved incl. inc-②: `regen` → « Correction en cours », verify-done → « Vérification des faits — validée · n faits cités », n=0 → plain « validée », never padded). `MISSING_DIMENSION_FR` = the batch-2 elicit answers (marge / par_client / stock / personnel — DRAFT copy pending owner wording; consumed by prompt.ts `detectMissingDimension`). `premiseCheckFr` = the item-2 premise-check verdict sentences (DRAFT copy). Entries of `MISSING_DIMENSION_FR` may carry `cta {label, action:"upload"}` — ONLY where a real surface exists (marge/par_client → the chat's own file picker; stock/personnel text-only, a button must never be a dead end). `structuralCardCopyFr` = LE gabarit des cartes structurelles (titre + corps + chantier) rendu depuis `analytics.day_class_impacts` par `monitor.ts` — 15 classes sur 17 passent par son titre par défaut, donc une correction ici vaut pour toutes, y compris les classes pas encore mesurées. **Forme C (owner 21/08)** : « Les {classe} vous rapportent / vous coûtent » — « Les » et non « Vos » (la classe de jour n'appartient pas à l'exploitant), verbe et non adjectif (lexique règle 8), sans « du chiffre » (le corps le dit, le montant est au coin). 5 titres écrits en dur esquivent le gabarit (remises, événements+, affluence, concurrents suivis, jours calmes). `STRUCTURAL_CHANTIER_FR` = un plan par classe ; **clé par classe RÉELLE, jamais par famille** — la clé `heat` a été orpheline du 29/07 au 21/08 (scission de la chaleur par dose : les classes sont `heat_25_27` et `heat_28_plus`), les deux retombant sur le générique en production. `heat_25_27` reste SANS plan : la mesure en fait une classe distincte (−610 €/j contre −159 €/j sur le site le mieux mesuré), sa phrase attend l'owner. Mart `fact_text` is reused verbatim; this fills the gaps | none |
| `telemetrySink.ts` 🆕 | `sinkTelemetry` | **Polish tier (16/07)** — every `[telemetry][*]` line PERSISTED to `analytics.consulter_telemetry` (partitioned; flags/counts only, never model text): the queryable data the owner's KV-cache decision was gated on (Vercel logs are ephemeral). console.log stays for live grepping; insert is fire-and-forget behind `waitUntil` (@vercel/functions — the crawl pattern); dev falls back to a floating promise. Event types: grounded, report-exec-summary, declared-capture/-answer, missing-dimension, premise-check | `analytics.consulter_telemetry` |
| `sensitivityCopy.ts` | `FEATURE_FR`, `TIER_SECTION`, `TIER_TOKEN_FR`, `envTodayLine`, `envPeriodLine`, `actionLine`, `citeSensitivity`, `decompositionLine` | Type B French copy, tier-register aware (owner-final). `decompositionLine` = the observed-difference cite ("+X pts au-dessus de vos journées … sans action") — never a proven cause. **`TIER_TOKEN_FR` (Phase 1 #5)**: the tier as a MID-SENTENCE token (établi/émergent/préliminaire) — `TIER_SECTION`'s headings group rows and don't fit in a sentence. Does NOT breach the locked no-hedging rule: fact lines stay flat (the sample carries the honesty); the token is required only when the model UPGRADES a fact into a causal claim | none |
| `sensitivityStore.ts` | `getSensitivities`, `canInfluence`, `TIER_INFLUENCE` | Typed accessor for vetted sensitivity store (tier + influence gate) | `analytics.b_sensitivity_store` (→ `mart.fct_location_sensitivity` in prod) |
| `bestInClassCrawlCore.mjs` (+`.d.mts`) | `LEVER_LABELS`, `INDUSTRY_LABELS`, `INTENT_LABELS`, `SOURCE_REGISTRY`, `tierForSource`, `userPrompt`, `callSearch`, `extractPlays`, `validate`, `SCHEMA` | SINGLE SOURCE for the best-in-class crawl CONTRACT (vocab + prompt + honesty gate + load schema). **Registre de sources par vertical (27/07, étape 1 « méthodes pertinentes »)** : tier 1 institutionnel/fondation/académique (France Num, Bpifrance, CCI, Wallace, CultureHive, AMA, UNIMEV, Cornell, UMIH, Atout France ; motifs `.gouv.fr`/`.gov`/`.edu`), tier 2 rapports multi-lieux + presse pro (TRG Arts, Spektrix, L'Hôtellerie-Restauration, Vitisphere…) avec confiance plafonnée « moyen » ; HORS registre → play rejeté à `validate` (blogs vendeurs SaaS/agences/inconnus) ; prompt cherche `site:` prioritaire ; `source_tier` dans SCHEMA. **Gate de RECOMMANDABILITÉ (27/07, après audit owner)** : le SYSTEM refuse les gestes non conseillables (prix libre/gratuité de masse, stratégies fourre-tout, échelle d'institution nationale, RGPD), impose outil/opération NOMMÉS, titre en mots de patron, « un cas = un play » ; `validate` drop mécaniquement les outcomes-méta (« la source ne quantifie pas ») et les démonstrations hypothétiques (« si N % … ») ; `dedupePlays` (partagé script+cron) écarte les doublons même source+industrie d'un run. `INDUSTRY_LABELS` couvre les verticals réels (culture, wine_tourism, food_nightlife, cultural, non_profit inclus). ESM, portable (no BQ/fs); shared by the standalone script (`crawl-best-in-class.mjs`) + the demand-drain cron (`cron/crawl-best-in-class.ts`) — edit the contract HERE only | none (pure); uses global `fetch` |
| `bestInClassStore.ts` | `getBestInClassPlays`, `leverForActionType`, `intentForState` | Typed accessor for the "lieux comparables" crawl store — reads vetted external case-study analogs by vertical + lever (+ optional `intent`; best-first: **`source_tier` d'abord (1 institutionnel · 2 presse pro/rapports · legacy sans tier → 3, dernier)**, puis confidence, puis named source); `leverForActionType(type, driver?)` : TABLE EXPLICITE couvrant tout le registre commitmentOrigins (étape 2, 27/07 — météo/calendrier/concurrence/tourisme/mobilité/score→frequentation, prix/remises→yield, réputation/contenu→fidelisation, positionnement/offre→conversion) ; le driver décide pour les cartes ventes K1 ; préfixe `structural_`→frequentation ; garde anti-fallback-accidentel unit-testée (`bestInClassStore.test.ts`); `intentForState` maps the "Votre action paie-t-elle ?" verdict (below/aligned/above) → intent (pivot/reinforce/scale). An analog to try, never a promised result | `analytics.best_in_class_plays` (demand-loaded by `crawl-best-in-class.cjs`) |

### Channels / competitive / import (sub-libs)

| Module | Key exports | Purpose | Data source |
|---|---|---|---|
| `channels/internalSend.ts` | `sendSlack`, `sendEmail` | Internal RULE-alert send primitives (sealed rail). P3.1-c : `InternalMessage.reply_to` optionnel (Resend reply_to — absent, comportement inchangé) | Slack, Resend |
| `internalAlertCards.ts` | `V1_ALERT_ACTION_TYPES`, `V1_ALERT_ACTION_TYPE_SET` | V1 internal-alert allowlist (5 perf RULE cards) | none |
| `competitive/constants.ts` | `VALID_CONFIDENCE`, `VALID_INDUSTRY`, `VALID_AUDIENCE`, `audiencesFromProse`, `BUCKET_MAP`, `classifySource`, … | Industry/confidence validators + source URL classifier + **the ONE prose→audience-taxonomy mapper** (Claude describes audiences in French prose; the overlap formula compares exact codes vs the location vocabulary — every writer of `competitor_directory.primary/secondary_audience` must map through here) | none |
| `competitive/geocode.ts` | `geocodeCompetitor` | Geocode via BAN, reject >30km from city | BAN API |
| `competitive/url-discovery.ts` | `discoverAgendaUrl`, `discoverTarifsUrl`, `isAgendaPath`, `isHomepagePath` | Agenda/tarif URL discovery via Browserless BQL | Browserless BQL |
| `import/salesCsv.ts` | `parseSalesCsv`, `validateGrid`, `mapHeaders`, `parseDate`, `normalizeNumber`, … | Deterministic, pure CSV parse/validate for FR POS uploads. **16/07: `sage100` registered as a named source (Les Olivades' POS) — override EMPTY by design (Sage 100 exports are template-configurable; exact headers come from their FIRST real file, never docs); the GENERIC layer gained the French document-export vocabulary (date pièce/document/facture, montant ht/net, net à payer, total, dotted P.U. variants, famille article, quantité vendue, moyen de paiement…) — probe: pièce-grain, facture-grain and daily-aggregate shapes all parse with exact values. UI source chip « Sage 100 » first in the composer list. **29/07 — premier VRAI export Sage 100 reçu** (Les Olivades, 6 308 lignes, 28/08/2025→27/07/2026) : l'override `sage100` reste VIDE, la couche générique suffisait. Trois champs canoniques AJOUTÉS — `invoice_number`, `item_code`, `item_description` — parce qu'un export au grain LIGNE DE FACTURE porte le n° de pièce sur chaque ligne : sans lui, 7 lignes d'une même facture comptaient 7 ventes et le panier tombait à 152 € au lieu de 1 100 € (6 308 lignes / 871 pièces). Alias avec le `°` (il survit à `normalizeHeader`). `item_description` conditionne `fct_client_offering_profile` (grain item_category × item_description) ; le code seul reste lisible par l'exploitant, qui lit ses propres références** | none |
| `import/sourceMappings.ts` | `GENERIC_MAPPING`, `SOURCE_OVERRIDES`, `resolveMapping` | Per-source header→canonical field mappings | none |

---

## AI subsystem — `src/lib/ai/`

Deterministic-first pipeline: build facts → decide → render (French) → optionally package via Claude. Claude only ever sees allowlisted fields.

### Contract & runtime backbone

| Module | Key exports | Purpose |
|---|---|---|
| `msAsterContract.ts` | `MS_ASTER_CONTRACT_VERSION`, `MS_ASTER_CONTRACT` | Global non-negotiable AI invariants (MS ASTER constitution) |
| `assertions/assertions_v1.ts` | `assertNoSentenceWithoutFactIdV1` | Enforce: no surfaced sentence without a known fact_id |
| `contracts/facts_v1.ts` | `FactV1`, `LineItemV1`, `RenderLineV1`, `CoverageBlockV1` | V1 fact / line-item / coverage contract types |
| `models.ts` | `modelFor`, `ModelRole`, `capsFor`, `ModelCaps` | **SINGLE model-ID registry** — kills scattered literals + `CLAUDE_MODEL*` env reads; roles: packager (**Sonnet 5**)/**briefing** (Haiku, daily cron)/classifier/enrichment/web_search/drafting. **Also the per-model capability SST (`capsFor`)** — `sampling` / `structuredOutputs` / `thinkingDefaultsOn`, every row API-proven 2026-07-15. Sonnet 5 REMOVED `temperature` (400) so `claude.ts` gates sampling on it; `structuredOutputs` is a SEND policy not raw acceptance — Haiku is false (API-accepts but induces a derived-arithmetic reject in grounded modes, measured 4/4 vs 0/4) |
| `groundedPayload.ts` | `toGroundedDayPayload`, `groundedFactStrings`, `composeHonestAbsenceFr`, `GroundedDayPayload`, `CitableFact` | **THE grounding adapter (Phase 2)** — brain `dc.llm` envelope → grounded prompt payload (citable_facts + signals + engines + forbidden; optional `extraFacts`). **`CitableFact.tier` (Phase 1 #5)**: passed THROUGH from the brain on `measured` (Engine 2 `confidence_tier`) + `observed_difference` (decomposition `tier`) only — never defaulted, so an untiered fact can't satisfy the causal gate. The ONE interface the day prompt / Point du jour (and later drafts/reports/assistant) reuse; no re-derivation. **`composeHonestAbsenceFr` (Phase 1 #3)**: zero citable facts → deterministic "pas de donnée mesurée à citer" line naming the absent categories (OWNER-FINAL copy; null when any fact exists). **`CitableFact.origin` (Étape 1 attribution, 07/08, docs/explorer-attribution-spec.md)** : clé d'origine optionnelle, passthrough comme `tier` (posée au site de construction ou jamais) — DISPLAY-ONLY, le validateur ne la lit pas ; résolue en libellé fr (`lib/fr/factOrigins.fr.ts`) dans le `facts_catalog` de l'enveloppe. Tested: `honestAbsence.test.ts` |
| `fr/factOrigins.fr.ts` 🆕 | `FactOrigin`, `FACT_ORIGIN_FR` | **Libellés OWNER-EDITABLE des chips d'origine** (Étape 1 attribution Explorer, approuvés owner 07/08) — 12 clés → libellés fr (« Vos ventes », « Météo du jour », …) ; jamais un nom de table ni un ID ; « Affluence estimée » garde « estimée » ; « Web (source) » n'entre qu'à l'étape 5. Consommé par `insight/prompt.ts` (facts_catalog) ; assignations : dayContext (sensibilités/décomposition→ventes, concurrents→concurrence, temps commerciaux→calendrier, alerte→meteo, origines étrangères→tourisme ; cartes/changements SANS origin — hétérogènes), builders (identity/day-perf→ventes, practices→bonnes_pratiques, events→evenements_user), familles via `FAMILY_FACT_ORIGIN` (footfall/audience null exprès : faits mixtes mesuré/BestTime) | — |
| `recoCoverage.guard.test.ts` 🆕 (test) | 7 assertions | **Garde-fou de couverture de la bibliothèque de méthodes** — tient l'invariant de `docs/features/commitments.md` §5 (« every `COMMITMENT_ORIGIN_ACTION_TYPES` entry MUST have a reco-library entry »), écrit mais non tenu : violé 76 fois sur 83 pendant cinq jours sans que rien ne casse. **CLIQUET, pas idéal rouge** — la dette du 31/07 est listée nommément (76 types) ; le test échoue quand elle EMPIRE (un type entre dans l'allowlist sans plans) ou quand la liste MENT (un type listé a reçu ses plans), donc écrire des plans oblige à l'y retirer. Garde aussi : le câblage DÉRIVE des clés de la bibliothèque (le vrai blocage — une liste en dur imposait deux gestes), aucune couverture perdue, au moins un plan exploitable par type, aucun plan jeté par le plafond de 3 (testé sur la SOURCE : sur la sortie l'assertion est inatteignable, `_recosFor` fait déjà `slice(0,3)`). Exécute `public/reco-library.js` puis `public/action-cards.js` en `vm`, dans l'ordre de chargement des pages — lire à la regex dirait ce qui est écrit, pas ce qui est câblé. Chaque assertion vérifiée par MUTATION. `chat_decision_*` exclus car absents de SPECS (vérifié par le test lui-même, pas supposé) | `public/reco-library.js`, `public/action-cards.js`, `lib/commitmentOrigins.ts` |
| `briefingGate.ts` | `isMaterialBriefing`, `BriefingGateInput` | Pure Point du jour material-signal gate (no email on quiet days) — unit-tested (`briefingGate.test.ts`); acute weather / commercial / competitor change / ≥0.3 Δscore / saved-event milestone |
| `runtime/allowlist.ts` | `ALLOWED_INPUT_FIELDS`, `pickAllowedPayload`, `assertPayloadCoverage` | Restrict Claude visibility to semantic+decision fields |
| `runtime/claude.ts` | `callClaudeMessagesAPI`, `ClaudeCallUsage` | Wrapper for Claude `v1/messages` — model via `modelFor`; **prompt caching** on the system prefix (`cacheSystem`, returns `usage` incl. `cache_read`); `userText` for raw-text (classifier) calls. **Per-model request surface via `capsFor`**: drops `temperature` where the model rejects it (Sonnet 5), disables adaptive thinking where omitting it would fire (Sonnet 5), and sends `output_config.format` (structured outputs) when `outputSchema` is passed AND the model's send-policy allows — returns `structured` so callers know the JSON is schema-guaranteed |
| `runtime/json.ts` | `parseJsonObjectStrict`, `stripMdCodeFence` | Strict JSON parse (strips md fences). Now only the FALLBACK path in `runPackager` (structured-outputs replies are parsed directly, no fence/repair) |
| `contracts/v3NarrativeChecks.ts` (+`.test.ts`) 🆕 | `validate_v3_grounding`, `v3SurfacedStrings` | **ÉTAPE 2 du chantier Explorer (R4-4, 08/08) — la porte de grounding des chemins `v3_narrative`** (mois/compare/mobilité/entité in-data), jusqu'ici shape-check seul : nombres surfacés (answer POLYMORPHE scanné champ par champ, caveats inclus) vs le payload allowlisté + entités vs le payload, REJECT jamais warn — le repli déterministe existant (windowTopDays/worstDays/compare) reste le plancher. LA dérogation (somme/écart exact de deux nombres de même unité) est RESSERRÉE localement : opérandes à valeurs DISTINCTES obligatoires — sur un payload entier, « 20 » était trivialement « 10+10 » de deux quantièmes (lie-bait attrapé au 1er run) ; la primitive partagée `reproducibleSumDiff` reste INTOUCHÉE (sa suite garde le seam grounded). Branché dans le validatorFn v3 inline de `runtime/runPackager.ts` après les shape-checks. Lie-bait 8 cas même commit (47/47 total) ; E2E : q8 mois + q9 compare passent attempt-1, latence inchangée (15 s / 9 s) | consommé par `runtime/runPackager.ts` |
| `runtime/stageEmitter.ts` 🆕 | `runWithStageEmitter`, `emitStage`, `StageEmit`, `StageState` | **Phase 5 — the ONE stage-event channel** between the pipeline and a streaming response. AsyncLocalStorage (Node built-in): race-free under concurrency, propagates through await chains so `runPackager` emits generate/verify without threading a callback through 5,000 lines. **No store set (non-stream, briefing cron, tests) → no-op — the JSON path is byte-identical by construction.** Structural truth rule: payload = key + state + numeric/flag extras; NO field for model text. Labels resolved at the wire edge from `contextCopy.STAGE_FR` |
| `eventTypes.ts` 🆕 (lib) | `EVENT_TYPES_GENERIC`, `eventTypesFor(industryCode)`, `eventTypeLabelFr(value)`, `EventTypeOption` | **Registre UNIQUE des types d'événement par MÉTIER du lieu** (spec `docs/evenement-dossier-spec.md` § 4, owner 03/08). Clés = codes INDUSTRY_LABEL ; 5 familles curatées (bouche/retail/culture/accueil-loisirs/pro), « autre » toujours en fin ; les 12 valeurs HISTORIQUES de days.astro préservées (des saved_items les portent) et servies en repli quand le métier est inconnu ; `eventTypeLabelFr` = le libellé pour TOUS les rendus (remplacera EVT_TYPE_LABELS de monitor, legacy) ; valeur inconnue → passthrough lisible, jamais inventé | — |
| `eventLifecycleCards.ts` 🆕 (lib) | `buildEventLifecycleCards(bq, location_id, clerk_user_id, today)` | **Cartes de cycle de vie des événements** (spec evenement-dossier § 5) — générateur ADDITIF consommé par `insight/monitor.ts` (8e entrée du Promise.all, concat aux candidates ; échec soft → [], ne casse JAMAIS pulse). 4 types : `event_threat` (occurrence ≤7 j × lvl_* ≥3 × nature extérieure — mêmes seuils que l'état Avant), `event_decision_due` (decision_date cochée ≤3 j sans choix), `event_prepare` (J-1, météo de demain), `event_measure` (J+1, écart € residual jamais recalculé ; ventes absentes → branche honnête). `date`=aujourd'hui (le jour d'AGIR), occurrence au payload, suppression_key `<type>:<loc>:<occurrence>` STABLE (reprise M'engager supprime pour de bon). Diff prouvé 03/08 : muse byte-identique, café +2/0 retrait | `raw.saved_items` × `raw.saved_item_dates`, `semantic.vw_insight_event_day_surface`, `mart.fct_client_day_residual` |
| `eventOccurrences.ts` 🆕 (lib) | `generateOccurrences({recurrence, dow, start, end, cap})`, `RecurrenceArgs` | **Génération PURE des occurrences d'un événement récurrent** (spec evenement-dossier § 1.2, owner 03/08 : plafond 52, « Du X au Y », hebdo par dow / mensuel au jour du mois — mois sans ce jour SAUTÉS, jamais approximés ; entrées invalides → []). Jamais l'horloge en entrée. Testé (5 cas, vitest colocalisé). Consommé par saved-items/create ; le cron J-7 le réutilisera | — |
| `profileLabels.ts` 🆕 (lib) | `frActivity`, `frAudience`, `frVenueType`, `frEventType`, `frObjective`, `frSeasonality`, `frWeatherSensitivity`, `frRegime`, `AUDIENCE_FR`, `VENUE_TYPE_FR`, `EVENT_TYPE_FR`, `OBJECTIVE_FR`, `SEASONALITY_FR`, `WEATHER_SENSITIVITY_FR`, `REGIME_FR`, `PROFILE_AUDIENCE_OPTIONS` | **The ONE enum→French map for the declared profile.** The profile fields are MACHINE enums (`live_event`, `mixed`, `local`, `professionals`); injecting them raw into a French prompt is how `live_event` was read as "lieu de spectacle" — same class as printing "alerte niveau 3". Nothing is authored here: activity reuses `competitive/constants.INDUSTRY_LABEL`, audience is days.astro's `AUDIENCE_FR` verbatim, venue mirrors profile.astro (`mixed` → "intérieur et extérieur", the descriptive register a sentence needs). Unknown enum → passthrough, never an invented label. Used by `insight/prompt.ts` (3 competitor prompts + the declared-activity fallback), `insight/generate-sowhat.ts`, and `admin/invite.ts` + `admin.astro` (C2 : PROFILE_AUDIENCE_OPTIONS = the 6 pickable audiences, verbatim from profile.astro's AUDIENCE_OPTIONS) | — |
| `onboardingClaim.ts` 🆕 (lib) | `claimProvisionedProfile` | **C3 (19/08)** — réclamation du profil pré-provisionné au premier login : bascule la ligne « invite:<uuid> » vers le vrai clerk_user_id (formats gardés des deux côtés, idempotent, vérité relue). Bascule AUSSI raw.watched_competitors + raw.competitor_tracking (veille posée par l'admin avant l'inscription, C4 — non fatal par table). Appelée par profile.astro quand un compte SANS ligne porte publicMetadata.provision_key ; harnais scripts/invite-verify.mts --send | `raw.insight_event_user_location_profile` (UPDATE) |
| `safeMarkdown.ts` 🆕 | `mdInlineToSafeHtml`, `escapeHtmlSafe` | **Phase 3 — server-side safe-markdown renderer.** Used where the SERVER prints model prose into HTML (`cron/daily-briefing.ts` headline/answer/key_facts, `cron/alerts.ts` narrative) — without it operators get literal `**asterisks**` in their inbox. SAFETY MODEL = the ordering: escape FIRST (model output inert), then re-introduce ONLY the whitelist (`**gras**`→`<strong>`, `*italique*`→`<em>`). No block tags, no links (nothing validates a model-emitted URL), no sanitizer dep. **THREE copies of the whitelist exist — keep them in sync** (not shared: static browser scripts can't import TS): (1) this file (server emails); (2) `card-kit.js` `mdInlineKit`/`mdBlockToSafeHtml` — the CHAT answer renderer since Phase 3 (block-level: adds `- ` bullets + paragraphs); (3) `ie-prompt.js` `mdInlineToSafeHtml` — residual, follow-rows only. Same escape-first ordering everywhere |
| `runtime/runPackager.ts` | `runAIPackagerClaude`, `Mode`, `MonthSubMode` | Orchestrator: mode → prompt + **schema** → Claude → validator. Each mode binds a `packagerSchemas` schema (structured outputs); when the transport reports `structured`, the reply is parsed DIRECTLY — the fence-strip + char-by-char re-escaper runs only on the fallback path (v3_narrative, unsupported models). **Phase 5: emits the REAL generate/verify stage boundaries via `stageEmitter` (no-op unless a streaming request set the ALS store); `verify done` fires ONLY on a validator PASS and carries `facts_cited` — a reject falls through to regen/floor with the verify row still waiting.** **`grounded_day` mode (Phase 2)** bypasses the field-allowlist (payload is brain-curated) + enforces the grounding validator; surfaces `usage` (cache) in debug |

### Prompts & validators (`contracts/`)

| Module | Purpose |
|---|---|
| `packagePromptV3.ts` | V3 narrative system prompt (window_top_days / compare_dates / day_why) |
| `packagerGroundedDayPrompt.ts` | **Grounded day-horizon prompt (Phase 2, VERDICT-FIRST)** — cites ONLY the brain's `citable_facts`; LEADS with a one-sentence verdict (état du jour + raison dominante), then the 2-3 facts that carry it, ranked by salience. NO action/advice (the real fired action card is attached in code). No invented numbers/entities/causes; honest-absence. **Phase 3 — formatting UNBANNED in string values: **gras** on the element that matters, *italique*, `- ` bullets (the client renders a safe whitelist); #titres/tables/links/HTML stay banned (render as inert text). "JSON strict" now scopes the ENVELOPE only.** **Règle 2ter NOMS PROPRES VERBATIM (07/08)** : chaque nom propre recopié EXACTEMENT sous la forme du fait cité (jamais raccourci/paraphrasé) — cause mesurée du reject systématique attempt-1 (5/5 sur « Pourquoi le 18/07 ? » : le modèle écrivait « Musée Guimet » pour « Musée national des arts asiatiques - Guimet » → reject entité par phrase → +15 s de regen par réponse, et plancher sur double-reject). Vérifié : reject_first disparu sur le run owner, smoke exec-summary family-report pass 1er essai. **R2-2 (07/08) — VERDICTS MESURÉS dans les réponses jour** : `competitorImpactFacts` + `eventDensityImpactFacts` (exports facts-only des familles competitor/events — MÊME mesure et MÊME phrasé que les cartes, contrastes observed_difference seulement, cold-start écarté) rejoignent la liste blanche des réponses jour NON-family-led, amorcés dans le LOT PARALLÈLE existant (coût = max, pas somme — 21 s inchangé) ; origins concurrence/evenements_proximite. Motif : la réponse jour racontait la proximité statique alors que l'entrepôt porte un verdict mesuré signé (f10c3e58 : activité des suivis = CA au-dessus de la normale). + matchers competitor élargis aux « ils me prennent des clients ? » GÉNÉRIQUES (verbe au PLURIEL uniquement — « le musée d'Orsay me prend-il » reste ENTITY_IMPACT/web ; 7 assertions de routage) ; E2E : « Les musées autour me prennent-ils des clients ? » → family_grounded_claude, verdict mesuré +17,2 pp au-dessus, 19 s (avant : web_search inventant Marmottan/Palais de Tokyo). **ÉTAPE 3 (planificateur, 08/08) : `familiesForQuestion` (cap 3, ordre du registre — la 1re = l ancien gagnant unique, lead/carte inchangés, 8 assertions) → providers en PARALLÈLE ; les familles secondaires apportent leurs facts (found only, origin par famille) ; le BLANKING du CA en family-led remplacé par le RANG (famille d abord, puis day-perf — une réponse météo dit enfin ce que le jour a fait) ; matcher météo « fait fuir mes clients » ajouté. E2E pluie : « Oui : −7 % sur vos 6 jours de pluie (−76 €/j) », 17 s.** **R3-1 (08/08) : `dayEventLandscapeFacts` (events.ts) — les événements ACTIFS le jour demandé (mart conflicts, event_date..event_date_end, ≤ 5 km, dédup par LIEU — 3 variantes « Silla » mesurées —, top 3 par audience commune) en faits descriptifs avec leur base de classification (même catégorie/audience commune %) ; zéro ligne = formulation scopée « relevés » (l'absence de crawl ≠ absence d'événement) ; batch parallèle, origin evenements_proximite. + règle prompt 3ter-0 : repères permanents (concurrents à X km) JAMAIS cités dans une réponse jour sauf question dédiée — E2E 11/07 : le bullet musées-à-X-km a disparu, remplacé par l'effet mesuré +17,2 pp et l'expo active du jour (Guimet, 33 % d'audience commune).** **Règles d'ARGUMENTATION (07/08, retour owner « states facts, doesn't answer ») : 1ter repères divergents = LES DEUX au verdict (−12 % vs habituel MAIS +13 % vs samedis → « variation ordinaire ») ; 2quater intégrité des référentiels (jamais deux nombres de bases différentes composés en une comparaison) ; 3ter faits stables ≠ explication (concurrent à X km cité en repère, jamais en cadre du verdict ; sans fait tieré → « aucune cause mesurée — l'écart peut n'être que la variation ordinaire ») ; 3quater aucun qualificatif d'intensité inventé (« forte densité » sur 3 concurrents = interdit). Vérifié sur 18/07 + 11/07 : verdicts réconciliés, zéro qualificatif inventé, pass attempt-1 ~20 s. **R2-2 : la saillance place un fait TIERÉ qui éclaire la question AVANT tout repère stable (règle 2)** |
| `packagerGroundedDayValidator.ts` | **Grounding validator (Phase 2)** — HARD-rejects any surfaced number / named entity / causal construction / predicted outcome not tracing to the payload; never warn-and-pass. **Caveats are scanned too** (closed with Phase 1 #2 — they were the one unscanned surfaced field, and feedback regen made relocating rejected content there reachable; generic honest denials still pass — `groundedCaveats.test.ts`). **ONE carve-out (Phase 1 #4, bounded arithmetic)**: a payload-absent number passes iff the validator RECOMPUTES it as a same-unit sum/diff of two numbers from the CITED facts (`reproducibleSumDiff`); accepted derivations surface as warnings for observability; wrong totals / % / uncited operands reject as before. Lie-bait suite: `groundedArithmetic.test.ts`. **Tiered causal register (Phase 1 #5)**: causal ATTRIBUTION is legal only in a sentence whose output cites a `measured`/`observed_difference` fact AND that carries that fact's OWN tier token (`TIER_TOKEN_FR`) — wrong tier, tier in another sentence, or an untiered fact (competitor/driver/weather) all reject; PREDICTED OUTCOMES never unlock at any tier. Scan is per-SENTENCE (`splitSentences`), not whole-text. Lie-bait suite: `groundedCausalTier.test.ts`. **Per-sentence provenance (Phase 1 #6)**: `sentence_provenance` (schema-required) maps each surfaced claim to its backing fact_ids; entity grounding is now LOCAL — an entity must appear in the fact ITS sentence cites, not merely somewhere in the payload (catches "real in f7, surfaced citing f2"), an entity with no covering provenance entry is an undeclared claim → reject, an unknown provenance fact_id → reject. `answer`/`key_facts` stay plain strings (UI untouched). Absent provenance (v3 / older payloads) → the global scan, never weaker. Lie-bait suite: `groundedProvenance.test.ts` |
| `groundingChecks.ts` | `extractNumbers`, `extractNumbersWithUnits`, `reproducibleSumDiff`, `splitSentences`, `norm`, `makeEntityRegex`, `extractNamedEntities`, `CAUSAL_ATTRIBUTION_PATTERNS`, `PREDICTED_OUTCOME_PATTERNS`, `CAUSAL_PATTERNS`, `DRAFT_OUTCOME_PATTERNS` | Shared anti-fabrication primitives for both grounded validators. **`extractNamedEntities` (Phase 1 #6)**: `makeEntityRegex` + leading-French-article strip + re-extract, so a capitalized sentence-initial article isn't glued onto the span ("L'écart de CA" → not an entity; "La Brasserie Lipp" → "Brasserie Lipp", grounded by its bare form). Day validator only — the DRAFT validator keeps its own `makeEntityRegex` use, unchanged. **Phase 1 #5 split**: `CAUSAL_ATTRIBUTION_PATTERNS` (past claims — the ONLY group the tier can unlock) vs `PREDICTED_OUTCOME_PATTERNS` (future promises — never unlockable). **`CAUSAL_PATTERNS` remains the exact union** so the DRAFT validator is unchanged (it scans both with no tier carve-out — draft copy is customer-facing). `splitSentences` = the per-sentence surface the tiered register needs. Numbers grounded by MAGNITUDE (normalizes −/–/— dashes; "−20 %" == "20 %"). **Bounded-arithmetic primitives (Phase 1 #4)**: `extractNumbersWithUnits` (unit = token after the number: €/%/°C/km/h/km/min/points) + `reproducibleSumDiff` (exact same-unit a+b / \|a−b\| recompute over cited-fact text; stated unit must match). `extractNumbers` untouched — the draft validator's behaviour is unchanged |
| `packagerGroundedDraftValidator.ts` | `validate_grounded_draft` | **Grounding validator for DRAFTS (Phase 3b)** — free-text channel copy; rejects a fabricated external fact (competitor/event/weather/attendance/outcome). FOUR legal sources: citable_facts + venue identity + user instruction + cardFacts (the card's own facts, 18/07); entities matched PER LINE, ALL-CAPS runs skipped (headings/salutations are style), token-fallback for reformulated legal entities (an invented proper noun still introduces an unknown token → reject); signal array LENGTHS enter the legal number pool (faithful counts); creative voice/CTA free |
| `packagerMonthPrompt.ts` | Month orchestration prompt (window_summary vs special_days) |
| `packagerMonthSpecialDaysPrompt.ts` | Special-days narration prompt |
| `packagerMonthWindowSummaryPrompt.ts` | Month window-summary prompt |
| `packagerMonthWindowWorstDaysPrompt.ts` | Worst-days-in-window prompt |
| `packagerUiV2Prompt.ts` / `packagerUiV2Validator.ts` | UI V2 FR prompt + output validator |
| `packagerUiV3Prompt.ts` | **Empty stub (0 lines)** — placeholder, not wired |
| `packagerUiV3Validator.ts` | Validates `UiPackagingV3` contract (v=3, header, dates, sections) |
| `packagerValidators.ts` | Month narration validators (orchestrator / window_summary / special_days) |
| `packagerSchemas.ts` 🆕 | `SCHEMA_GROUNDED_DAY`, `SCHEMA_UI_V2`, `SCHEMA_MONTH_ORCHESTRATOR`, `SCHEMA_MONTH_WINDOW_SUMMARY`, `SCHEMA_MONTH_SPECIAL_DAYS` | **JSON Schemas for structured outputs (the SHAPE half; sibling of `packagerValidators`).** One per mode, keyed `additionalProperties:false` to mirror each validator's `hasOnlyKeys`. Retires the fence-strip/repair parser — NOT the validators: the dialect has no minItems/maxLength, so array COUNTS + string LENGTHS + all grounding stay validator-enforced. No schema for `v3_narrative` (polymorphic `answer`) — it keeps the parser path. **`SCHEMA_GROUNDED_DAY` gained `sentence_provenance` (required, Phase 1 #6)** — per-claim `{text, fact_ids}` the validator checks entities against locally |
| `uiPackagingV3.ts`, `uiPackagingV3Types.ts` | V3 UI packaging types (header, dates, sections, evidence, lookup) |

### Deterministic engines (`decision/`, `facts/`, `find_dates/`)

| Module | Key exports | Purpose |
|---|---|---|
| `decision/decision_signals.v1.ts` | `DecisionSignalsV1`, `WeatherSignalV1`, `CompetitionSignalV1`, `CalendarSignalV1` | Decision-signal types by dimension |
| `decision/day_why/day_why_v1.ts` | `renderDayWhyV1`, `DayWhyV1Input` | Deterministic day narrative: regime, competition, weather, actions |
| `decision/engines/compare_dates.ts` | `compareDatesDeterministicV1` | Rank dates by regime/score/weather/competition |
| `decision/top_days/window_top_days.ts` | `windowTopDaysDeterministic` | Top-3 dates: facts + line_items + coverage **Seul consommateur : `api/insight/prompt.ts` (~5788), le REPLI du chat Consulter — la branche `catch` quand le packager Claude jette, pour les intentions `WINDOW_TOP_DAYS` / `WINDOW_WORST_DAYS`.** Il n'en lit que `headline` / `summary` / `key_facts` / `caveat` : **`v1` (facts_by_date + coverage) est calculé puis JETÉ**. C'est là que vit le calendrier depuis `7b206e6` (26/05) — la phrase « Calendrier : … » a quitté `key_facts` pour un fait PAR DATE dans `v1`, plus riche, que personne ne consomme. Migration à moitié faite ; brancher `v1` dans le repli est un chantier ouvert, non décidé. Concurrence lue sur **500 m / 1 km / 5 km** depuis le même commit (`events_within_10km_count` existe pourtant toujours dans la vue, peuplé — c'était un choix, pas une contrainte). Lignes du shortlist : `semantic.vw_insight_event_30d_day_surface` (`SELECT *`) |
| `decision/worst_days/window_worst_days.ts` | `windowWorstDaysDeterministic` | Worst-3 dates: facts + line_items + coverage **Seul consommateur : `api/insight/prompt.ts` (~5788), le REPLI du chat Consulter — la branche `catch` quand le packager Claude jette, pour les intentions `WINDOW_TOP_DAYS` / `WINDOW_WORST_DAYS`.** Il n'en lit que `headline` / `summary` / `key_facts` / `caveat` : **`v1` (facts_by_date + coverage) est calculé puis JETÉ**. C'est là que vit le calendrier depuis `7b206e6` (26/05) — la phrase « Calendrier : … » a quitté `key_facts` pour un fait PAR DATE dans `v1`, plus riche, que personne ne consomme. Migration à moitié faite ; brancher `v1` dans le repli est un chantier ouvert, non décidé. Concurrence lue sur **500 m / 1 km / 5 km** depuis le même commit (`events_within_10km_count` existe pourtant toujours dans la vue, peuplé — c'était un choix, pas une contrainte). Lignes du shortlist : `semantic.vw_insight_event_30d_day_surface` (`SELECT *`) |
| `decision/top_days/window_top_days.test.ts` (test) | 10 tests | Garde le contrat du repli déterministe. **Rouge du 26/05 au 31/07** : `7b206e6` (fourre-tout, 26 fichiers) avait basculé la concurrence 10 km → 500 m/1 km/5 km et sorti le fait calendrier de `key_facts`. Rejoué le 31/07 — module+test du 17/02 : **10/10** ; module d'aujourd'hui : 2 échecs. Le test avait donc ATTRAPÉ un vrai changement et a été laissé rouge deux mois. Recalé le 31/07 sur la couche où le calendrier vit désormais (`v1.coverage` + `v1.facts_by_date`), intention inchangée ; libellés et statuts relevés en EXÉCUTANT le module. Vérifié par mutation (fait calendrier retiré → 1 échec ; `getC5km` cassé → 2 ; `dim: "calendar"` retirée → 1). Cible : `window_top_days.ts` |
| `decision/worst_days/window_worst_days.test.ts` (test) | 10 tests | Jumeau du précédent, même histoire et même recalage. Cible : `window_worst_days.ts` |
| `facts/buildCoverageFromRows.v1.ts` | `buildCoverageFromRowsV1` | Coverage builder (field presence by dimension) |
| `facts/buildFactsFromSelectedDaysRow.v1.ts` | `buildFactsFromSelectedDaysRowV1` | Fact builder from selected-days row |
| `corrections.ts` 🆕 | `getActiveCorrections`, `appendCorrectionEvent`, `correctionsBrief`, `captureCorrectionFromTurn`, `looksLikeCorrection`, `CorrectionType` | **Phase 2.3 — persistent user corrections.** **Hybrid capture (increment 2): `looksLikeCorrection` regex gate → Haiku (`modelFor("classifier")`) extracts `{is_correction,type,fact}` → appends assert/supersede. `prompt.ts` kicks it off in PARALLEL early + awaits before the brief reads corrections (so a this-turn correction persists + reflects immediately); never throws, never blocks.** Append-only event log `analytics.consulter_correction_events` (assert/supersede/clear; latest-active per (location_id, correction_type) = current truth; full history = learning corpus for a downstream dbt layer). App owns write (append) + read (latest-active); never overwrites/deletes. `correctionsBrief` formats active corrections as the TOP identity-brief lines (above measured sales). Read failure is swallowed (never breaks the answer). Consumed by `prompt.ts` businessBrief. Seeded-proof on f10c3e58 (librairie overrides measured café, acknowledged, then cleared) |
| `facts/buildIdentityFacts.ts` 🆕 | `buildIdentityFacts`, `assembleIdentityFacts`, `IdentityFact`, `IdentityFactsResult` | **Phase 1 — measured customer-identity facts.** `claim_type:"measured"` facts for what a venue SELLS (mix), TOP ITEMS, and SCALE (basket/daily CA), each with a deterministic trust tier folded into `fact_fr`. Offering view → mix (revenue_share, self-normalizing); performance view → scale — NEVER crossed (offering's absolute revenue doesn't reconcile). Windows anchor on the data's own dates (seeds are future-dated); perf DATE bounds inlined as literals (Node client silent-0-rows on DATE param mismatch). Below the reliability floor → `{status:"insufficient"}` so the caller ELICITS vs bluffs. Reads `semantic.vw_insight_event_client_offering`, `…_client_performance`. **Grounded-day phrasing: the MIX fact leads with ONE category share + ranks the rest by NAME — multiple share %s make the packager sum arbitrary subsets (a derived, ungrounded number the validator rejects despite the prompt's no-arithmetic rule). Full per-category shares stay in `categories` for the no-validator paths (discovery/reports).** Consumed-by: `prompt.ts` grounded-day answer + `daily-briefing.ts` Point du jour (increment 2, via `extraFacts`); drafts EXCLUDED (customer-facing, would leak operator intel). Discovery/reports = increments 3-4 |
| `facts/buildDayPerformanceFacts.ts` 🆕 | `buildDayPerformanceFacts`, `DayPerfFact` | **Phase 4 (16/07) — day-performance facts: the day answer says how the day WENT, not just its context.** PAST measured day → `CA réalisé vs CA habituel` (day_residual, dow+trend), vs ANALOGS (`fct_client_day_analogs`, gated `analog_n ≥ 5`; the POOL leads the comparison — « vs vos 12 derniers samedis (13 semaines d'historique mesuré) » — owner remark 16/07: a median without its measurement base reads as hand-waving, and a trailing clause gets paraphrased away; prompt rule 2bis BASE DE MESURE backs it), + component movers (`*_delta_pct` from sales_signals_daily — states ONLY measured components, a NULL delta is silence, never « stable »; material at |Δ| ≥ 5 %). TODAY/FUTURE day → same-dow `CA habituel` (mean of last 10 same-dow measured days, min 3) + latest measured day's performance. All numbers INSIDE fact strings (validator-whitelisted); `observed_difference`/`observed`, tierless, comparative phrasing — never causal. Cold account → 0 facts, answer unchanged. Consumed-by: `prompt.ts` grounded-day `extraFacts` (NON-family-led only — a family-led answer leads with its own dimension); prompt rule 1bis (PERFORMANCE D'ABORD) makes the verdict lead with it | `mart.fct_client_day_residual`, `…day_analogs`, `…sales_signals_daily` |
| `find_dates/find-dates.ts` | `findDates`, `FindDatesParams`, `FindDatesResult` | BQ query + Claude narrative w/ scoring + quality floor |

### Impact narration (`impact_narrations/`, `impact_rules/`)

| Module | Key exports | Purpose |
|---|---|---|
| `impact_narrations/compile_month_impact_narration_v1.ts` | `compileMonthImpactNarrationV1` | Map decision signals → implications via assertion assets |
| `impact_narrations/evaluate_impact_blocks_v1.ts` | `evaluateImpactBlocksV1` | Signals → FiredBlocks (weather/calendar/competition) |
| `impact_narrations/impact_assertion_asset.v1.ts` | `IMPACT_ASSERTION_ASSETS_V1` | Truth-asset library (3 weather, 14 calendar, 8 competition) |
| `impact_narrations/impact_mapping_contract_v1.ts` | `FiredBlock`, `ImpactDimensionMappingV1` | Signal→block mapping contract |
| `impact_narrations/impact_mappings_v1.ts` | `WEATHER_MAPPING_V1`, `CALENDAR_MAPPING_V1`, `COMPETITION_MAPPING_V1` | rule_id → fired-block mappings (no text) |
| `impact_narrations/impact_narration_fr.ts` | `toImpactNarrationV1Fr` | EN tag → FR translation |
| `impact_narrations/impact_narration_v1.ts` | `ImpactNarrationV1`, `ImpactNarrationMasterV1` | Narration contract types by dimension |
| `impact_rules/v1_5/*` | `competition.rules`, `seasonality.rules`, `weather.rules`, `impact_rule_asset` | v1.5 impact rule assets per dimension |

### Rendering & UI build

| Module | Key exports | Purpose |
|---|---|---|
| ~~`points_cles/points_cles_v1.ts`~~ | — | **SUPPRIMÉ le 31/07/2026.** Rendu déterministe des « Points clés » de la vue jours. Audit avant suppression : son unique consommateur, `api/insight/days.ts`, l'appelait pour CHAQUE jour puis exposait le texte deux fois (`day.points_cles` + champ racine) — et `days.astro` l'affectait à `MS.pointsCles` **sans jamais le relire**. Le bloc « Pourquoi ces dates ? » de cette page vient d'ailleurs (`renderCriteriaBlock(dayRowsByDate)`, 0 occurrence de `points_cles` dans son corps) ; l'`ai_points_cles_text` de `month.astro` vient de `month.ts` (sortie LLM), sans rapport. Aucun accès dynamique possible (ni `JSON.stringify(day)`, ni `Object.keys`, ni clé calculée). Son test vivait sur un contrat disparu le 16/03 et était rouge depuis. **À ne pas confondre avec `render/shared/render_points_cles_v1.ts`**, autre fonction du même nom, VIVANTE (plancher grounded du chat Consulter) et toujours sans test |
| `render/renderLineItemsFr.v1.ts` | `renderLineItemsFrV1` | V1 IR → French text via template dispatch |
| `render/shared/render_points_cles_v1.ts` | `renderPointsClesV1` | Shared de-dup + ordering (action lines last) |
| `ui/types.ts` | `PackagerOutput`, `PackagerResult`, `SelectedDaysPackagerResult` | Packager output types |
| `ui_normalized/ui_normalized_v2.ts` | `buildUiNormalizedV2`, `normalizeWindowTopDayRow` | Normalize raw rows, month filters, per-date facts |
| `ui_packaging_v3/buildUiPackagingV3Month.ts` | `buildUiPackagingV3Month` | Deterministic month packager (truth-only sections). Weather facts NAME their nature via `contextCopy.weatherNatureFr` (`lvl_*`) + the warehouse's `weather_label_fr` — never a raw WMO code or "niveau N"; reads `vw_insight_event_30d_day_surface` (via `prompt.ts` `SELECT *`) |

> `src/components/ai/ir/lookup_ir_v1.ts` mirrors/consumes these on the component side (importé par `api/insight/prompt.ts`). **31/07** : `src/components/ai/render/shared/render_points_cles_v1.ts` SUPPRIMÉ — copie orpheline, octet pour octet identique à `src/lib/ai/render/shared/render_points_cles_v1.ts` et importée par personne (l'import de `day_why_v1.ts` résout vers `lib/`, pas vers `components/`). Il restait DEUX autres fonctions du même nom, à ne pas confondre : `lib/ai/render/shared/…` (127 l., 22/02) sert le plancher grounded du chat Consulter via `day_why_v1` → `prompt.ts:4513` — VIVANTE et SANS TEST ; `lib/ai/points_cles/points_cles_v1.ts` (328 l., réécrite le 16/03) sert `api/insight/days.ts` → `days.astro`, où `MS.pointsCles` est affecté puis **jamais relu** (sortie calculée et jetée à chaque chargement).

---

## Scripts — `src/scripts/`

| Script | Purpose | Data source |
|---|---|---|
| `build-designmatrix.cjs` | Type B COMMITTED design-matrix build: residual × context → `b_real_designmatrix` (the engine's fit source). Features driven by the single-source registry `src/lib/sensitivityFeatures.json`; `is_oos` = most-recent 20%/venue. `--dry <table>` writes a scratch copy | `mart.fct_client_day_residual` + `mart.fct_location_context_daily` + `src/lib/sensitivityFeatures.json` (in) → `analytics.b_real_designmatrix` (out) |
| `sensitivity-engine.cjs` | Type B offline engine: OLS+BQML fit, VIF pre-check, per-venue contrast + consistency gates, BH FDR, tier ladders, pooled lean. REAL factor set/mechanism gate = `src/lib/sensitivityFeatures.json` (single source) | `analytics.b_v1_seed` / `b_real_designmatrix` + `src/lib/sensitivityFeatures.json` (in) → `b_sensitivity_store` / `_seedtest` (out) |
| `crawl-best-in-class.mjs` | Offline crawl for the "lieux comparables" advice slot (ESM; shares the crawl contract `src/lib/bestInClassCrawlCore.mjs` with the demand-drain cron): per (industry × lever × **intent**: pivot/reinforce/scale) web-searches reputable sources for a real case study (comparable venue applied move X → measured outcome Y), validates (drops no-source / no-outcome), loads `analytics.best_in_class_plays`. Names the venue only when the source does; external web only, never another user's data. **`MODE=refresh` = DEMAND-DRIVEN (not calendar)**: crawls only verticals with ACTIVE commitments, only cells missing or older than `TTL_DAYS` (90) — usually a no-op; supersedes just the crawled cells (WRITE_APPEND). `MODE=full` = whole rebuild (WRITE_TRUNCATE). `INDUSTRIES=`/`LEVERS=`/`INTENTS=` subset ; `CONCURRENCY=` pool de workers (défaut 6, cap 10) | Anthropic `web_search_20250305` (needs ANTHROPIC_API_KEY); `refresh` reads `analytics.action_commitments` + `semantic.vw_insight_event_ai_location_context` (active verticals) → `analytics.best_in_class_plays` (out); read via `bestInClassStore` |
| `ai_runtime_smoke.mjs` | Smoke test for AI packager runtime (`runAIPackagerClaude`) | needs ANTHROPIC_API_KEY |
| `print_env.mjs` | Diagnostic: prints ANTHROPIC_API_KEY prefix/len + CLAUDE_MODEL | none |
| `prompt.ts` | Client-side in-page AI prompt rendering (calls `/api/insight/prompt`) | none |
| `days.ts` | **Empty stub** | — |
| `month.ts` | **Empty stub** | — |

---

## Frontend surfaces

### App pages (`src/pages/app/`) — Clerk-gated

`insightevent/` route group: `pulse.astro`, `tableau.astro` (🆕 04/08 — Tableau de bord, atterrissage Piloter ; Nav/BottomBar y pointent, la liste vit derrière « Tous mes événements »), `evenement.astro` (🆕 03/08 — LISTE « Mes événements » par défaut (04/08, vw_insight_event_user_events), création MSEventForm `?new=1|?dates=`, dossier 3 états `?saved_item_id=`, bloc bilan déclaratif 3 questions `?mode=bilan`), `insight.astro`, `days.astro` (**04/08 : liste « Mes dates » et onglet CHOIX démis** — sans sélection → redirige vers la liste /evenement ; `?saved_item_id=` → redirige vers le dossier ; reste la surface de DÉTAIL jour par jour, 7 entrées câblées), `month.astro` (**04/08 : hors du flux de création** — « Créer l'événement avec ces jours → » envoie la sélection au formulaire `?dates=`, plus jamais à la comparaison days), `map.astro`, `prompt.astro`, `engagement.astro`, `competitor.astro`, `suivis.astro`, `reactions.astro`, `rapport.astro`. **`monitor.astro` et `events.astro` SUPPRIMÉES (04/08, GO owner — bascule complète : liste + bilan au dossier + tous liens repointés ; l'API `insight/monitor.ts` reste, pulse en dépend).** Plus `admin/admin.astro`, `index.astro`, and `insightmarketing/prompt.astro`. **R8-nav (08/08, décision owner) : la rangée NAVIGATION d'InsightBottomBar (Explorer / Mes dates — dupliquait la nav haute) SUPPRIMÉE ; la rangée d'ACTIONS (Partager choix / Enregistrer, showActions — days.astro) RESTE, seule porteuse du chrome (fond+liseré ; wrapper display:none sans elle via :has) ; offsets --ie-bottom-nav-h → 0px sur prompt+month (l'input du chat descend au vrai bas ; la barre de sélection du mois --ie-30d-selectedbar-h intacte) ; « Mois »/« Mes dates » n'ont plus de lien persistant mobile — entrées contextuelles seulement (CTA « Ouvrir le mois » du chat, finder, flux événement), accepté owner ; PiloterBottomBar SUPPRIMÉE AUSSI (08/08, 2e passe owner : « Piloter et Agir ont encore un footer ») — pure nav (Piloter/Agir/Explorer), retirée des 5 pages (pulse, tableau, insight, evenement, competitor), composant conservé dormant ; zéro référence orpheline (ms-bottombar / --ie-bottom-nav-h absents de ces pages). Explorer : respiration de l'input du chat = padding-bottom 16px + env(safe-area-inset-bottom) (l'input touchait le bord après la suppression de la barre) — le padding du fil s'auto-guérit via syncInputWrapHeight (var mesurée). Menu Agir de pulse (03/08, owner) : UN gabarit à 3 entrées partout — [M'engager | Enrichir vos bonnes pratiques] · Consulter la source · Communiquer en interne. « Proposer une offre » et « Créer une notification » sont des SOUS-MODES du workspace Communiquer (`wsModeBar` : Message / Proposer une offre (opportunité, `data-agir-offer-channel`) / Notification auto (types `MS_INTERNAL_ALERT_TYPES`, `data-agir-notify-el`) ; `openAlertPanelCore(cardIdx, btn, panelMode, container)` remplace openAlertPanel ; `offerConfigHtml()` = l'invite de complétion de profil du 26/07, conservée en sous-mode ; le handler `data-agir-offer-config` est supprimé). La divergence « monitor garde l'ancien menu » est éteinte avec la page (04/08).**

> Per CLAUDE.md: `public/action-cards.js` is browser-cached by `?v=` — bump the cache-buster on the consuming surface (pulse ~348, monitor ~285, rapport ~47, insight ~158 — currently `?v=27`) and hard-refresh after edits. Astro dev does **not** reliably hot-reload API-route `.ts` — restart the dev server after server-side edits.

### Static client scripts (`public/`) — served fresh, no build/HMR, cache-busted by `?v=`

| Script | Purpose | Calls |
|---|---|---|
| `action-cards.js` | **Cartes de faits en EUROS (23/08 v2, `?v=72`, owner : bascule part → euros)** : `hour_share_move` / `item_share_move` / `offering_mix_shift` lisent `delta_eur` = réel − (part typique × attendu du jour par le moteur) ; coin « ce jour » via `corner_day_mode` (CARD_VALUE_TYPES) lu sur `delta_eur` dans pulse. **`competitor_hours_change` vivante (23/08, `?v=69`)** : `hoursDiffFr(old_value, new_value)` lit les periods GBP du payload (journal `int_competitor_snapshot_deltas`) et nomme le jour qui change ; ancien payload hash → phrase sans `? → ?`. **Perturbation NOMMÉE sur les 4 cartes mobilité + événement nommé en 1re phrase de `competition_proximity` + `distLabel` « 159 m » (23/08, `?v=68`)** : `namedDisruption(a)` lit le payload dbt `mobility_named` (titre IDFM, ligne, arrêt) et prime sur l'arrêt du site ; la surface coupe le corps à 2 phrases / 200 car. — tout fait nommé vit en PREMIÈRE phrase. **`hour_share_move` (23/08, `?v=66`)** : carte au grain HEURE, calquée sur `item_share_move` (mots de l'heure = surface footfall « le pic de 7h ») ; payload `fct_client_hourly_signals_daily` ; rail performance. **`item_share_move` + rail performance (23/08, `?v=63`)** : nouvelle carte au grain PRODUIT, calquée mot pour mot sur `offering_mix_shift` (surface approuvée, « catégorie » → « produit »), même contrat de payload (`fct_client_item_signals_daily`). Arrondi à 1 décimale FR sur le produit (5,5 % vs 2,8 %, +2,7 pts — l'entier écrasait ces parts), signe − typographique. Geste « À faire » et non « À surveiller » : le verbe est proscrit (règle 8), je ne propage pas une faute sur une carte neuve — **l'original sur `offering_mix_shift` reste, signalé, non demandé**. Les deux cartes rejoignent `MS_INTERNAL_ALERT_TYPES` (et `V1_ALERT_ACTION_TYPES` serveur, même liste) : cartes de performance first-party, elles surfacent sur AUJOURD'HUI, dernière anomalie par type — sinon elles n'apparaissaient que le jour exact de l'anomalie, passé. Rendu réel f10c3e58 : « Latte Rg représente 5,5 % de votre CA ce jour vs 2,8 % en moyenne (+2,7 pts). » **« Alerte alerte météo » (23/08, `?v=58`)** : `weather_hazard_onset` préfixait `hazardLabel(d)` par « Alerte », or le REPLI de ce helper est « alerte météo » — mot doublé sur **2 567 jours sur 8 453 en alerte (30 %)**, ceux sans aléa nommé. Bascule sur **`hazardPhrase(d)`**, qui existait déjà dans le fichier (`h === 'alerte météo' ? h : 'alerte ' + h`) et que 9 autres cartes emploient — rien d'inventé. 4 points d'appel : corps, instagram, site (qui doublait autrement : « Bannière alerte météo. alerte météo. ») et note interne. **RECTIFICATION du relevé** : les « . » orphelins signalés sur `weather_improved`/`weather_worsened` étaient des ARTEFACTS de l'échafaudage (rendu avec `d = {}`) — `weather_label_fr` n'est vide sur aucun des 14 573 jours. **`foreign_tourism_signal` LISAIT LE MAUVAIS CHAMP (22/08, `?v=57`)** : sa phrase venait de `countries_on_school_holiday` — en août, 23 pays européens tous en « Summer holidays », soit le « ⇒ c'est l'été » condamné par l'audit du 27/07, écrivable sans ouvrir le compte (test 11). Le câblage du 01/08 avait posé le chiffre RÉEL dans le même payload sans que personne le lise : `countries_named`, `share_total_pct`, `profile_reference_year`. Couverture mesurée : **108 tirs sur 128, 27 sites sur 32** (les 5 sans = PACA non publiée). La phrase n'est PAS inventée — elle reprend le `detail_fr` que le modèle dbt écrit déjà, réserve comprise (« Ce n'est pas une mesure de votre fréquentation »), simplement accentuée. Repli = la phrase de repli du modèle. Rendu réel : « Royaume-Uni 16 %, Suisse 11 %, Allemagne 11 %, Pays-Bas 5 % en vacances scolaires — leur poids dans les nuitées étrangères de votre région (INSEE 2025), soit 43 % cumulés. Ce n'est pas une mesure de votre fréquentation. » **HORAIRES DE LA CARTE, PAS D'AUJOURD'HUI (22/08, `?v=55`)** : `todayHours(p)` → `hoursForCard(p, a)` — lisait `new Date().getDay()` alors que les cartes portent `affected_date` jusqu'à J+14 (monitor.ts) ; un « Programme du week-end » rédigé un vendredi annonçait les horaires du vendredi. 19 points d'appel bascules, dont `weekend_opportunity`, `weekend_vacation_low_comp`, `top_day_approaching`, `weather_window`, `weather_improved`, `perfect_storm` — toutes prospectives. AUCUNE chaîne visible modifiée. Jour déclaré fermé (`"dim": null`) → `''`, comme l'absence d'horaires. **DÉFAUT PRÉEXISTANT SIGNALÉ, non traité** : les appelants écrivent « Horaires : » + valeur, donc le vide donne « Horaires : . » — déjà le cas pour les 15 sites sur 32 sans horaires déclarés ; le corriger réécrirait 19 chaînes visibles non demandées. **Cache-buster : 4 surfaces, pas 3** — `rapport.astro` charge aussi le fichier, en plus de pulse/insight/monitor. **`hoursFrag(p, a, label)` (22/08, `?v=56`)** : le libellé ne s'écrit plus sans sa valeur. Mesuré sur 449 tirs (90 j, parc réel, 14 types) — 335 n'avaient rien à annoncer (236 sites sans horaires déclarés, 99 jour déclaré fermé) et livraient « Horaires : . » dans un brouillon copié tel quel. 13 points d'appel conditionnés, 5 déjà gardés par `if (h)`. Mots INCHANGÉS quand la valeur existe (prouvé en rendant les brouillons réels via le profil `f10c3e58` chargé en base). Au passage « Vos horaires aujourd'hui » → « Vos horaires » sur `competitor_hours_change` : la lecture est datée, le mot ne pouvait plus être garanti. | **`monthly_sales_hole`/`monthly_sales_spike` (07/08 nuit, `?v=46`)** : grain MOIS par canal (C3, docs/monthly-sales-spec.md) — copie MM/AAAA + « Porté par : » (top_parties), vm sur les 2 pics réels (12/2025 44 225 €, 03/2026 81 582 €). **`weekly_sales_hole`/`weekly_sales_spike` (07/08 soir, `?v=45`)** : grain SEMAINE par canal (C2, docs/weekly-sales-spec.md) — sowhat depuis le payload mart (semaine du JJ/MM au JJ/MM, canal, CA fr-FR, médiane 6 sem, « Données jusqu'au »), vérifiés vm sur les lignes réelles du mart (pic 30/03 13 705 €, trou 17/11 874 €). **`client_dormant` (07/08, `?v=44`)** : première carte au grain CLIENT (C1, docs/client-patterns-spec.md) — spec SPECS complète (label « Client régulier sans commande », sowhat depuis le payload mart : cadence, silence, ratio, CA fr-FR, dates JJ/MM, « Données jusqu'au »), seeds note_interne+email, thème « ventes » du taxonomy client (parité recoThemeMap testée). Vérifiée par harnais Node vm sur les 4 payloads BQ réels. **`low_competition_window` recadrée (28/07, `?v=35`)** : la carte ne parle plus de « concurrence » — `competition_index_local` mesure la densité d'événements TOUS SECTEURS pondérée par la distance (`0,7 × (4×500m + 3×5km + 2×10km + 50km)`), pas la concurrence. Titre « Moins d'activité que d'habitude dans votre périmètre » ; corps = UN seul chiffre (écart en % sous la normale du lieu) au lieu d'un compte comparé à un indice ; direction (rapportent plus / moins / on ne sait pas) pilotée par `a.enjeu.eur_year` — jamais affirmée sans mesure. Supprime au passage un « Score N/10 » sur un score /100. **Unité `pct_same_sector` (28/07, `?v=33`)** : le payload dbt porte désormais un POURCENTAGE 0-100 tandis que la vue sémantique `d.pct_same_bucket_5km` reste un RATIO 0-1 — le helper `samePct(a,d)` porte seul la normalisation (payload prioritaire, vue ×100) et les 5 sites y passent (`same_bucket_saturation`, `ft_peak_saturated`, `saturated_bad_weather`). Corrige au passage un `Math.round()` sur chaîne et un `||` qui écrasait un vrai 0 %. Voir `docs/competition-split-spec.md`. **Description crawlée RETIRÉE des corps de carte (21/08, arbitrage owner)** : les 46 injections `userEdge(p)`/`crawledOffering(p)` dans les `sowhatFn` sont supprimées ; elle RESTE dans les 68 graines de brouillon (Communiquer), où elle sert à rédiger. Trois motifs, tous mesurés : (a) elle ÉVINÇAIT la mesure — le plafond de 200 c. était consommé par elle avant d'atteindre le fait mesuré (prouvé A/B sur `weekend_opportunity` et `competitor_threat_direct`, dont « Votre cible… » et « Même cible… » n'apparaissaient jamais) ; (b) l'exploitant connaît son offre — repère permanent, que `packagerGroundedDayPrompt` règle 3ter-0 interdit déjà de citer sur une carte du jour ; (c) non généralisable en version courte — 3 comptes seulement portent une description crawlée, de 127 à 279 caractères, sans structure commune. Effet mesuré : corps tronqués 6 → 1, aucun corps vidé. **`trunc()` coupe désormais au dernier ESPACE** (repli dur si le premier mot dépasse) et le plafond global de 200 passe par elle — fini « planification pour ident… » et « — r… » à la place de « risque de cannibalisation ». **`audience_overlap_pct` : `Math.round(×100)` retiré de 2 sites** (`competitor_threat_direct`, `competitor_audience_conflict`) — le champ EST un pourcentage 0-100 (vérifié en base, n=21), 3 autres lecteurs le lisaient déjà brut ; « 10000 % » → « 100 % ». **Identité de carte : payload d'abord, jamais `topComp(d)`** — les deux objets n'ont aucune clé commune (jour = `event_uid` sans id d'organisateur, payload = `competitor_id` sans event_uid) ; l'ancien ordre mélangeait les sources champ par champ et attribuait l'événement du jour au concurrent de la carte (mesuré sur Domaine de Poulvarel). **« Même cible » conditionnée** au recouvrement mesuré ≥ 40 % (REAL_BAR, `insightFamilies/competitor.ts:19`). **Single source of truth** for action-card branding, personalized sowhat, per-channel draft seeds, Agir dropdown. Does ALL rendering client-side from `day_surface` + `location_context`. **Site chip (19/07): candidate `location_label` resolved from pulse's multi-site map `window._engLocLabels` (filled at top-of-render, before renderDailyBrief) via the candidate's own `location_id` — rendered by the builder's EXISTING chip-n (pulse:~1792); single-site → empty map → no chip. `?v=30` on pulse/insight/monitor/rapport.** TODO (when rolling the card-specific pattern): the `sales_surge` sowhat infers the driver from `dominant_factor`+delta fallback; the **authoritative** field is `primary_revenue_driver` (conversion/footfall/basket) on `fct_client_sales_signals_daily` — repoint then. | (rendered from data passed in) |
| `scripts/explorer-quality-battery.ts` 🆕 | CLI (`npx dotenv -e .env -- npx tsx scripts/explorer-quality-battery.ts [port]`, `BATTERY_ONLY=n` debug) | **LA porte de QUALITÉ du chat Explorer** (R3-5, 08/08) — pendant de la lie-bait (vérité) : l'owner ne paie plus de son temps chaque régression d'utilité. 12 questions réelles vs le endpoint local (MS_AUTH_BYPASS) : portes DURES (producer attendu, budget latence, provenance+catalog sur grounded, jamais « catégorie C », réponse non vide) + JUGE Sonnet 5 sur la RUBRIQUE v1 (7 règles, chacune issue d'un retour owner daté ; **STATUT DRAFT — approbation owner en attente ; l'owner édite `RUBRIC_FR` dans le script**). Sortie : `explorer-battery-report.md` + baseline JSON (Δ par question au run suivant), gitignorés. Piège mesuré : Sonnet 5 pense implicitement — max_tokens 500 → texte VIDE ; 3000 requis. COUVERTURE PLAINTES (08/08, owner : « ça ne couvre pas tout ») : TABLE DE TRAÇABILITÉ plainte→porte en tête du script (la couverture se LIT, ne se croit pas) ; R9 VOIX (zéro remplissage/101/robotisme) ; 17 cas — + samedi-dernier (porte DATE calculée = samedi précédent), vacances scolaires (porte « vacances » dans la réponse), tourisme (porte % présent), continuation « et le dimanche ? » (porte used_date = lendemain du frame). SUITE CLIENT récurrente src/client/ (happy-dom sur les VRAIS card-kit+ie-prompt, 8 cas, 3 fichiers — UN eval/fichier, deux copies du module se disputent le DOM) : fil frais/périmé/Nouvelle conversation, 3 états de l'état vide, chips PROV/PROVFB byte-identique. Piège moteur mesuré : afterend multi-éléments INVERSÉ sous happy-dom → insertion unitaire en boucle inverse dans renderDynamicSuggestions (ordre re-prouvé navigateur). PRÉ-MERGE 08/08 : 14 cas (jour-inexpliqué avec porte WEB — web_context + ≥1 source https — et objection multi-tours), 14/14 portes dures, juge ~3,8 (baseline 3,51). Lie-bait WEB dédiée : contracts/webContextChecks.test.ts (card-kit exécuté en vm — http/javascript: sans lien, XSS inerte, boîte vide jamais rendue ; 6 cas, 53/53 total). Prompt entité/web : règle CHIFFRES DES PAGES (fréquentation/tarifs/horaires cités tels quels, qualificatifs vagues interdits si la page donne le nombre — Orsay R3 2,9→3,3). Baseline 08/08 : 12/12 portes dures, juge moyen 3,51/5 (pires : R1 premise/pluie, R4 mois sans événements nommés). **R4-3 : le juge note ce que L'UTILISATEUR voit — key_facts exclus sur grounded/family (non rendus par le client ; « répétition » artefact de juge mesurée au run 1), présence de la carte famille signalée** | `/api/insight/prompt` (local), Anthropic API |
| `scripts/e2e-onboarding-synth.ts` 🆕 | **E2E onboarding SYNTHÉTIQUE rejouable** — rejoue le parcours complet d'un compte neuf sur l'infra RÉELLE avec un tenant jetable (ids `SYNTH_*`, garde stricte) : profil cloné (adresse réelle) + les 4 jobs dbt du sign-up → import CSV frais 45 j (creux/pics calibrés portes 70/130) via le VRAI handler import/sales-csv (déclenchement after-upload PROUVÉ par la cause API) → vérités (marts signaux, cartes ventes, geste premier-test, zéro faux figé) → purge raw + rebuild (assertion 0 ligne). Modes : run complet (~12 min, poll dbt) / `--purge` idempotent. **Premier passage vert 06/08 — CHRONO MESURÉ : inscription → contexte géo 8,0 min · import → marts ventes 1,6 min · 17 cartes ventes produites (underperf×5, down_wow×5, cannibalization×4, surge×3)**. Piège attrapé à ce run : l'import écrit `client_id = clerk_user_id` (pas la location) — purge par location_id seul | dbt Cloud API v2, `raw.client_transactions`, `raw.insight_event_user_location_profile`, `dims.dim_client_location`, `mart.fct_client_sales_signals_daily`, `mart.fct_location_daily_action_candidates`, handlers import/sales-csv + insight/dashboard |
| `scripts/ie-prompt.js` (1188 ln) | Insight-event prompt UI harness. **`renderAiOutputHtml`: when the response carries `family_card`, renders the full family card INLINE via `MSCardKit.<render>({ok:true,...data})` before the intent branches** (footfall etc. — the detailed answer in-chat, no click). Needs `card-kit.js` loaded by `prompt.astro`. **Phase 2: `THREAD_CONTEXT.last.family` (from `meta.resolved_family`) rounds the conversation frame; `clarification.chips` render as tappable buttons (`.ie-clar-chip`, delegated click → `submitQuestion(chip.send)`)**. **Phase 3: `renderAiOutputHtml` is now an ADAPTER — `blocksFromResponse(out)` maps the envelope (per intent, content-parity: same splits/labels as the old six branches) to typed `blocks[]` → `MSCardKit.renderAnswerBlocks`. The register pill + clarification chips are BLOCKS (bubble bolt-ons deleted); the six divergent HTML branches + 3 regex-built HTML paths are gone from this file. Server unchanged (adapter retires path-by-path when the packager emits native blocks). Debug hook `window.__ieBlocksFromResponse` drives the harness.** **Staged inline loader (owner-approved proto)**: replaces the 140px GIF — text-height stage rows at the answer's insertion point (Routage → Lecture ventes → Contexte → Rédaction → Vérification); intermediate stages timer-paced (interim; Phase 5 inc-0 swaps to real SSE events, same visuals), the LAST stage only completed by real arrival — nothing claimed done before it is. User bubble = Direction A (neutral #F3F4F6, borderless, 17px, asymmetric radius — prompt.astro). **Phase 5 inc-①: sends `stream:true`; `readPromptStream` (fetch ReadableStream + frame parser — EventSource can't POST) consumes `stage` events → `makeStageEventHandler` switches the loader to SERVER-DRIVEN rows on the first event (timers cleared, rows upserted by key in arrival order — inc-②'s regen row can simply appear); `result {status,body}` → `resLike` → the verbatim error branching. Content-type sniff → JSON fallback (middleware-stripped streams degrade, never break). New submit aborts the in-flight stream (`__iePromptCtrl`); 90 s hard timeout. **inc-②**: done events may UPDATE a row's label (« — validée · n faits cités »); the `regen` row renders with the amber dot (#BA7517); the adapter passes `facts_cited` (from `ai.output.cited_fact_ids`) on the register block → card-kit `abRegister` extends the vetted pill « Vérifié · n faits cités » (C1, owner-approved; count only when real). Hook `__ieStageHandler` for the harness. **inc-② reveal**: `revealAnswerBlocks` — the ALREADY-validated answer's blocks arrive staggered (pill+headline instant, rest ~120 ms apart; no-op under prefers-reduced-motion; pure presentation, DOM complete from frame 1). Hook `__ieReveal`.** **Batch 1+2 (16/07): CTA suppressed on `ai.mode==='deterministic_honest_absence_v1'`; lookup headline deduped vs a single item's name; dedicated ELICIT branch (`deterministic_offering_elicit_v1` / `deterministic_missing_dimension_elicit_v1`) → `[headline, prose(asserts_nothing)]` (+ a `cta {action:'upload'}` block when `actions.primary.type==='upload_csv'` — delegated click on `data-ab-cta-action="upload"` opens the composer's OWN file picker `#ie-import-file-input`, one import flow) BEFORE the intent branches — the DAY_DIMENSION_DETAIL branch parses competitor-lines and silently DROPPED single-paragraph prose, which lost elicit answers entirely; family-led SEMANTIC dedupe (17/07, supersedes the exact-match rule): when a packager headline exists it IS the verdict — the card's `lead`/`contest_lead` are suppressed (chat-only; deep pages keep theirs); follow-rows hint « le plus proche d'abord » renders ONLY when >1 candidate carries a real distance.** **Polish (16/07): NATIVE BLOCKS — when `ai.output.blocks` is present (server-authored typed blocks; lookup path first) the adapter renders them verbatim (register still client-derived, server register blocks dropped — one pill; legacy answers keep the string parse); HEADLINE TYPE-OUT — the validated verdict types out word-by-word (~26 ms/word, plain-text headlines only, min-height reserved — no layout jump; markup headlines and prefers-reduced-motion keep the instant reveal); the honest cousin of token streaming (real streaming would show unvalidated text).** **Day 2 (16/07) — clickable décisions: `decorateCommitableDecisions` (hook `__ieDecorateCommits`) adds an « M'engager » button to every `.ms-decision-line` inside an inline family card (10 families); click opens the SHARED `MSCommitForm` (same form/POST as pulse/évolution — one commit flow) prefilled with the décision text; origin `chat_decision_<family>` + the card's date; success/error swap in place. No-ops without commit-form.js (prompt.astro now loads it). Cache-buster `?v=24`, card-kit `?v=34`, commit-form `?v=2`.** **R7 (08/08, proto approuvé owner) — ÉTAT VIDE 3 SLOTS : « Trouver une date » (inchangé) · UNE carte contextuelle (priorité anomalie du dernier jour mesuré |z|≥2 (monitor J−2..J+6, daily_revenue/revenue_robust_z) → alerte météo → repli météo ; sous-titre écrit depuis les données du compte, chiffre d'abord) · « Générer un rapport » (mois dernier). Clic = pré-remplit une question ÉPROUVÉE batterie (« Pourquoi le 07/08 ? », « Quel est l'effet de la chaleur sur mes ventes ? », « Génère le rapport de juillet »). Les 3 questions génériques MEURENT (_legacyBuildDynamicSuggestions conservé) ; label suggestions masqué (tout sous ACTIONS) ; cartes au look plein (valeurs de la carte finder). Vérifié navigateur sur module réel + monitor stubbé : états A/B/C + placement + label. Proto : public/prompt-empty-proto.html.** **Étape 1 attribution (07/08, docs/explorer-attribution-spec.md)** : sur les producers grounded (`grounded_day_claude`/`family_grounded_claude`), quand l'enveloppe porte `sentence_provenance` + `facts_catalog`, l'adaptateur construit un bloc `sourced` (segments = les entrées de provenance, chips = libellés des faits cités, dédupliqués, max 2, id hors catalogue ignoré) — **GARDE DE COUVERTURE (séquentielle — mesurée sur un run réel f10c3e58 07/08 : la provenance du modèle couvre AUSSI headline et key_facts) : on consomme dans l'ordre les entrées qui reconstruisent EXACTEMENT et INTÉGRALEMENT la réponse (les échos headline/key_facts sont sautés) ; tout écart ou reste → repli byte-identique sur le rendu prose actuel (prouvé au harnais PROVFB)**. DAY_DIMENSION : chips seulement quand le rendu actuel serait de la prose (les ROWS concurrents restent intacts — ADD, don't replace). Raccourci rapport (`reportPeriodFromText`) + CTA `date_range` INTOUCHÉS. **R2-5 (07/08, direction owner « tout au look bloc/carte ») : sur les chemins grounded couverts par la provenance, l'anatomie devient pilule → headline `variant:"lead"` (18px, chiffre d'abord) → sections chipées dans le conteneur carte (`sourced.card:true`) → rangée `action` (suggested_action serveur) → CTA ; les repli prose et les producteurs dialogue restent inchangés. Vérifié harnais (styles calculés : lead 18px, carte 0,5px/#e5e7eb/r10, rail #378ADD).** **R5 (08/08, owner) : bouton « Nouvelle conversation » (`data-ie-new-thread`, rangée visible seulement quand un fil existe) — purge store + frame + historique, page vide, focus input ; auto-expiration : restauration seulement si le dernier échange < 1 h (aller-retour rapport survit, visite du lendemain propre) ; 3 assertions navigateur (stale/fresh/clear) vertes. `?v=36`.** **R2-3 (07/08, bug owner « le rapport a effacé le fil ») : le fil PERSISTE en sessionStorage (`ms_ie_thread_<loc>`, 8 derniers échanges, enveloppe SANS debug sauf thread_context_out, garde 1,5 MB + retry quota) — `persistThreadEntry` sur chaque réponse ok, `restoreThread` au chargement re-rend via le MÊME `renderAiOutputHtml` + reconstruit CONVERSATION_HISTORY et le frame (mêmes fonctions que le chemin vivant) ; prouvé navigateur : 2 bulles + 5 chips + verdict restaurés depuis le store.** `?v=34`, card-kit `?v=45` sur prompt.astro. | `/api/insight/prompt`, `/api/insight/monitor`, `/api/insight/find-dates`, `/api/import/locations`, `/api/import/sales-csv`, `/api/competitive/competitor-signals` |
| `map-markers.js` (543 ln) | Leaflet marker rendering for the Insight Event map (`createVenueMarker`, `createEventMarker`, `createRoadMarker`, …) | (Leaflet layers) |
| `reco-library.js` | **`monthly_sales_hole`/`spike` (07/08 nuit, `?v=12`)** : 3 plans `_default` chacun (revue de comptes / en-cours / tournée ; qualification compte par compte / réassort / référence). **`weekly_sales_hole`/`spike` (07/08 soir, `?v=11`)** : 3 plans `_default` chacun (reconstituer la semaine avant d'agir / achats / animation ; identifier la cause du pic / réassort / rejouer en mesurant). **`client_dormant` (07/08, `?v=10`)** : 3 plans `_default` (appel direct pour établir la cause, raison datée de recommander, friction traitée avant le volume) — la carte ne connaît pas la cause de la rupture, les plans la font établir. **Conditionnalité par SIGNE MESURÉ (28/07)** : au-delà des clés `driver`, une entrée peut porter `enjeu_positif` / `enjeu_negatif` — `_recoSignKey(a)` lit `a.enjeu.eur_year` (classe du dayClassRegistry) et le geste suit le SENS de l'écart mesuré sur CE lieu ; sans mesure → `_default`, dont le geste est de MESURER, jamais d'affirmer une direction. Ordre de résolution : driver → signe → `_default` (le driver gagne, zéro régression sur les cartes ventes). Premier cas : `low_competition_window` — vérifié sur données réelles, les jours calmes valent +88 €/j sur un lieu et −49 €/j sur un autre, et 20 sites sur 24 n'ont aucune mesure. **Owner-editable** source of the recommended action PLANS (`window.MS_SALES_RECO_LIB` + `_BY_INDUSTRY` override) — structured `{title,description,why,tag}` objects (legacy strings still accepted), rendered premium on insight "Plan à essayer"; `action-cards.js` `_planText` flattens for the M'engager form + report. Loaded before `action-cards.js` on pulse + rapport + insight. **31/07** : `action-cards.js` DÉRIVE désormais son câblage des clés de ce fichier (fini la liste de 7 types recopiée) — une entrée ici = des plans dans « M'engager », sans second geste. Échafaudage commenté en fin de fichier pour les **27 types qui tirent sans plans** (mesuré 90 j : 33 tirent, 6 couverts), avec les variables de payload réelles de chaque type | — |
| `event-form.js` 🆕 | **MSEventForm — le formulaire de création d'événement-dispositif** (protos v2.1/v3 validés) : types par métier + nature ext/int + horaires + dispositif (versé aux bonnes pratiques) + responsable roster + KPI dominant (famille produit incluse, profit grisé sans marge déclarée) + CIBLE À DEUX LIGNES (apport propre jugé, total du jour en conséquence — attendu par jour de semaine du serveur) + Répéter (hebdo/mensuel, Du/Au). Création → POST saved-items/create ; récurrent → chaîne l'engagement de la 1re occurrence (`window_start_date`, origin `event_<type>`) — INTERIM documenté : verdict auto = CA vs attendu jusqu'à l'incrément 4 ; ponctuel → lien vers la comparaison legacy jusqu'à l'état Décider (incrément 3). Hébergé par evenement.astro (v=1) | `/api/insight/evenement?create_context=1`, `/api/channels/team`, `/api/saved-items/create`, `/api/commitments` **v5 (04/08, proto v4 validé) : le shopping de dates DANS le formulaire** — mini-grille mensuelle ‹ › (source `/api/insight/month`, même surface que le Calendrier, fetch parallèle + cache par mois), teinte = CA attendu par jour de semaine (dow_baseline), pastilles météo niv ≥ 2, ★ férié, souligné vacances, ligne vacances/périodes commerciales nommées, infobulle contexte par jour, chips FACTUELLES (meilleur jour de CA, fériés, risques — jamais un classement sur score plat), candidates conservées entre mois (max 7), champ « Durée : N jour(s) » (ponctuel seulement) → `duration_days` ; les inputs date libres sont remplacés par la grille ; prouvé vm 10/10 sur payloads réels f10c3e58 **v7 (05/08, retours owner écran réel)** : modèle de sélection EXPLICITE (sous-libellé « chaque clic = un jour de LANCEMENT ; à 3 jours, cliquer le 08 couvre 08→10 ») + la durée SE VOIT (jours couverts teintés 0.22, rafraîchis au changement de durée) ; souligné vacances retiré (effet 3D — contexte en ligne de mois + infobulle) ; chip « Meilleur jour de CA : samedi (≈ X €/j, vos ventes 90 j) — tout sélectionner » (fait calculé nommé comme tel, la valeur bouge avec la fenêtre 90 j : 1 097→1 098 constaté) |
| `draft-workspace.js` 🆕 | **THE shared Communiquer/Faire suivre workspace** (`window.MSDraftWorkspace`) — faithful extraction of Pulse's validated inline workspace (18/07), consumed by BOTH pulse.astro (Agir → Communiquer en interne) and insight.astro (hero Communiquer/Faire suivre): channel dropdown limited to CONFIGURED channels only (usableChannels(cfg) — note interne REMOVED, no dead options, owner 18/07; initial channel: Communiquer ("comm") = first configured SEND channel — NEVER a public fallback (18/07 bug: a no-send-channel venue opened Facebook); "offer" (Pulse Proposer une offre) = first configured PUBLIC channel; none → per-mode configure-CTA → /profile; resolved location_id + configured channels logged to `window.__msDbg.draftWorkspace`; EDITOR SHOWS IMMEDIATELY (owner 19/07): generation fills fields in place — visible « Rédaction/Ajustement en cours » state, text fields + actions frozen, recipient stays usable, stale responses dropped via `_genSeq` token, current draft in `_draftByIdx` (not wire's closure); module self-injects `@keyframes ms-spin` (#ms-dw-style) since pulse doesn't define it; the generated BODY reveals word-by-word via `MSTypewrite` (19/07 — title/hashtags instant, `_twByIdx` handle finished before any new generation, guard-fallback to direct set)), roster recipients w/ signal_routing suggestion on ALL send channels, fiche signal jointe, Copier/Enregistrer/Ajuster, Publier→/Envoyer→ (config-gated; note interne never gated), VISIBLE send errors, Automatiser, « Mes brouillons » drawer, unknown-recipient save. API channel always `note_interne` (display alias 'internal' never reaches APIs). Per-location caches (config/team/drafts). `open(mount, opts)` / `openLibrary` / `pickChannel` | `/api/channels/config`, `/api/channels/team`, `/api/analytics/list-drafts`, `/api/insight/generate-action-draft`, `/api/channels/publish`, `/api/channels/automation`, `/api/analytics/save-draft`, `/api/analytics/track` |
| `commit-form.js` | Shared "M'engager" commitment form (`window.MSCommitForm`), used by pulse feed + évolution page + insight deep pages + Consulter chat (Day 2). **Roster Responsable incassable (18/07): custom ownerPool/fetchOwners empty or failing ALWAYS falls back to the default self-fetch — a surface can enrich the pool, never silently kill the proposals.** **Objectif libre (18/07): the Modeste/Net blind chips are replaced by a % ⇄ € goal setter (slider 1–100, real-baseline anchor, detectability floor + non-blocking sub-noise warning, presets labeled with their real values, recap + CTA carrying the figure, `30 jours` window chip); self-fetches `?goal_context=1` per window; POSTs `threshold_basis:'pct'`+`threshold_pct`.** **Responsable streamlined (16/07): the pool SELF-FETCHES from `/api/channels/team` (the profile roster) when the caller passes only `location_id` (module-level cache, one fetch per page); chips carry name · role; last-used responsable remembered per location (localStorage `ms_last_owner_<loc>`) and PREFILLED when still on the roster (single-member roster prefills itself; a typed name is remembered on submit too). No caller wiring needed — insight's explicit `fetchOwners` still honored. `?v=3` on all four consumers. **Pulse MIGRATED (16/07 — parity debt CLOSED): the feed's `[data-agir-commit]` handler now builds/wires THIS form (inline cmChip*/buildCommitFormHtml/wireCommitForm deleted); the migration added the two pulse-only features to the shared form — `opts.suggestions` (reco rows above the textarea, click-to-fill) and `origin.creation_baseline_daily` in the POST (→ `window_expected_revenue`; the shared form previously DROPPED it). Pulse passes its cross-site roster fallback via `fetchOwners` and keeps its onDone (success + `renderEngagements` refresh). ONE commit form everywhere: pulse, chat, évolution, insight.** **Typewriter (19/07): a prefilled plan and a clicked suggestion reveal word-by-word via `MSTypewrite` (ms-loader; guard-fallback to direct set); `?v=6` on all four consumers.** **Bonnes pratiques (26/07) : self-fetch `/api/best-practices` quand `opts.origin.origin_action_type` est présent — les pratiques du lieu appariées s'insèrent AU-DESSUS des suggestions génériques dans « Mon action » (rangées signées/datées avec tier « prouvée » vert / « déclarée » gris, click-to-fill identique) ; échec/vide → formulaire inchangé. `?v=7` sur les quatre consommateurs.** **Validation bruyante (26/07 soir, retour de test) : le submit ne fait plus un `return` muet — champs manquants surlignés (#B45309) + ligne « Il manque … pour vous engager » + focus ; `?v=8`.** **« Références de votre secteur » AJOUTÉ puis RETIRÉ le 27/07 (`?v=11`) — décision owner après test : le store best-in-class produit des PREUVES (« X a fait Y »), pas des PLANS (le how) ; ce slot demande le how. Les cas crawlés restent sur la page évolution + insight « Plan à essayer ». Les 3 méthodes = bonnes pratiques du lieu puis reco-library. Voir `docs/best-in-class-registry.md`.** **Origin complet depuis insight (26/07) : `fsOpenCommit` (insight.astro, wireActionBar) passe l'origin COMPLET construit depuis la candidate aplatie `feedItem` — `origin_driver` (primary_revenue_driver‖dominant_factor), `origin_suppression_key`/`origin_card_instance_id`, `origin_affected_date` (YYYY-MM-DD via `fsOriginYmd`, repli signal_date), `creation_residual_pct/z`, `creation_confidence_tier`, `creation_baseline_daily` (avg_30d puis expected_revenue) — même mapping que le wire M'engager de pulse ; avant, seul `origin_action_type` partait et le POST /api/commitments avait origin_driver/creation_* à null depuis les pages profondes. Prouvé par harness Node rejouant le code extrait verbatim de la page sur lignes BQ réelles (f10c3e58 sales_discount_no_lift + sales_revenue_down_wow réel).** | `/api/commitments`, `/api/best-practices`, `/api/channels/team` |
| `public/*-proto-data.js` + protos/harnais 🆕 | **Captures réelles gelées pour maquettes et harnais** (chantiers Piloter/Agir/onboarding, ≤ 24/08) : `agir`/`competitor`/`dossier`/`engagement-kpi`/`events-criteres`/`fiche-enrich`/`hero-kpis`/`piloter(-autour/-full/-ops)`/`savoir-tile`/`tableau`-proto-data.js — payloads réels du compte figés, consommés par les protos (`dossier-proto.html`, `events-criteres-proto.html`, `party-role-proto.html`, `rapport-canaux-proto.html`, `tableau-proto.html`) et harnais (`bilan-harness.html`, `channels-render-harness.html`) ; AUCUNE page app ne les lit — outillage de conception, servi par le launch `proto-static` (port 4173). Capture/revue : `scripts/shot-capture.ts`, `scripts/copy-review*.ts`, `scripts/agir-proto-harness.ts`, `scripts/_tmp-*.ts` (audits ponctuels) ; tirages JSON dans `shots/` | — |
| `bp-form.js` 🆕 | Formulaire « Enrichir vos bonnes pratiques » (`window.MSBpForm`) — la branche POSITIVE de M'engager (carte opportunité, `data-agir-positive` posé au build pulse). Grammaire visuelle MSCommitForm ; pistes cliquables tirées du driver de la carte ; levier-moyen en chips (seule taxonomie choisie) ; roster `/api/channels/team` (j.items) ; AUCUN champ obligatoire + deux sorties honnêtes de première classe (« c'était le contexte » / « je ne peux pas l'expliquer ») qui n'écrivent rien (log analytics/track seulement) ; « Ajouter + m'engager à la rejouer » enchaîne sur MSCommitForm prérempli puis PATCH lie l'engagement. `?v=1` sur pulse | `/api/best-practices`, `/api/channels/team`, `/api/analytics/track` |
| `card-kit.js` 🆕 | **SINGLE source of the card-detail render kit** (`window.MSCardKit`): shared components (`msTable`, `msSortTable`, `msMovers`, `msStrip`, `msDecision`, `msDeltaCell`, formatters) + PURE renderers (json→HTML, no fetch/DOM): system cards `renderWeather/renderSales/renderAudience/renderTrackRecord/renderEvents/renderCompetitor/renderTourism/renderFootfall`, and the USER-generated card `renderEvolution(data, COPY)` (the commitment "Consulter l'évolution" doc — **jauge demi-cercle tricolore POSITION + points pairs + courbe en unité du KPI déclaré avec lignes habituel/objectif (15/08, `data.kpi`; repli barre % si absent)**, chart, decision headline, advice, capture, sources; 19/07 proto v3: `data.site_name` renders as plain grey 13px text right of the « Engagement » kicker IN THE DOC HEADER — inline flex on that line only; card-kit `?v=30` on engagement). `insight.astro` loaders + `engagement.astro` fetch then delegate here (engagement keeps only the capture/advice WIRING). **Phase 3 — `renderAnswerBlocks(blocks)` + `mdBlockToSafeHtml`: THE one renderer for the Consulter chat answer.** Block vocabulary `register / headline / verdict / prose / facts / datecards / lookup / rows / caveats / cta / card / clarification`; `msDecision` lines carry `class="ms-decision-line" data-dl-head/body` (additive markers for the chat commit decoration — no visual change); `segment` = per-block PROVENANCE box (R2, 17/07: a labelled vetted/web/model container for the sub-part of a MIXED answer whose register differs from the top pill — the premise verdict inside a web/model entity answer ships « Vérifié · vos données »); `cta` has an ACTION variant (`{action, label}` → `<button data-ab-cta-action>`, behavior wired by the consuming surface's delegation — used by the elicit upload CTA) alongside the `/`-url link variant; every primitive's style values COPIED from the prompt.astro `.ie-*` classes they replace (no invented colors). Truth rules IN the renderer: register REQUIRED (missing → least-trusted "Non vérifié" pill + console.error, never silent; clarification-bearing sets exempt — a question asserts nothing; same exemption for any block flagged `asserts_nothing:true`, used by the elicit answers — the system ASKING for data asserts nothing); prose = escape-FIRST then whitelist (**gras**/*italique*/`- ` bullets/paragraphs; #titres/tables/links/HTML stay inert text). Verified offline by `card-harness.html` (renders real `f10c3e58` JSON) — page == harness. **`sourced` (Étape 1 attribution, 07/08)** : segments de réponse validée + chips d'origine en fin de section (décision owner) — texte via le MÊME pipeline `mdBlockToSafeHtml` que `prose` (typo identique), chips gris (valeurs de la pilule web), display-only, n'améliorent JAMAIS la pilule registre. **R2-5 (direction owner 07/08 « tout au look bloc/carte ») : `sourced.card:true` = conteneur carte discret (0,5px #e5e7eb, radius 10) autour des sections chipées ; primitive `action` = la rangée « Action du jour » (rail bleu datecards #378ADD) portant `suggested_action` — la VRAIE carte déclenchée serveur, jamais du texte modèle, jusqu'ici calculée puis JETÉE par le client** | consumes the `insight/*` + `commitments/evolution` card JSON |
| `card-harness.html` 🆕 (dev) | Render harness — loads `card-kit.js` with real `f10c3e58` fixtures (sales, weather rich + honest-absence, audience, track-record both branches) so card output is verified in a browser BEFORE shipping (page is Clerk-gated). **Phase 3 section: drives the REAL `ie-prompt.js` adapter (`window.__ieBlocksFromResponse`, stub `#ie-prompt-root`) over captured live payloads (top-days / grounded day / clarification / compare / family-led) + a markdown zone + an XSS zone with a structural PASS/FAIL assertion (0 active elements) + the forced no-register pill demo. **Zones PROV/PROVFB (Étape 1, 07/08) : assertions PASS/FAIL structurelles — chips attendus rendus (id hors catalogue ignoré), rangée « Action du jour » (R2-5), ET repli byte-identique au rendu B quand la provenance ne couvre pas la réponse. PROV porte la forme RÉELLE de la provenance (échos headline/key_facts à sauter — couverture séquentielle).** Serve `public/` + open; not linked from the app | `card-kit.js`, `scripts/ie-prompt.js` |
| `reactions.js` (67 ln) | Type B read-only "Comment votre lieu réagit" renderer; paints pre-cited lines only | `/api/insight/sensitivities` |
| `ms-loader.js` 🆕 | **Canonical page loader** (`window.MSLoader`): central per-page message map + `MSLoader.html(pageKey)` (returns the branded `.ms-loader` markup) + a single global ticker that rotates every visible `.ms-loader__msg[data-ms-loader]` label through its page's messages. Styling in `global.css` (`.ms-loader*`, brand font, negative-margin fix for the gif's baked-in padding). Loaded on pulse/monitor/insight/days/events/competitor/engagement/reactions/family-report/suivis/prompt; replaces every page's hand-rolled loader. BaseLayout's corner loader is now marketing-only (`!isApp`). **Also hosts `window.MSTypewrite` (19/07): word-by-word reveal of ALREADY-VALIDATED text into an input/textarea (pure presentation — the draft validator needs full text, so no real token streaming); time-elapsed pacing (throttle-proof) + `duration+250ms` completion backstop; interrupted by mousedown/keydown/paste on the field or CAPTURE-phase mousedown anywhere in `container` (fires before click handlers read `.value`); programmatic focus does NOT interrupt. Consumers: draft-workspace.js (body fill), commit-form.js (prefill + suggestion click) — both guard-fallback to direct set. `?v=2` on pulse/insight/engagement/prompt.** | — |
| `kpi.js` (19 ln) | Plausible analytics event tags (`data-kpi`, PDF downloads) | — |
| `vendor/leaflet/leaflet.js` | Self-hosted Leaflet (never CDN — per CLAUDE.md) | — |

### Reusable components (`src/components/`)

`Nav.astro`, `Footer.astro`, `InsightBottomBar.astro`, `PiloterBottomBar.astro`, `OfferCard.astro`, `SectionQuestionnaire.astro`; `ai/PackagerCard.astro`, `ai/ir/lookup_ir_v1.ts`, `ai/render/shared/render_points_cles_v1.ts`; `home/` (`Hero`, `MiniOffers`, `Proofs`, `SocialProof`).
